/** Not mocked; the empty state until Kalp files something. */
export default function HobbiesWindow() {
  return (
    <div className="kos-body">
      <p className="kos-muted">Nothing filed yet.</p>
    </div>
  );
}
