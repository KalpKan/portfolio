# STATUS — Kalp's portfolio platform

_This is Kalp's dashboard. Updated after every task. Newest at top of each section._

## Needs Kalp (human checkpoints)

### H0 — one-time logins and keys: ✅ DONE 2026-09-18
All operator keys received and verified (PostHog, Cloudflare, UptimeRobot, Neon, Supabase access token). Stored only in `~/.config/portfolio-ops/secrets.env` (owner-only). Vercel uses the already-logged-in CLI. The Vercel/Supabase MCP OAuth flows kept expiring, so the CLI + Management API route is used instead; nothing further needed.

### H1 — buy the .com domain: ✅ DONE 2026-09-18 (`kalpkan.com`)
Bought on Cloudflare Registrar (zone active, Free plan). Same day the hub went live at **https://kalpkan.com** (www redirects to it) and the basketball dashboard at **https://hoops.kalpkan.com**, both with valid TLS. Records for the other subdomains are added as each Vercel project is created (`docs/DNS_PENDING.md`).

### H13 — let agents delete throwaway repos (1 minute, whenever)
The `gh` login on this Mac has the `repo` scope but not `delete_repo`, so the T1.2 smoke-test repo `KalpKan/template-smoke` could only be archived. In Terminal: `gh auth refresh -h github.com -s delete_repo` (one browser click), then `gh repo delete KalpKan/template-smoke --yes`. Nothing else depends on it.

### H6 — hub hero copy + contact links (cosmetic, whenever)
The hub hero currently shows a bio line the worker wrote: "Western University. Working toward physician-scientist work in neurotech…" (clamped to two lines on phones). Reply keep / edit (give the text) / cut. Also say which contact links to show under it, if any: email, GitHub (KalpKan), LinkedIn URL. The contact row is already built (`components/ContactRow.tsx`) and hidden while the three values in `lib/site.ts` are empty, so filling it in is a one-line edit and a push, no design work.

### H3 — media for showcase pages (whenever convenient)
Projects now: UnPark/Antifreeze, Automatic RC Car, Yash Birthday PCB, DIY EEG (under construction), FlashCards (placeholder page).
The case-study pages are live with real prose; wherever a photo, screenshot or video is missing the page shows a dashed block that says exactly what is wanted. Drop files in `~/projects/portfolio/media-inbox/<project>/` (git-ignored) and tell Claude, or send YouTube links for video. iPhone HEIC is fine; Claude converts. Exactly what each page wants:
- **UnPark / Antifreeze:** 2 device photos (the Pi + MPU6050 as worn, and the bench setup) · 3 app screenshots (dashboard, episode list, analytics) · one 30–60 s demo video (unlisted YouTube link).
- **Automatic RC Car:** the two clips already on your Mac, `~/Desktop/Out and About/Sidequest/Automatic-RC-Car/Videos/Car Video.mp4` (100 s) and `Autonomous Car Video.mp4` (10 s), uploaded to YouTube as *Unlisted*; send the two links · optional: 2 phone photos of the car (the page currently uses frames pulled from the videos, which are soft).
- **Yash Birthday PCB:** 2 photos of the real board (front lit up in a USB-C port, and the back) · one sentence on which LED colours you used. The page already shows KiCad 3D renders of both sides, your layout screenshot and the schematic.
- **DIY EEG:** an LTspice plot of `V(ain0)` and `V(ad_out)` from `EEG_Simulation.cir` (run it, then File → Export or a screenshot) · a breadboard photo once parts arrive · say whether the 17-page plan PDF may go public. The page already shows the KiCad schematic.
- **FlashCards:** page is the short "under construction" placeholder per your note; when you want it filled: 5 iPhone screenshots (sign-in, sets list, one set, the study card, the widget) · optional 20 s screen recording.
- **classmyschedule:** nothing until H9 is answered.

### H14 — DIY EEG: link it from the hub? (10 seconds, whenever)
The EEG page is published as "under construction" at **https://kalpkan.com/projects/eeg** (schematic, the signal chain with the real part values, LTspice numbers, what is and is not done). Per your note the registry status stays `coming`, which means the hub's project list still says "case study soon" and does not link to it. Reply **"link EEG"** and Claude flips the status to `live` so the row links; or leave it until the breadboard exists.

### H10 — approve the RC-car history rewrite (found 2026-09-18, T4.1)
`KalpKan/Automatic-RC-Car` is 943 MB to clone because 64,273 training images sit in an old commit even though the current files are clean. **PR #1** (https://github.com/KalpKan/Automatic-RC-Car/pull/1) is safe to merge now: it only adds a README that links the case study. Shrinking the clone needs a history rewrite and a force-push, which changes every commit id; the exact commands are in the PR. Reply **"purge RC car history"** and Claude runs them (or run them yourself from the PR), then re-clone any local copy.

### H11 — emotes: try it on your phone (2 minutes, whenever)
Open https://emotes.kalpkan.com on your phone, allow the camera, and do a flex, a yawn and a thumbs-up. The worker could only prove the demo mode and the unit tests (no real webcam or phone in the agent sandbox); the camera path is the same code but a real face in real light is the honest test. If a gesture never fires, say which one and the T5.b hardening loop tunes that rule's thresholds (`src/gestures/*.ts`, each has a fixture test).

### H12 — Plant It: two API keys (optional) + one sign-in click (found 2026-09-18, T2.1)
`https://plantit.kalpkan.com` is live and fully usable in **demo mode**: identification comes from a bundled list of 5 common houseplants and care guides from a built-in library, so nothing is billed. To get real identification and species-specific care guides, paste two keys into Vercel → project `plantit` → Settings → Environment Variables (Production and Preview), then Deployments → Redeploy:
1. `PLANTNET_API_KEY` — from https://my.plantnet.org/ (free, 500 requests/day). Turns on real species identification.
2. `OPENAI_API_KEY` — from https://platform.openai.com/ → API keys (set a low monthly budget there too). Turns on care guides for species outside the bundled library.
Both are capped by the app at 50 calls per day each (`PLANTNET_DAILY_LIMIT` / `OPENAI_DAILY_LIMIT`, Firestore counter); past the cap the demo path answers, so a busy day can never cost more than the free tiers. `/api/health` shows `identification: "plantnet"` / `care: "openai"` once the keys are in.
Also: the agent could not complete Google sign-in in the browser (choosing an account in the Google chooser grants OAuth as you, so it stopped there; the signed-in flow was proven through the API with a throwaway token instead). Please sign in once at https://plantit.kalpkan.com, add a plant from any photo, open it and press "Water now" on the Simulated sensor — 1 minute — and tell the next agent if anything looks wrong. Optional tidy-up: one orphaned 22 KB smoke-test photo `e2e-smoke-plantit/ouC7NUpSSivAKvdZNEKL.jpg` is left in Supabase Project B → Storage → `plantit-photos` (the agent had no local copy of the service-role key); delete it from the dashboard whenever.

### H4 — decide later
Make `promptflip` and `FlashCardsApp` public on GitHub once Claude marks them "ready".

### H5 — promptflip deployed twice on Vercel: ✅ RESOLVED 2026-09-18 ("keep 35qv")
Kalp chose **keep `promptflip-35qv`**. Done the same day: pre-delete checks re-run (35qv `HTTP 200` + `/api/health` `db: ok`, Ready build `dpl_4GkXsM4AYFbeQPLsRtEJFpyNh1ah`; `promptflip` 404 `DEPLOYMENT_NOT_FOUND`, `inspect` "Can't find the deployment"); env-var names compared (same six on both, nothing lost); broken project `promptflip` (`prj_rSr9QbGDbB2FLOeAAilYmqO5DKL1`) deleted; `~/projects/promptflip` re-linked to `promptflip-35qv` (`prj_t2eXoIxI2HN3snfyFpGAkTqHa4VD`); `promptflip.kalpkan.com` attached (DNS-only CNAME, TLS issued); `NEXT_PUBLIC_APP_URL` → `https://promptflip.kalpkan.com` and the build redeployed; Supabase Auth Site URL + redirect list updated (old `*.vercel.app` entries kept); UptimeRobot monitor moved. Evidence: `docs/DNS_PENDING.md` §5, `skills/portfolio-ops/incidents.md` 2026-09-18 resolution line. Vercel project name is now simply `promptflip-35qv` everywhere; `https://promptflip-35qv.vercel.app` keeps working as a fallback alias.

### H7 — rotate Supabase Project B's JWT secret? (found 2026-09-18, T1.1; low risk, your call)
While exposing the `hoops` schema, an agent printed the full Management API `GET /postgrest` response, which includes Project B's `jwt_secret`, into its local Claude Code transcript on your Mac (nowhere else: not in a repo, commit, or Vercel). Rotating it (Supabase → project `platform` → Project Settings → API → "Generate new JWT secret") also regenerates the `anon`/`service_role` keys, so Claude would then re-set `SUPABASE_ANON_KEY`/`SUPABASE_SERVICE_ROLE_KEY` on the basketball Vercel project and redeploy (10 minutes, no data impact). Reply "rotate B" to do it, or ignore if you are fine with a local-only exposure. Details: `skills/portfolio-ops/incidents.md` 2026-09-18.

### H8 — drop the old `public` basketball tables in Project B? (found 2026-09-18, T1.1)
Basketball now lives in the `hoops` schema; the restored project still holds the identical old copies `public.sessions` (4 rows) and `public.shot_events` (92 rows). They cost nothing and nothing reads them. Reply "drop public copies" and Claude will remove them; otherwise they stay as a backup.

## Spend tracker
| Item | Monthly (CAD) |
|---|---|
| `kalpkan.com` domain (Cloudflare Registrar, at-cost renewal, roughly $10.50 USD/yr) | ~$1.30 |
| Everything else (Vercel, Supabase, Neon, Firebase, UptimeRobot, PostHog) | $0 |

## Tasks
| ID | Phase | Task | Status | URL | Verified by | Date |
|---|---|---|---|---|---|---|
| T1.2 | 1 | New-project template repo (Next.js + schema-scoped Supabase client + migration 0001 + `/api/health` + PostHog via `/ingest` + footer + registry stub) | done (public GitHub template `KalpKan/portfolio-template`, CI green, 28 tests; proven by a timed throwaway spin-up `template-smoke`: create → install → test → build → Vercel deploy → `curl /api/health` = `{"ok":true,"service":"template","db":"skipped"}` in 10 min 05 s (build 16 s, the rest Hobby build queue), then the Vercel project removed; the throwaway repo is archived, not deleted, see H13; repo was private until the resumed worker's check, made public after a secret scan) | https://github.com/KalpKan/portfolio-template (runbook "Create a new project from the template") | worker self-verified 2026-09-18 (vitest, lint, build, local health, gh/vercel state) | 2026-09-18 |
| T4.1 | 4 | Showcase / case-study pages on the hub + RC-car repo purge | done (live with real prose: UnPark, RC car, Yash Birthday PCB (KiCad renders + schematic, written from the .kicad files), DIY EEG (under construction, from the schematic + LTspice netlist + plan PDF; registry stays `coming`, H14); Outline removed and FlashCards reduced to the placeholder page per Kalp 17:57; every missing photo/video is a labelled placeholder, list in H3; classmyschedule held back, H9; RC-car purge PR #1 open, H10; registry↔content, media-size, hardware-facts and render tests (55 pass); Lighthouse see session log) | https://kalpkan.com/projects/unpark, /projects/rc-car, /projects/yash-birthday-pcb, /projects/eeg (unlinked); PR https://github.com/KalpKan/Automatic-RC-Car/pull/1 | | 2026-09-18 |
| T0.0 | 0 | Living ops skill scaffold | ✅ verified | skills/portfolio-ops/ (installed to ~/.claude/skills/portfolio-ops/) | verifier 2026-09-18 | 2026-09-18 |
| T0.1 | 0 | Hub repo + Next.js skeleton | ✅ verified + polished (code review, design review, polish verifier all passed) | https://kalpkan.com | verifier 2026-09-18 | 2026-09-18 |
| T0.2 | 0 | Cleanup old Vercel projects + archive repos | ✅ verified (partial by design: `tokengamblecoinflip` deleted; `token-gamble-coinflip` + `token-coinflip` archived; `promptflip-35qv` kept, see H5) | https://github.com/KalpKan/token-gamble-coinflip, https://github.com/KalpKan/token-coinflip (archived) | verifier 2026-09-18 | 2026-09-18 |
| T0.3 | 0 | UptimeRobot monitors | ✅ verified (3 monitors up, email alerts, public status page, $0) | https://stats.uptimerobot.com/a6n3Wx3PBp (inventory: docs/monitors.md) | verifier 2026-09-18 | 2026-09-18 |
| T0.4 | 0 | DNS_PENDING.md | ✅ reviewed (plan); **executed for hub + hoops + promptflip** 2026-09-18 (promptflip after H5 resolved); plato/plantit/pushups/emotes/microtubules wait on their Vercel projects | https://kalpkan.com, https://hoops.kalpkan.com, https://promptflip.kalpkan.com, docs/DNS_PENDING.md §5 | reviewer 2026-09-18 (plan) | 2026-09-18 |
| T2.1 | 2 | Plant It (PlantWater) repaired and live: CRA static + Express `/api` function on Vercel, Firebase stays Spark, photos in Supabase Project B bucket `plantit-photos`, simulated sensor + Water now, per-day spend guard, demo identification without keys | done (live; `/api/health` → `firestore: ok`; API flow proven end to end on the live host: identify → Supabase photo → simulated device → Water now event → delete; PostHog `plant_photo_uploaded`, `plant_identified`, `$pageview` arriving from the host; UptimeRobot `804031032` on the status page; 28 jest tests; browser proof stopped at the Google account chooser, H12) | https://plantit.kalpkan.com (fallback `https://plantit-kappa.vercel.app`; plan docs/superpowers/plans/2026-09-18-plantit.md) | | 2026-09-18 |
| T1.4 | 1 | Microtubule quantifier in the browser (OpenCV.js) | done (live; browser result equals the Python pipeline to 4 decimals on 3 recovered sample cells, vitest-checked; works at 390 px with no server calls beyond `/ingest`; Lighthouse 0.98; PostHog `sample_loaded` + `image_analyzed` arriving; `run_analysis.py` takes flags) | https://microtubules.kalpkan.com (health: /health.json) | | 2026-09-18 |
| T1.3 | 1 | Plato on Vercel Python + Neon | done + reviewer fixes applied (live, E2E proven in Chrome: real KIN 2000 outline → 7-event .ics; PostHog events arriving; fixer 2026-09-18: `/ingest` header whitelist so the session cookie never reaches PostHog, SDK cache headers, custom lead-time mapping kept, unique `/tmp` uploads with cleanup, one upsert per edit, `/download` guard; 29 tests) | https://plato.kalpkan.com (health: /api/health) | | 2026-09-18 |
| T1.1 | 1 | Supabase Project B "platform" + basketball on `hoops` schema | done (Project B restored + renamed `platform`; `hoops` schema live with 95 shots; dashboard `source: live`, no demo banner; Edge Functions `hoops-ingest-shot` + `health`; PostHog `session_viewed` + `shot_ingested` arriving; keep-alive monitor 804030499) | https://hoops.kalpkan.com (data: /api/dashboard; Project B health: https://yzppfufqaekgaxcrsqxp.supabase.co/functions/v1/health) | | 2026-09-18 |
| T5.a | 5 | Harden to consumer grade: plato (4 rounds), hoops (2), microtubules (2) — spec → test → fix loop → review → verify | **paused 17:51 EDT, resumable** (see docs/RESUME-HARDENING.md; specs done, first test round done, hoops fix interrupted) | docs/reports/ | | 2026-09-18 |
| T5.a-microtubules-spec | 5 | microtubules spec + labelled corpus for the audit | done (spec `docs/reports/microtubules-spec.md`: 10 stories with measurable bars; corpus of 36 original cells + 4 whole-well + 25 edge files with Python ground truth committed to `KalpKan/Microtubule-Quantification` `tests/fixtures/`; vitest 57/57 with diff 0.0000 on all 52 PNGs; browser run on JPEG/WebP/BMP/EXIF/12-24 MP all within 0.01 pt; gaps: generic wrong-file message + stale result, 24 MP freezes 15 s / 475 MB, result below the fold at 390 px, no explanation of the number, no download) | docs/reports/microtubules-spec.md | | 2026-09-18 |
| T5.a-hoops-spec | 5 | hoops spec + ground-truth fixtures for the audit | done (spec with 10 stories and measurable bars; 95-row snapshot + independent metric generator committed to `KalpKan/Basketball-Stat-Tracker` `b34f00e`; live numbers match ground truth per UTC day; 3 product defects logged in incidents.md, not fixed: React #418 hydration + 1970 "Jan 1/Dec 31" session, Consistency basis mismatch + eFG% 150 %, no iOS-stub notice / misleading DB-failure banner) | docs/reports/hoops-spec.md | | 2026-09-18 |
| T5.a-hoops-r1 | 5 | hoops TEST + CRITIQUE round 1 (real browser, Playwright in 3 timezones × 2 widths × 2 schemes, Lighthouse ×3, ingest exercised, DB-failure simulated) | done: **PARTIALLY WORKING**, 1/10 stories meet the bar, score 32/100. Blockers: Progress chart bars render at 0 px (empty box); React #418 + the same session dated differently on pill/chart/table and across timezones (Sep 18 becomes Sep 19 in Tokyo). Majors: 1970 epoch session shown as "Dec 31" and ingest accepts pre-2000/future timestamps; Consistency basis mismatch; eFG 150 %; no iPhone-stub notice; phone table cut off + 36 px toggles + contrast; DB failure shows sample numbers with a "set SUPABASE_URL" banner and no /api/health; cross-device day merge hides new sessions. Next: FIX round 1 | docs/reports/hoops.md | | 2026-09-18 |
| T5.a-plato-spec | 5 | plato spec + labelled corpus + scorer for the fix loop | done (spec with 10 stories and a measurable bar; 42-PDF corpus at `~/projects/plato-corpus/` (outside git), 10 outlines labelled by hand in `KalpKan/Plato` `tests/corpus/ground_truth/`, `run_extractor.py` + `score.py` + gated `tests/test_corpus.py`; baseline: dated assessments correct 14 %, term dates 0/10, sections 27 % recall, and the live .ics puts every undated assessment on the term end date; 5 defects logged in incidents.md, none fixed) | docs/reports/plato-spec.md | | 2026-09-18 |
| T3.2 | 3 | Clash Royale emote detector in the browser (MediaPipe Face/Hand/Pose landmarkers, 3 ported gesture rules) | done (live; `KalpKan/emote-detector-web` public, Python original preserved in private `KalpKan/clash-emote-bot-python`; 33 unit tests on landmark fixtures + a headless demo-replay test; webcam + demo mode; MP3s ~25x smaller than the WAVs; no network after load; Supercell notice in footer + README; MobileNetV2 not shipped, said so in README; Lighthouse perf 0.91; PostHog `session_started`/`emote_fired`/`demo_video_played`). Resumed after the first worker was stopped waiting on a queued Hobby build (incidents.md) | https://emotes.kalpkan.com (health `/health.json`; fallback `https://emotes-eight.vercel.app`; note `emotes.vercel.app` is a stranger's project) | worker self-verified 2026-09-18 (curl, vitest, Lighthouse); reviewer pending | 2026-09-18 |
| T5.b | 5 | Harden to consumer grade: plantit, pushups (real-time webcam), emotes (detection) — starts when their builds finish | queued | | | |
| T5.1 | 5 | Plato parser quality: on the KIN 2000 outline all 6 assessments land on the term end date (2026-04-30) and titles carry trailing digits ("Tracker 11", "Plan3"); the .ics parses but is not useful to a student until dates match rows (incidents.md 2026-09-18 "Plato extractor puts every assessment on the term end date") | queued (after all deploys; product defect, not hosting) | https://plato.kalpkan.com | | |
| T0.5 | 0 | PostHog analytics | done (project "Kalp portfolio" 616829 on the free plan, no card, hard-capped at the free allocation; hub sends cookieless via `/ingest`, `project_card_clicked` verified from a real browser click; dashboard with 3 insights; contract in docs/analytics.md) | https://us.posthog.com/project/616829/dashboard/2112106 (contract: docs/analytics.md) | | 2026-09-18 |

## Decisions log
- 2026-09-18: H5 resolved by Kalp: keep `promptflip-35qv`, delete `promptflip`. Rationale: 35qv is the only project whose builds ever reached Ready and it serves the URL already in use; deleting it would have changed the public URL and needed a build fix first. The `-35qv` suffix stays in the Vercel project name (renaming is cosmetic and would invalidate the local link again); the public name is `promptflip.kalpkan.com`.
- 2026-09-18: DNS records use the project-specific `<hash>.vercel-dns-017.com` CNAME that `vercel domains verify` prints, not the generic `cname.vercel-dns-0.com` from the docs; the apex uses the single `A 76.76.21.21` that `inspect` printed rather than Vercel's newer two-address pair, to keep one record per host. UptimeRobot monitors stay on the `*.vercel.app` hosts for 24 h before being moved.
- 2026-09-18: T0.3 monitors the basketball dashboard at its public alias `v0-basketball-analytics-dashboard-seven.vercel.app`, because the team-scoped alias redirects every visitor to a Vercel SSO page. UptimeRobot v2 write API is blocked on the Free plan; v3 is used for writes (runbook updated).
- 2026-09-18: Supabase Management API shows 4 projects: PromptFlip (active), ShootIt = basketball (paused), plato-course-converter (paused), "KalpKan's Project" (paused). Paused projects do not count toward the 2-active cap. Plan: restore ShootIt as Project B "platform" in T1.1; leave the other two paused (Plato goes to Neon). Never have more than 2 active.
- 2026-09-18: T0.2 kept `promptflip-35qv` instead of deleting it: it is the live promptflip (READY, health+DB ok); the locally-linked `promptflip` project is the broken one. Escalated as H5 rather than guessing.
- 2026-09-18: `/goal` mode rejected the prompt (4000-char limit), so the orchestration runs in the interactive session instead. Same operating model (worker → reviewer → verifier).

## Session log
- 2026-09-18: T1.2 fixer pass after verifier rejection. (1) Template README "Verified" Health row now states the pasted body was re-captured at 22:05Z before teardown and that the 10 min 05 s figure comes from the start marker and the first curl (`KalpKan/portfolio-template` `635e12e`). (2) incidents.md `fetch failed` entry: appended a correction note, the Date should read `~20:19Z (16:19 EDT)`. (3) `KalpKan/template-smoke` deletion still needs Kalp (H13 stays open): the `gh` token has no `delete_repo` scope and the agent permission layer also refuses repo deletion; verification.md row "Throwaway removed" keeps `archived` until H13 closes. No plan, deploy, or Supabase change.
- 2026-09-18: T1.2 done (resumed). Template `KalpKan/portfolio-template` (public, `is_template`): Next.js 16 App Router + TS + Tailwind 4, vitest (7 files / 28 tests), zod-validated env, `getSupabase()` scoped to `NEXT_PUBLIC_APP_SCHEMA` (null without env), `supabase/migrations/0001_create_schema.sql` with `__APP__` placeholder + grants + default privileges + `health_select_one()` and the "expose schema" comment, `/api/health` → `db: ok|skipped|error` (503 on error, `no-store`), PostHog files copied verbatim from the hub with `lib/events.ts` TODO, footer "part of kalpkan.com", CI (lint, migration guard, test, build), non-developer README with the `projects.json` stub and `supabase gen types` step. Throwaway `template-smoke` spun up end to end in 10 min 05 s and its Vercel project removed (README "Verified"). Found on resume: the first worker had pushed everything but written no ops record and the repo was private; fixed both (4 incidents.md entries: `vercel deploy` false `fetch failed`, `delete_repo` scope missing → H13, worker stopped before records, repo created private). Plan: docs/superpowers/plans/2026-09-18-template.md.
- 2026-09-18 18:15: T2.1 done (resumed worker). Previous worker's repair (`KalpKan/PlantWater` `c604ac9`: Express as `/api` function, CRA static, Supabase photos, simulated sensor, spend guard, demo plants, PostHog) pushed; found + fixed two production bugs the tests missed (`0f068db`: `Timestamp` missing from the Admin wrapper made every write route 500 while health was green; `alert()` froze the login tab) and one data bug (`7d650bf`: deleting a plant left its `events` subcollection). `plantit.kalpkan.com` attached (CNAME `370dd6cbaae773c9.vercel-dns-017.com`, Cloudflare `8e680afcb9cdf93bef7933944f5e9b4f`, HTTPS 200 in ~35 s), Firebase authorized domains updated (added `plantit-kappa.vercel.app`, removed the foreign `plantit.vercel.app`), UptimeRobot `804031032` added to the status page, `projects.json` live. Live API proof (throwaway uid, deleted after): identify → `Ficus lyrata` 0.86 demo, photo at `.../plantit-photos/<uid>/<id>.jpg` 200, device `mode: simulated` VWC 45, water `ok: true` event `source: simulated`. Google sign-in stopped at the account chooser (H12); enabling anonymous guest sign-in was refused by the permission system, so no guest mode shipped. Plan: docs/superpowers/plans/2026-09-18-plantit.md.
- 2026-09-18 18:05: classmyschedule removed (Kalp: not his work). H9 closed.
- 2026-09-18: T3.2 done (resumed). `emotes.kalpkan.com` attached (CNAME `27cc1c6040a60ce2.vercel-dns-017.com`, DNS-only, record `299df004202d8df449da623799220231`); the first worker had pushed the repo, linked Vercel project `emotes` (framework Vite, env names set, GitHub-connected) and written the runbook/settings/verification rows, then stopped while the production build sat ~90 min in Vercel Hobby's one-build-per-team queue. Live checks: page + health 200 on Vercel, four ML files + MP3 served with immutable cache, `/ingest/e/` → 400, Lighthouse 0.91, 33 tests. Two incidents recorded (build queue; negative DNS cache on the campus resolver). `projects.json` emotes → live. H11 added (try it on your phone).
- 2026-09-18 17:57: registry changes from Kalp: Outline removed; FlashCards marked under construction; Yash Birthday PCB (KiCad) and DIY EEG (under construction) added as showcase entries. Showcase worker notified via its plan file.
- 2026-09-18 17:51: wind-down at Kalp's request. Hardening run paused (resumable); Phase 2–4 build workflow restarted on Opus with resume-from-git instructions; Phase 1 finishing on Opus. All models Opus from here; economical mode next week.
- 2026-09-18: T5.a hoops TEST round 1. Wrote `docs/reports/hoops.md` (section 11 format): verdict PARTIALLY WORKING, 10 stories with evidence, 10 defects (2 blockers, 7 majors, 1 minor bundle), impeccable critique 19/40, exact verification commands. Numbers are right (`--check` exit 0; 95 = DB count; shot map 95 dots, rates sum to 100) but the Progress chart is an empty box (percentage-height bars inside an auto-height flex column), React #418 fires in every non-UTC browser and the real 2026-09-18 session shows as "Sep 19" in Asia/Tokyo, the 1970 test session is a normal row, the ingest function returned 200 for a 1970 and a +30 d timestamp (audit rows deleted; DB back to 5 sessions / 95 shots), the 390 px table is clipped, and `/api/health` is 404. Evidence in `docs/reports/evidence/hoops-r1-*` (screenshots, payload, Playwright audit JSON + script). Ops: the Management API `secrets` value for `INGEST_API_KEY` is not what the function accepts; `~/.config/portfolio-ops/hoops-ingest.key` is (incidents.md).
- 2026-09-18: T4.1 done. Case-study template (`components/showcase/`, typed content in `content/projects/<slug>.ts`, inline-SVG flow diagrams that go vertical on phones) with real prose written from each repo's code: UnPark (MPU6050 at 10 Hz, 4.8/3.8 m/s² thresholds, 3 s stillness, Firestore + Socket.IO + hourly Cloud Function), RC car (evdev + SDL2 controller threads, HSV green tracking, 64,273-image tuning set), Outline (it is a PencilKit circle-variability analyzer, not an idea outliner; registry corrected), FlashCards (from file names only; private repo with a committed Firebase plist, so no repo link). RC-car photos are frames from the two build videos on Kalp's Mac (`scripts/video-poster.py` + `scripts/media-to-webp.mjs`; videos never committed). classmyschedule is byte-identical to `jshklz/classmyschedule`, so it stays "coming" (H9). RC-car repo: PR #1 adds a README and documents the `git filter-repo` rewrite (H10); the 943 MB is history-only, HEAD is clean. Tests: registry↔content lock-step, WebP ≤ 300 KB, page render (50 pass). Verified in Chrome at 1440 and 390 (same-origin iframe; the window would not shrink), both themes, no overflow, no console errors (`docs/images/showcase/`). Lighthouse performance on `/projects/unpark`: 0.99 on the local production build (FCP 0.8 s, LCP 2.1 s, TBT 30 ms, CLS 0); production value in the T4.1 row once the Git-triggered deploy lands (two older deploys sat "Initializing" for 17 min and a manual `vercel --prod` was not permitted for this agent). Plan: docs/superpowers/plans/2026-09-18-showcase.md.
- 2026-09-18 18:30: T4.1 resumed after Kalp's 17:57 registry change. Wrote the Yash Birthday PCB page from the KiCad files (12 × 37 mm USB-C board, player-and-ball cut through the board on Edge.Cuts, three 0603 LEDs with 220 Ω, 5.1 kΩ CC pull-downs, DRC clean 2025-07-14) with `kicad-cli pcb render` front/back renders, the schematic export and Kalp's layout screenshot (all WebP ≤ 300 KB) → status `live`. Wrote the DIY EEG page from the schematic, `EEG_Simulation.cir` and the plan PDF (AD620 Rg 1 kΩ ≈ ×50, 0.48 Hz HPF, 48 Hz LPF, TL084 ×11, 1.65 V bias into an ADS1115 on a Pi 4; board file empty) → published as "under construction" while the registry stays `coming` (new rule in `app/projects/[slug]/page.tsx`: a non-draft content file renders even when the status is `coming`; drafts still get the placeholder). FlashCards content marked `draft: true` so its page is the placeholder Kalp asked for. Verified locally (production build) at 1440 and 390 (iframe harness), both themes, no overflow, no console errors (`docs/images/showcase/pcb-*.jpg`). Lighthouse `/projects/yash-birthday-pcb` local: 0.99 / 0.95 warm (0.87 on the first, cold-image-cache run; LCP is the hero). Production deploys were stuck: Vercel incident "Deployment stuck in initializing state" (status page, 21:36 UTC), seven team deploys QUEUED up to 84 min; a prebuilt deploy (`vercel build` + `vercel deploy --prebuilt --prod`) was queued too. See incidents.md.
- 2026-09-18: T5.a microtubules SPEC agent. Wrote `docs/reports/microtubules-spec.md` (what the page promises, quoted from its READMEs; 10 user stories; a measurable bar per story; asset table; today's baseline). Found the original 36 ImageJ crops under `~/Desktop/Out and About/Sidequest/Microtubule Quantification/` (T1.4 had them as lost) and committed them plus 4 whole-well images and 25 generated edge cases with Python ground truth to the project repo; new `web/tests/corpus.test.ts` (57 tests, all diffs 0.0000) and `web/scripts/browser-corpus.js`. Accuracy already meets the 1-point bar on every fixture in Chrome, including JPEG, EXIF-rotated and 24 MP inputs; the consumer-grade gaps are UX (wrong-file handling with a stale result, main-thread freeze and 475 MB on 24 MP, result hidden below the fold on a phone, no explanation of the number or warnings on degenerate inputs, no download/copy). Four incidents.md entries; verification.md rows added.
- 2026-09-18: T5.a hoops SPEC agent. Wrote `docs/reports/hoops-spec.md`: what the dashboard promises (quoted from its README/handoff doc), 10 user stories, a measurable consumer-grade bar per story, and the test assets. Built the assets in `~/projects/basketball/tests/`: a snapshot of all 5 sessions / 95 shots in the `hoops` schema, a pure-Python ground-truth generator with `--check` against `/api/dashboard` (live page matches on every count, FG%, eFG%, swish rate and streak per UTC day), the handoff doc's synthetic 3-shot session, and a re-snapshot script (commit `b34f00e`, pushed). Found and logged in incidents.md, not fixed: React error #418 on every load because date labels are formatted without a timezone (the 23 epoch-zero test shots from 1970 show as "Jan 1" on the chart and "Dec 31" in the table), Consistency computed over 5 raw sessions (67.6) while the table shows 4 merged days (85.9), eFG% proxy hits 150 % on a 1-shot session, and no sentence anywhere saying the iPhone app is not available yet. Baseline screenshot `docs/reports/evidence/hoops-spec-desktop-2026-09-18.jpg`.
- 2026-09-18: T5.a plato SPEC. Built the labelled corpus (42 real Western outlines copied to `~/projects/plato-corpus/pdfs`, sha256 manifest in the repo, PDFs never committed) and hand-labelled 10 of them (term, every lecture/lab/tutorial slot, every assessment with weight + date or an explicit TBA/registrar status). `tests/corpus/score.py` measures recall/precision per event type; baseline at `de32e96`: assessments 92 % R / 82 % P, but dated assessments correct only 14 % (4/29), term dates 0/10, sections 27 % R, clean titles 77 %. Live flow confirmed the .ics silently dates every undated assessment on the term end (2026-04-30). Spec `docs/reports/plato-spec.md` sets the bar (dates ≥ 95 %, no fabricated dates, failures explained on the review page). Corpus gate test is report-only until the fixer flips `PLATO_CORPUS_GATE=1`.
- 2026-09-18: Phase 5 turned into a closed test→fix loop per Kalp; workflow launched for plato/hoops/microtubules; plantit/pushups/emotes follow their builds.
- 2026-09-18: hub polish batch verified on production (tablet strip, phone first-screen, kind-derived verbs, OG card, stricter health). Lighthouse clean reading deferred until the Phase 1 workflow stops loading the machine.
- 2026-09-18: Hub polish batch (docs/superpowers/plans/2026-09-18-hub-polish.md) shipped in 3 commits (`6b8081e`, `e905bb0`, docs commit). Every box ticked except the H6 hero copy, which stays as written until Kalp answers (the contact row is built and hidden). Lighthouse had slipped to 0.84-0.87 after T0.5 (fonts 131 KB + posthog-js competing for LCP); fixed by self-hosting the 400-weight subsets (77 KB) and booting PostHog after `load`; re-measured after deploy: Lighthouse on e905bb0: six runs 0.87 / 0.86 / 0.96 / 0.97 / 0.90 / 1.00 (median 0.93; runs with uncontended FCP ~0.2 s scored 0.96–1.00; the sub-0.90 runs coincided with a second headless Chrome / load 8). Earlier reads of 0.84-0.87 came while orphaned T1.4 vitest workers pegged the CPU (incidents.md). Vercel project Node pinned to 22.x to match CI and the README. Deviations noted in the commit: the count line says "coming" not "repos" (same word the rows use), and `.impeccable/` stays where the design tool reads it, with `docs/design/README.md` as the pointer.
- 2026-09-18: T1.4 done. `KalpKan/Microtubule-Quantification` gained `web/`: Vite + TypeScript page running the exact executed steps of `process_image` with the official OpenCV.js served from the repo (offline after load) at https://microtubules.kalpkan.com (Vercel project `microtubules`, root `web`, auto-deploys from `main`; CNAME `bb0edf923bf938a4.vercel-dns-017.com`). The raw cells were never committed and the Desktop folder is gone, so the three samples were recovered pixel-exact from the nearest-neighbour "Original Image" panel of the `Results/*_analysis.png` figures (verified against the overlays); the Python venv run reproduces the published CSV exactly and the browser matches it with diff 0.0000 (tests: `web/tests/pipeline.test.ts`, `tests/test_run_analysis.py`). Two gotchas logged in incidents.md: `/ingest/:path*` 404s PostHog's trailing-slash endpoints in a static `vercel.json` (`:path(.*)` fixes it) and the OpenCV.js module is a self-resolving thenable (never `await` it). Plan: docs/superpowers/plans/2026-09-18-microtubules.md.
- 2026-09-18: T0.5 done. PostHog "Default project" renamed "Kalp portfolio" (id 616829, US), replay + heatmaps + input masking on by API; billing stays free with no card (custom `$0` limits need a card and the billing API refuses personal keys, so the free-tier hard cap is the guardrail; screenshot docs/images/posthog-billing-limits.png). Hub: posthog-js via `/ingest` rewrites, `persistence: memory`, autocapture, `project_card_clicked {slug,type}` sent instantly over sendBeacon (the batched version was lost on navigation; incident logged); `NEXT_PUBLIC_POSTHOG_KEY`/`_HOST` on Vercel prod+preview, deployed. Dashboard 2112106 with "Visitors by site", "Visitors by country", "Top demos by usage". `npm test` currently fails on `lib/projects.test.ts` (T1.3 made Plato live; test hard-codes 1 live site), not a T0.5 file. Plan: docs/superpowers/plans/2026-09-18-posthog.md.
- 2026-09-18: T1.1 done. Supabase Project B restored from paused (dashboard Resume; the sandbox refused the API POST), renamed `platform` via the API; basketball's 4 migrations rewritten to a `hoops` schema (+ `health_select_one`), applied through the Management API SQL endpoint, old `public` rows copied over (92 shots kept); `hoops` exposed in PostgREST; Edge Functions `hoops-ingest-shot` and `health` deployed; dashboard client scoped to `hoops`, types regenerated, "Demo data" banner only when env is missing, PostHog via `/ingest` with `session_viewed`/`shot_ingested`; Vercel env set (6 names), Root Directory fixed to `apps/web`, deployed; 3 sample shots ingested (95 total, `source: live` on https://hoops.kalpkan.com); UptimeRobot keep-alive monitor 804030499 on the health function, added to the status page. Human checkpoints H7 (JWT secret rotation) and H8 (drop old public tables) added. Plan: docs/superpowers/plans/2026-09-18-platform.md.
- 2026-09-18: T1.3 done. Plato runs as one Vercel Python 3.12 function (`src.app:app`, 72.8 MB bundle) at https://plato.kalpkan.com with Neon database `plato` (role `plato_owner`, pooled URL), stateless flow (.ics streamed in the same request, cookie 380 B), `/api/health` with `select 1`, PostHog via `/ingest` proxy + `pdf_uploaded`/`pdf_parsed`/`ics_downloaded`. Warm p50 0.17 s (`/`), 0.31 s (health); first cold 2.2 s; a 12-page parse takes ~16 s on Vercel. Extractor date quality is weak on some outlines (logged in incidents.md for T5.x). No UptimeRobot monitor yet. Plan: docs/superpowers/plans/2026-09-18-plato.md.
- 2026-09-18: H5 executed ("keep 35qv"): `promptflip` Vercel project deleted, local checkout re-linked, `promptflip.kalpkan.com` live with TLS, `NEXT_PUBLIC_APP_URL` + Supabase Auth URLs re-pointed, monitor 804030255 moved. Google sign-in from the new host reaches the Google account chooser with no error (redirect URI is Supabase's own callback), so no Google Cloud console change (H2) is needed; Kalp should do one real login on `promptflip.kalpkan.com` to confirm the round trip.
- 2026-09-18: H1 landed (`kalpkan.com`). T0.4 executed: A + www records for the hub and CNAME for hoops created DNS-only via the Cloudflare API; www → apex 308 set via Vercel API; TLS issued for all three within 2 min. The hoops project needed a `vercel redeploy` of its last Ready build first because its newest production build had errored since April (no deletion). Evidence in `docs/DNS_PENDING.md` §5.
- 2026-09-18: T0.3 done and verified; status page https://stats.uptimerobot.com/a6n3Wx3PBp.
- 2026-09-18: T0.1 hub deployed to Vercel Hobby (project `portfolio`); health, status proxy, 12-entry registry, CI workflow, README for Kalp. Lighthouse perf 0.92.
- 2026-09-18: repo created, plan copied to docs/hosting-plan.md, Phase 0 dispatched.
- 2026-09-18: T1.3 fixer. Reviewer rejection handled in `KalpKan/Plato` `de32e96`: the `/ingest` PostHog proxy forwarded the signed session cookie (blacklist -> whitelist, test added), static SDK now cacheable through the proxy, `custom_lead_time_mapping` survives the `/review` POST, uploads get a uuid `/tmp` name and are deleted after the request, one Neon upsert per edit, `/download` without a chosen section redirects to `/review`; 29 tests pass; live health `db: ok`, browser `$pageview` arriving. The "390 px overflow" was headless Chrome laying out at 500 px (incident logged; CSS guard added anyway). Plato parser quality is now tracked as T5.1.
