# Verification

The exact commands and URLs that prove each part of the platform is healthy. Use these to go from "something is wrong" to "this specific thing is wrong" in minutes, and to prove a task is done before reporting it. Paste actual output, not a description of it.

Rows marked **PENDING** target something the plan calls for but that is not live yet; the command is written now so it is ready, and the row is updated (with the real URL and a "confirmed on <date>" note) by the task that makes it live. The source list is `docs/hosting-plan.md` section 10.

Placeholders: `<domain>` is `kalpkan.com` (bought at human checkpoint H1 on 2026-09-18); `<prod-url>` is the `*.vercel.app` URL of an app that has not been moved onto the domain yet.

## Quick triage (the "first 5 checks" from SKILL.md, as commands)

| # | Check | Command | Healthy output | Status |
|---|---|---|---|---|
| 1 | UptimeRobot status page | open `https://stats.uptimerobot.com/a6n3Wx3PBp`; via API (after `set -a; source ~/.config/portfolio-ops/secrets.env; set +a`): `curl -s -X POST https://api.uptimerobot.com/v2/getMonitors -d "api_key=$UPTIMEROBOT_API_KEY&format=json&monitors=804030255-804030256-804030271-804030499-804031032-804031239-804031518-804031519-804031520" \| jq '.monitors[] \| {friendly_name, status}'` | the nine platform monitors `status: 2` (up); the six paused `shop.travisscott.com` monitors (`status: 0`) are Kalp's old ones and are not a failure | confirmed 2026-09-18 (T0.3); nine monitors since 2026-09-19 (Phase 2–4 audit added the three static demos). A custom `status.<domain>` is a paid feature and is not planned |
| 2 | Failing app's health route | `curl -sf https://<prod-url>/api/health` | promptflip: JSON containing `"db":"ok"`; hub: `{"ok":true,"service":"hub","time":"..."}` | both confirmed 2026-09-18 (re-checked by the Phase 1 audit: promptflip `db: ok`, hub `ok: true`) |
| 3 | Vercel deployment logs | `npx vercel ls <project> --scope kks-projects-2edcb11a` then `npx vercel inspect <deployment-url> --logs` | newest deployment state `Ready`, no build errors | works now |
| 4 | Supabase project status | Supabase dashboard project list, or Supabase MCP `list_projects` | both projects `ACTIVE_HEALTHY`, none `INACTIVE` (paused) | use the Management API (`verification.md` Supabase section, first row); the Supabase MCP OAuth never completed (STATUS.md H0), so do not wait on it |
| 5 | Cloudflare DNS record | `dig +short <sub>.kalpkan.com` and `dig +short kalpkan.com` | subdomain: the project-specific `<hash>.vercel-dns-017.com` name that `npx vercel domains verify <host>` printed when the record was created (recorded in `docs/DNS_PENDING.md` §5); apex: `76.76.21.21` | confirmed 2026-09-18: `kalpkan.com` → `76.76.21.21`; `www.kalpkan.com` → `a9e60d5e9d41cb23.vercel-dns-017.com.`; `hoops.kalpkan.com` → `71da701c9c3d8bdf.vercel-dns-017.com.` |

## Per-component checks

### Hub (`KalpKan/portfolio`, Vercel project `portfolio`, https://kalpkan.com)

| Check | Command | Expect | Status |
|---|---|---|---|
| Health route | `curl -sf https://kalpkan.com/api/health` | `{"ok":true,"service":"hub","time":"<ISO>"}` with header `cache-control: no-store` (`curl -sI ... \| grep -i cache-control`) | ✅ 2026-09-18 |
| Page serves | `curl -sI https://kalpkan.com \| head -1` | `HTTP/2 200` | ✅ 2026-09-18 |
| Status proxy | `curl -s https://kalpkan.com/api/status/promptflip` | `{"slug":"promptflip","ok":true,...}`; `ok:false` means promptflip's health route did not answer 2xx JSON `ok: true` (see `lib/health.ts`) | ✅ 2026-09-18 |
| Share card | `curl -sI https://kalpkan.com/opengraph-image \| head -1` and `curl -s https://kalpkan.com \| grep -o 'og:image" content="[^"]*'` | `HTTP/2 200`; an absolute `https://kalpkan.com/opengraph-image?...` URL | ✅ 2026-09-18 (hub polish) |
| Registry valid + KalpOS logic | `cd ~/projects/portfolio && npx vitest run --pool=forks --maxWorkers=1 --testTimeout=10000` | all pass: `lib/projects`, `lib/health`, `lib/posthog`, `lib/track`, and since T6 `lib/tiles` (registry → tiles, filter counts 12/7/5/5/2), `lib/signal`, `lib/visitor`, `lib/windows`, `lib/clock`, `lib/contact`, the `components/kalpos/*` component tests and `app/projects/[slug]` publishing rule; since the 2026-09-20 fixes also `lib/boot` (readiness, ≥ 900 ms / ≤ 3 s, deep-link policy, the pre-paint script), `lib/chime` (fake AudioContext) and the real-pointer window tests (158 tests, 24 files; `.worktrees/**` and every `node_modules` are excluded); since T6.2 also `components/kalpos/KalpOSMenu` (menu button, keyboard, click-outside, `menu_action`), `RestartSheet`, the lock / restart stages in `KalpOS.test.tsx`, `closeAll`, `rearmChime`, and the shell's `reboot`/`restart`/`lock`/`logout` (236 tests, 30 files) | ✅ 2026-09-20 (236 passed / 30 files, T6.2) |
| Build clean | `cd ~/projects/pushups && npm run build` | `✓ built`; `dist/` has `wasm/`, `models/pose_landmarker_full.task` (and **no** `models/form*/`), `demo/`, `health.json`; entry JS ≈ 4 KB, `session` chunk ≈ 147 KB (lazy; was 1 MB with TF.js until FIX r3) | FIX r3 2026-09-19 |
| CI green | `gh run list --repo KalpKan/portfolio --limit 1` | latest run `completed success` | ✅ 2026-09-18 |
| Node versions agree | `grep node-version .github/workflows/ci.yml`; `grep -A1 engines package.json`; Vercel → project `portfolio` → Settings → General → Node.js Version | all `22` (pinned 2026-09-18 via `PATCH /v9/projects/<id> {"nodeVersion":"22.x"}`) | ✅ 2026-09-18 |
| Lighthouse performance | `for i in 1 2 3; do npx -y lighthouse@12 https://microtubules.kalpkan.com --only-categories=performance --quiet --chrome-flags="--headless=new" --output=json --output-path=/tmp/lh$i.json; jq .categories.performance.score /tmp/lh$i.json; done` | `>= 0.9` **on each of three consecutive runs** (one run is not evidence) | ✅ 0.98 on 2026-09-18 (one run); ❌ TEST r1: 0.73 / 0.97 / 0.61 (D7, OpenCV parsed on the main thread); ✅ FIX r1 2026-09-19: **0.97 / 0.97 / 0.99** (LCP 2.1 / 1.9 / 1.9 s, TBT 10 / 0 / 0 ms; OpenCV now loads inside the worker) |
| Live tiles (KalpOS) | open https://kalpkan.com, unlock (type anything, Enter), open the Projects folder; in the console `[...document.querySelectorAll('.kos-tile-art--live')].length` and `document.querySelector('.kos-sidebar-foot').textContent` | a `live` app with a `healthUrl` turns cyan and draws its trace within ~8 s; the sidebar foot reads `pinged HH:MM:SS · 7/7 ok`; case studies grey with ▲; `coming` dashed; a live tile is `<a target=_blank>` to the app | ✅ 2026-09-20 (T6 preview: 7 live tiles, 7 traces, `7/7 ok`) |
| Layout at four widths (KalpOS) | in Chrome at 1440 and 1024 (the desk, card 2c) and at 768 and 390 (the phone sheet, card 1e; use same-origin `<iframe>`s when the window will not shrink) | 1440/1024: menubar, folders, widgets, dock; Projects window 760 px centred; 768/390: top bar, name + measured count line, 2×3 folders, note, Projects bottom sheet with one row per tile; no horizontal scroll; the lock screen unlocks with Enter (a coarse-pointer tap of the pill on a phone) | ✅ 2026-09-20 (`docs/images/kalpos/`) |
| Boot → lock → desk (every plain visit) | `curl -s https://kalpkan.com \| grep -o 'data-stage="[a-z]*"\|kos-boot-mark">[A-Z]*'`; then in Chrome on a plain `/` load, and again after a reload | `data-stage="boot"` and `kos-boot-mark">KK` in the HTML (the boot is server-rendered); the KK mark resolves from a blur over the hairline, the hairline fills with the health round, after ≥ 900 ms (≤ 3 s) the mark blurs out and the lock fades in (`.kos[data-stage="lock"]`); the password field has focus; type anything, Enter → pulse, chime, blur-out, desk breathes in. **A reload shows the boot and the lock again** (no `localStorage` skip; `localStorage.getItem("kalpos:visited")` is `null`). Headless timeline: `node docs/reports/evidence/kalpos-boot-timeline.mjs https://kalpkan.com /tmp` prints the stage / hairline / mark samples | ✅ 2026-09-20 (preview `portfolio-dp5wkf5lf` in Chrome: boot → leaving → lock on a fresh load, reload the same; local production build: mark 0→1 in 600 ms, hairline 0.125→1 with the seven checks, lock at 1.7 s, 0 console errors) |
| Lock never skipped; deep link and `?desk` skip both | `curl -s 'https://kalpkan.com/?desk' \| grep -o 'data-kos-boot'` (the pre-paint script is in the HTML); in Chrome open `/?desk` and `/projects/rc-car` | `/?desk`: no boot, no lock, desk with a 400 ms crossfade (`html[data-kos-boot="desk"]`); `/projects/rc-car`: the desk with the `Automatic RC Car` dialog from the first paint; plain `/`: boot then lock, every time | ✅ 2026-09-20 (preview: `/projects/rc-car` straight to the desk with the window; local: `/?desk` attr `desk`, boot `display: none`, lock opacity 0 before hydration) |
| Startup chime | Chrome, a plain load: `document.querySelector('.kos').dataset.chime` after the unlock; `new AudioContext().state` after any click | `chime` is `"unlock"` (or `"boot"` when Chrome already allowed audio for the tab); the context is `running`; no console error when audio is blocked (`playChime` resolves `false`); the speaker toggle in the menubar (`.kos-mute`, `aria-pressed`) mutes it and persists `kalpos:mute`; PostHog `chime_played {at}` | ✅ 2026-09-20 (preview: `data-chime="boot"`, `AudioContext.state` `running`, mute toggle rendered `aria-pressed="false"`; unit: fake AudioContext, 4 voices → gain → master 0.3 → lowpass → destination, attack 0.12 s, release 1.8 s, peaks sum ≤ 1) |
| KalpOS menu → Restart replays boot→lock; terminal `reboot` | Chrome on https://kalpkan.com, unlocked: click the **KalpOS** word in the menubar (`button[aria-haspopup="menu"]`), then **Restart…** → **Restart**; watch `document.querySelector('.kos').dataset.stage` (or install a `MutationObserver` on `.kos` attributes and log `data-stage` / `data-leaving` / `data-chime`). Then unlock, open the Terminal from the dock, type `reboot` ⏎. Also **Lock Screen** (or ⌃⌘Q; ⌘L is never intercepted since T6.3): `dataset.stage` and `document.activeElement.getAttribute('aria-label')` after 400 ms; ⌃⌘R opens the sheet, Esc closes it. Phone (390, or a same-origin 390 px iframe): the ■ KalpOS in the top bar opens the same menu, rows 44 px. Unit: `npx vitest run components/kalpos/KalpOSMenu.test.tsx components/kalpos/KalpOS.test.tsx lib/shell.test.ts` | menu: `role="menu"` with About KalpOS · Clean Up ⌥⌘1 · Lock Screen ⌃⌘Q · Restart… ⌃⌘R · Mute/Unmute chime (the last mirrors `.kos-mute` `aria-pressed` and `kalpos:mute`), first row focused, Esc returns focus to the button; Restart: a `role="alertdialog"` "Restart KalpOS? The desk will reboot." with Restart focused; on Restart the stage goes `desk → restarting → (~300 ms) boot → boot/leaving (≥ 900 ms later) → lock` with `data-chime` cleared and set again (the chime sounds once more) and 0 `[role=dialog]`; the URL is `/` again if a case study was open; `data-kos-boot` is gone from `<html>`; then Enter unlocks to the desk with `data-boot="animate"`. Terminal `reboot` prints `Restarting…` and does the same after ~400 ms with no sheet (`lock`/`logout` show the lock; all four in `help` and Tab-complete). Lock Screen: `desk → locking → (320 ms) lock`, the desk blurred out with its windows and the password field focused, no second `chime_played`. PostHog `menu_action {item}` | ✅ 2026-09-20 (preview `portfolio-i6c5p2bmi`: restart timeline `restarting → boot +312 ms → chime re-armed +12 ms → leaving +895 ms → lock +602 ms`, lock `locking → lock` in 321 ms with the field focused, ⌃⌘R + Esc, ⌘L, terminal `logout` → lock, `reboot` on the local production build; phone iframe 390: menu 4 × 44 px rows, Lock Screen → lock; screenshots `docs/images/kalpos/restart/`) |
| KalpOS window close / minimise beats (T6.3) | `node docs/reports/evidence/kalpos-close-check.mjs https://kalpkan.com` (borrows promptflip's Playwright; opens About me from its icon, held click on the red light, then the yellow one, recording `data-anim`, `getAnimations()` and the computed transform). By hand in Chrome: open a window, held click on the red light (`left_click_drag` to the same point), watch a `MutationObserver` on `data-anim` | close: `data-anim="close"` with exactly one running animation `kos-win-close`, the frame scales in place (`matrix(0.98…)`) while fading, the dialog is gone ≤ 250 ms after the release (`PASS close: dialog removed 159 ms`); minimise: exactly one `kos-win-min` toward the window's own dock tile (`--from-x/y` point at `.kos-dock-item[data-window]`), the frame leaves at ~320 ms and the tile shows the running dot; `no page errors`. The old build fails both (`exactly one animation ran: []`, dialog removed 320 ms after the release: the frame jumped to the icon rect and sat there) | ✅ 2026-09-20 local production build `PASS ×9`; kalpkan.com before the fix `FAIL ×5` (the record of the defect); preview `portfolio-3k1ogwkrh` by hand in Chrome (one `kos-win-close@running`, one `kos-win-min@running`, 0 dialogs left, Notes dot); production after the merge: see the T6.3 session log |
| KalpOS desk icons move, persist, Clean Up (T6.3) | Chrome on https://kalpkan.com/?desk: drag a folder (`left_click_drag`) to the far right, read `localStorage.getItem('kalpos:icons')` and the button's `style.left/top`; reload; focus the icon (`el.focus()`) and press arrow keys; **KalpOS** menu → **Clean Up** (or ⌥⌘1). Unit: `npx vitest run lib/icons.test.ts components/kalpos/Desk.test.tsx` | the icon follows the hand 1:1 and, on release, `style.left/top` are `38 + c·22` / `64 + r·22` px with the cell saved under its id (`{"hobbies":{"c":52,"r":12}}`); a reload places it there again; ← → ↑ ↓ change one cell (through a neighbour to its far side, never past the menubar line, the dock band or the sides); a drop on another icon lands on the nearest free cell; Clean Up puts every icon back on the card 2c grid (Hobbies `148, 64`) and the menu row reads `Clean Up ⌥⌘1`; no window opens after a drag; Lock / Restart keep the positions | ✅ 2026-09-20 preview `portfolio-3k1ogwkrh` in Chrome with real drags and keys (drag → `1182, 328` = cell `52, 12`, reload same, ← ← ↑ → `1138, 306`, Clean Up → `148, 64`); local production build with Playwright (100 px steps: the first version lost the drag, fixed by capturing on pointerdown) |
| KalpOS Trash swat (T6.3) | Chrome on https://kalpkan.com/?desk: install `new MutationObserver(...)` on `.kos-desk` for `data-swat` / `data-state` / `data-hand` / `data-lid`, then `left_click_drag` a folder onto the desk Trash (or the dock's Trash tile); also with `prefers-reduced-motion` (Playwright `reducedMotion: 'reduce'`). Unit: `npx vitest run lib/swat.test.ts components/kalpos/Desk.test.tsx` | held over the trash: the folder's `data-state="crumple"` (computed `filter: url("#kos-crumple")`, transform ≈ `matrix(0.845, 0.089, …)`) and the trash's `data-lid="up"` (only the one under the pointer); dropped: `data-swat` runs `dropped (200 ms) → swatted (780 ms: hand out + toast + fly) → home (200 ms) → idle`, `.kos-hand[data-hand=out]` contains an `<svg>` (no emoji), `.kos-toast` reads "Nice try — everything on this desk shipped." and is gone by 1.8 s, the folder's `style.left/top` never change, nothing new in `kalpos:icons`, PostHog `trash_swat {icon}` once; the Trash icon itself cannot be swatted; reduced motion: `data-state="return"`, hand still shows, ~660 ms total | ✅ 2026-09-20 local production build (Playwright timeline `7 ms dropped/sink → 215 swatted/hidden/hand out/toast → 526 fly → 1016 home/hand in → 1234 idle → 1812 toast gone`; frames `docs/images/kalpos/desk/swat-*.jpg`); preview `portfolio-3k1ogwkrh` in Chrome with a real drag (`over → dropped → swatted → home → idle`, folder back at `148, 64`; the tab was `visibility: hidden` behind other agents' tabs, so its timers were clamped to 1 s and no frames painted: read timings from Playwright, not from a hidden tab) |
| KalpOS lock shortcut is ⌃⌘Q; ⌘L is the browser's (T6.3) | Chrome on https://kalpkan.com/?desk: press ⌘L (the `key` tool sends it to the page only), then ⌃⌘Q; read `document.querySelector('.kos').dataset.stage` and `document.activeElement.getAttribute('aria-label')` | after ⌘L: `desk` (the event is not `defaultPrevented`, so a real browser focuses its address bar); after ⌃⌘Q: `locking` then `lock` within 320 ms with `Password` focused; the menu row reads `Lock Screen ⌃⌘Q`; icon positions survive the lock | ✅ 2026-09-20 preview `portfolio-3k1ogwkrh` in Chrome (⌘L → `desk`; ⌃⌘Q → `lock`, `Password`, Hobbies still at `148px`) |
| Traffic lights with a real mouse (held click) | Chrome: open Projects with one click, then a **held** click on each light (mouse down, ~100 ms, up; the computer tool's `left_click_drag` to the same point does this) | red closes the window; yellow minimises it into the Finder dock tile which keeps its running dot, a click on the tile restores it; green zooms (`width: calc(100vw - 24px)`) and a second click restores (760 px); dragging the title bar / sidebar top still moves the window; a double-click on a desk icon opens its window once. Unit: `npx vitest run components/kalpos/Window.test.tsx components/kalpos/KalpOS.test.tsx` (`realClick()` applies pointer capture the way Chrome does). Both engines headless: `node docs/reports/evidence/kalpos-lights-engines.mjs https://kalpkan.com live` → `PASS` twice (chromium, webkit) | ✅ 2026-09-20 (preview `portfolio-dp5wkf5lf`: red → 0 dialogs, yellow → dock dot, tile → restored, green → zoom/restore, drag 340→539 px, Esc closes; before the fix, on the live site, the held click's `pointerup`/`click` were retargeted to the handle `DIV` and the window stayed) |
| Deep link opens the desk with the window | `curl -s https://kalpkan.com/projects/rc-car \| grep -o 'role="dialog"[^>]*aria-label="[^"]*"' \| head -1` and `\| grep -c 'id="problem-head"'` | one dialog labelled `Automatic RC Car`; `1` (the case study is server-rendered inside the window; `og:image` stays absolute) | ✅ 2026-09-20 (T6 preview) |
| Windows are accessible | Chrome: Tab reaches the skip link, the five desk icons, then the dock; Enter on a folder opens its window and focuses it; Esc closes the top window; inside a window Tab wraps | as described; `role="dialog"` + `aria-label` on every window | ✅ 2026-09-20 (T6 preview) |
| Reduced motion | Chrome with `prefers-reduced-motion: reduce` (DevTools rendering emulation, or stub `matchMedia` in the console before unlocking) | boot is a 400 ms crossfade (`data-boot="crossfade"`), windows appear in place (`data-anim="place"`), the dock does not magnify, the trace is pre-drawn (`app/kalpos.css` reduced-motion block, 8 rules) | ✅ 2026-09-20 (local, `next dev`) |
| Lighthouse performance (hub) | `for i in 1 2 3; do npx -y lighthouse@12 https://kalpkan.com --only-categories=performance --quiet --chrome-flags="--headless=new" --output=json --output-path=/tmp/lh$i.json; jq .categories.performance.score /tmp/lh$i.json; done`; the same on `/projects/rc-car` (the desk) | `>= 0.9` on each of three runs; CLS `0` (the clock is filled before the first paint by `CLOCK_SCRIPT`) | ✅ 2026-09-20 local production build: `/` 0.97 ×8, `/projects/rc-car` 0.92 / 0.93 / 0.93; production `/` 1.0 / 0.99 / 0.99 / 0.9 / 0.9 / 0.89 / 0.88 / 0.85 and `/projects/rc-car` 0.97 / 0.96 (the sub-0.9 runs had an observed first paint of 1.3–2.3 s after `load`; a control run on `microtubules.kalpkan.com` showed the same at the same time, so re-run on a quiet machine before calling it a regression) |
| Case-study pages serve | `for s in unpark rc-car flashcards porsche-pcb-keychain eeg; do curl -s -o /dev/null -w "$s %{http_code}\n" https://kalpkan.com/projects/$s; done` | `200` for each; `curl -s -o /dev/null -w '%{http_code}' https://kalpkan.com/projects/classmyschedule` → `404` (removed 2026-09-18). Template fingerprint: `curl -s https://kalpkan.com/projects/<slug> \| grep -o 'id="[a-z-]*-head"' \| wc -l` → 5–7 for a case study, 0 for a placeholder (`grep -c` counts lines and the HTML is one line, so it always printed `1`; corrected 2026-09-19) | ✅ 2026-09-18 18:50 (T4.1, production); Phase 2–4 audit 2026-09-19: unpark 7, rc-car 6, flashcards 0, porsche-pcb-keychain 5, eeg 5, classmyschedule `404` |
| Registry and content agree | `cd ~/projects/portfolio && npx vitest run --pool=forks --maxWorkers=1 --testTimeout=10000 content/` | `content/case-study.test.ts` (every showcase slug has a content file and vice versa, live pages are not drafts) and `content/media.test.ts` (every file under `public/images/projects` is `.webp` ≤ 300 KB) pass | ✅ 2026-09-18 (T4.1) |
| Case-study page layout | Chrome at 1440 and 390 (390 via a same-origin `<iframe>` if the window will not shrink), both themes; `document.documentElement.scrollWidth <= innerWidth`; console errors 0 | no horizontal scroll; diagram labels inside their boxes; placeholders read "coming: Kalp will add"; hero not cropped to floor | ✅ 2026-09-18 (T4.1, rc-car + unpark; 18:30 porsche-pcb-keychain + eeg on the local production build, `docs/images/showcase/pcb-*.jpg`); since T6 (2026-09-20) the case study renders inside a desk window at 1440 and a full-height sheet at 390 (`docs/images/kalpos/case-study-window-1440.jpg`, `phone-case-sheet-390.jpg`), one theme only |
| Under-construction page | `curl -s https://kalpkan.com/projects/eeg \| grep -o 'under construction' \| head -1` and `curl -s https://kalpkan.com/projects/flashcards \| grep -c 'coming soon'` | `under construction` (full page, registry `coming`, content not a draft); `1` (draft content → placeholder page) | ✅ 2026-09-18 18:50 production |
| Hardware content facts | `cd ~/projects/portfolio && npx vitest run --pool=forks --maxWorkers=1 --testTimeout=10000 content/hardware.test.ts` | 4 pass: PCB page carries 5.1k / 220 / 12 × 37, EEG page carries AD620 / TL084 / ADS1115 / 0.48 Hz / 48 Hz, FlashCards stays a draft, both schematics carry `tone: "document"` (dark-theme invert) | ✅ 2026-09-18 |
| Diagram edge labels fit the gap | `cd ~/projects/portfolio && npx vitest run --pool=forks --maxWorkers=1 --testTimeout=10000 components/showcase/diagrams/` | 3 pass: every horizontal edge-label line ≤ 68 px at 6.6 px per glyph (`H_GAP - 4`), ` / ` splits into stacked `<tspan>`s. In Chrome at 1440 on `/projects/rc-car`: `[...svg.querySelectorAll('text')]` label boxes lie between the node `rect` boxes, none overlapping (fixer 2026-09-18: "events / frames" was 99 px wide in a 72 px gap and ran into the Raspberry Pi node). Production check: `curl -s https://kalpkan.com/projects/rc-car \| grep -o '>events</tspan>\|>events / frames<' \| sort \| uniq -c` | ✅ 2026-09-18 (local production build); ✅ production 2026-09-19 00:14 UTC (Phase 2–4 audit): `2 >events</tspan>`, no `events / frames` |
| Publishing rule pinned | `cd ~/projects/portfolio && npx vitest run --pool=forks --maxWorkers=1 --testTimeout=10000 "app/projects"` | 3 pass: eeg (registry `coming`, no draft) renders the full page with "under construction"; flashcards (`draft: true`) renders the placeholder; unpark (`live`) says "case study" | ✅ 2026-09-18 |
| Document images on the dark sheet | Chrome, `/projects/eeg`, `document.documentElement.dataset.theme='dark'`, then `getComputedStyle(document.querySelector('img[data-tone=document]')).filter` | `invert(0.93) hue-rotate(180deg)` (schematic paper goes near `#0e1014`, wires stay green); in light theme `none` | ✅ 2026-09-18 (local production build); **retired 2026-09-20 (T6):** KalpOS has no dark desk (black is only the lock screen), so the filter never applies; the `Media.module.css` rule is inert |
| Case-study Lighthouse | `npx lighthouse https://kalpkan.com/projects/unpark --only-categories=performance --quiet --chrome-flags="--headless" --output=json \| jq .categories.performance.score` | `>= 0.9`; run it twice, the first run pays the image optimizer's cold cache (0.87 → 0.99/0.95 on `porsche-pcb-keychain` locally) | local production build: unpark 0.99, rc-car 0.95, porsche-pcb-keychain 0.99/0.95 (2026-09-18); production 2026-09-18 18:50: porsche-pcb-keychain 0.91, unpark 0.84–0.88 (STATUS.md session log) |
| RC-car purge PR | `gh pr view 1 --repo KalpKan/Automatic-RC-Car --json state,headRefName` | `OPEN` on `purge-training-data` until Kalp merges; after the approved rewrite a fresh clone's `git count-objects -vH` shows `size-pack` under 1 MiB | ✅ opened 2026-09-18 (T4.1) |

### promptflip (`KalpKan/promptflip`, Vercel project `promptflip-35qv` (the only one since H5, 2026-09-18), Supabase Project A)

| Check | Command | Expect | Status |
|---|---|---|---|
| Health (DB-touching) | `curl -sf https://promptflip.kalpkan.com/api/health` | JSON with `"db":"ok"` | confirmed 2026-09-18: `{"ok":true,"db":"ok",...,"commit":"aff55a5...","region":"pdx1"}` (the `promptflip-35qv.vercel.app` alias returns the same) |
| Page serves | `curl -sI https://promptflip.kalpkan.com \| head -1` | `HTTP/2 200` | confirmed 2026-09-18 (Let's Encrypt cert `cert_QAufduOZKLgVC3Bvb9bon3qi`) |
| Env vars present (names only) | `cd ~/projects/promptflip && npx vercel env ls --scope kks-projects-2edcb11a` (linked to `promptflip-35qv` since 2026-09-18) | the six names in `settings-map.md` all listed for `production`; `NEXT_PUBLIC_APP_URL` pulled with `npx vercel env pull --environment production <scratch file>` reads `https://promptflip.kalpkan.com` | confirmed 2026-09-18 |
| Google sign-in works on the new domain | open `https://promptflip.kalpkan.com`, click Sign in with Google | Google consent screen appears with no `redirect_uri_mismatch` / `origin_mismatch` error; after login the redirect returns to `promptflip.kalpkan.com` | confirmed 2026-09-18: from `https://promptflip.kalpkan.com/login` the Google "Choose an account" screen opened (continue to `nhddxonizdxwbvwcxklu.supabase.co`, `redirect_to=https://promptflip.kalpkan.com/auth/callback`), no error; login not completed by the agent, so the round trip back to the host is still unconfirmed by a human |

### basketball / hoops (`KalpKan/Basketball-Stat-Tracker`, Vercel project `v0-basketball-analytics-dashboard`, Supabase Project B schema `hoops`)

| Check | Command | Expect | Status |
|---|---|---|---|
| Dashboard reads real rows | `curl -s https://hoops.kalpkan.com/api/dashboard \| jq -c '{source, totalShotsRecorded}'` | `"source":"live"` and a count > 0 (`95` on 2026-09-18); `"mock"` means the Supabase env vars are missing or Project B is paused | confirmed 2026-09-18 (T1.1); same on the public alias `https://v0-basketball-analytics-dashboard-seven.vercel.app` |
| Demo banner absent | `curl -s https://hoops.kalpkan.com/ \| grep -c 'Sample data'` | `0` (the banner only renders when `/api/dashboard` says `mock`; amber = settings missing, red = database unreachable, `dataError` set) | confirmed 2026-09-18; **FIX r1 2026-09-18 (`7eead57`)**: banner renamed 'Sample data' with two variants (D8) |
| Page serves | `curl -sI https://hoops.kalpkan.com \| head -1` | `HTTP/2 200`, `server: Vercel` | confirmed 2026-09-18 |
| PostHog proxy | `curl -sI https://hoops.kalpkan.com/ingest/static/array.js \| head -1`; in a browser, the Network tab shows `POST /ingest/e/` and `/ingest/s/` returning 200 | `HTTP/2 200` | confirmed 2026-09-18 |
| Custom events arrive | `curl -s "$POSTHOG_HOST/api/projects/616829/events/?event=session_viewed&limit=1" -H "Authorization: Bearer $POSTHOG_PERSONAL_API_KEY" \| jq -c '.results[] \| {event, host: .properties["$host"]}'`; same with `event=shot_ingested` | one row each with `host: hoops.kalpkan.com` | confirmed 2026-09-18 (`session_viewed` from a pill click, `shot_ingested` from the Edge Function) |
| Tests and typecheck | `cd ~/projects/basketball && npx pnpm test && npx pnpm --filter @basketball-stat-tracker/web typecheck` | `# pass 23`, `# fail 0` (metric math against both fixture corpora, chart px bars, notice/table/banner, ingest validation), tsc silent; also passes under `TZ=Asia/Tokyo` and `TZ=America/Toronto` | confirmed 2026-09-18; **FIX r1 2026-09-18 (`7eead57`)**: 23 tests, three timezones |
| Migrations never touch `public` | `bash ~/projects/basketball/supabase/migrations/test_hoops_schema.sh` | `PASS` | confirmed 2026-09-18 |
| Numbers match ground truth | `curl -s https://hoops.kalpkan.com/api/dashboard > /tmp/hoops-payload.json && python3 ~/projects/basketball/tests/compute-expected-metrics.py --check /tmp/hoops-payload.json` (re-snapshot first with `tests/snapshot-hoops-rows.sh` if shots were added since 2026-09-18 and point `ROWS` at the new file) | exit 0 and `all counts, FG%, eFG%, swish rate, streaks, consistency and hidden shots match the ground truth` (compared with the fixture's `dashboard` block: one row per device + UTC day, pre-2000 hidden, bounded eFG% v2); the printed lines show Consistency 64.0 / Avg Streak 9.8 / hidden 23 of 95 on 2026-09-18 | confirmed 2026-09-18 (SPEC agent): counts/FG/eFG/streaks match; Consistency is on the raw-session basis (67.6) while the table is per UTC day (85.9), see incidents.md; **FIX r1 2026-09-18 (`7eead57`)**: exit 0 locally on the built app with the production env (Consistency 64.0 on the same basis as the table); live host re-checked 2026-09-19 02:36Z after `7eead57` deployed: exit 0 |
| No console errors on load | open `https://hoops.kalpkan.com` in a real Chrome tab, read the console | 0 errors | **FAIL 2026-09-18**: `Minified React error #418` (hydration mismatch from timezone-dependent date labels; incidents.md); re-check after the Phase 5 fix; **FIX r1 2026-09-18 (`7eead57`)**: 0 errors in Chromium ×6 and WebKit ×2 (UTC/Toronto/Tokyo, 1440 + 390 px) against the local production build and again on the live host at 02:37Z (D2); **TEST r2 2026-09-19 02:45Z**: 0 errors in Chromium ×6, WebKit ×6 and real Chrome (Toronto) on the live host |
| Spec + fixtures for the audit | `ls ~/projects/basketball/tests/fixtures/` and `~/projects/portfolio/docs/reports/hoops-spec.md` | `hoops-rows-2026-09-18.json`, `hoops-expected-metrics.json`, `synthetic-tap-session.json`, `synthetic-30-sessions.json`, `synthetic-30-expected-metrics.json`, `README.md`; spec lists 10 user stories with measurable bars | created 2026-09-18; 30-session synthetic corpus added by the second SPEC run (`1f743b9`) |
| Synthetic corpus regenerates deterministically | `cd ~/projects/basketball && python3 tests/generate-synthetic-sessions.py && python3 tests/compute-expected-metrics.py --rows tests/fixtures/synthetic-30-sessions.json --out tests/fixtures/synthetic-30-expected-metrics.json && git status --short tests` | `30 sessions, 801 shots`; `git status` prints nothing (seeded, so the files do not change) | confirmed 2026-09-18 |
| Functional smoke: chart bars render | `mkdir -p /tmp/hoops-audit && cd /tmp/hoops-audit && ln -sfn ~/projects/promptflip/node_modules node_modules && cp ~/projects/portfolio/docs/reports/evidence/hoops-r2-playwright-audit.mjs . && S=$PWD URL=https://hoops.kalpkan.com node hoops-r2-playwright-audit.mjs > results.json` (the script must sit next to `node_modules`; `URL` can point at a local `next start`) | in every config `errors []`, every `chart.bars[].renderedH` > 0 and `modes.*[].renderedH` > 0, `ticks` `100…0`, `pillLabels[1..]` = `chart.chartLabels` reversed, `rows[*][0]` = the same dates, `hasNotice`/`repoLink` true, `hiddenNote` set, `tableWrap.overflowX` `auto`, no button `h` < 40, `faintTextNodes` 0, `scrollWidth` = `clientWidth` | **FAIL 2026-09-18 (TEST r1)**: every bar 0 px in all 6 configs (`docs/reports/hoops.md` D1); **re-run 2026-09-18 (TEST r1b)**: still 0 px in Chromium ×6, Playwright WebKit ×4 (`npx playwright@1.63.0 install webkit`) and real Chrome; script now `hoops-r1b-playwright-audit.mjs` (adds bar heights per mode, pill-vs-chart-vs-table labels, WebKit); **FIX r1 2026-09-18 (`7eead57`)**: bars 121–208 px in FG/eFG/Streak in all 8 configs (script `hoops-r2-playwright-audit.mjs`; percentage heights replaced by px from a fixed 208 px plot, D1); same result on the live host 2026-09-19 02:37Z, `ALL CONFIGS PASS`; **TEST r2 2026-09-19**: PASS on the live host in Chromium ×6 + WebKit ×6 with the wider `hoops-r2t-playwright-audit.mjs` (adds per-node contrast, clipped-text and per-pill checks) — bars 121–208 px, contrast min 7.06:1, 0 errors; the same run found the one open defect: phone chart labels truncated (`clipped` 2, next row) |
| Functional smoke: 30-session corpus fits | `cd ~/projects/basketball && npx pnpm test` (the `30-session synthetic corpus` test feeds `tests/fixtures/synthetic-30-sessions.json` through `buildDashboardPayload`); render + measure: `cd apps/web && npx pnpm build && cp ~/projects/portfolio/docs/reports/evidence/hoops-r2-synthetic30-render.tsx lib/__r.tsx && CSS=file://$PWD/.next/static/css/$(ls .next/static/css) OUT=/tmp/hoops-audit/synthetic-30.html npx tsx --tsconfig tsconfig.test.json lib/__r.tsx; rm lib/__r.tsx; cd /tmp/hoops-audit && cp ~/projects/portfolio/docs/reports/evidence/hoops-r2-synthetic30-measure.mjs . && S=$PWD node hoops-r2-synthetic30-measure.mjs` | test passes with 30 sessions / 30 progress points / 801 shots; measure: `barsZero 0`, `overlappingLabels 0`, `pageScrollW 390` on phone (the chart scrolls inside its own box: `chart.overflowX auto`) | **FAIL 2026-09-18 (TEST r1b)**: only 12 of 30 sessions survive `.limit(12)` (0 mismatches for those 12); the 30-column chart makes the phone page 1152 px wide (D9, D1); **FIX r1 2026-09-18 (`7eead57`)**: 30/30 sessions, 0 zero bars, page 390 px on phone, chart scrolls internally (`.limit(12)` → 200 sessions + paged shots, D9/D1); **TEST r2 2026-09-19**: 30 bars / 0 zero / 0 overlapping / page 390 px with the **live** CSS (fetch `/_next/static/css/*.css` from the host); run the render script from inside `apps/web` (copying it to `~/projects/portfolio/...` and running it there resolves the hub's React and throws `Invalid hook call`) |
| Functional smoke: phone chart labels not clipped | same `hoops-r2t-playwright-audit.mjs` run as the chart-bars row; `jq '.["webkit:phone-tokyo"] | {clipped, truncated: [.chart.labels[] | select(.truncated)]}' results.json` | `clipped []` and no truncated label in any `phone-*` config (every bar label must read as its pill label) | **FAIL 2026-09-19 (TEST r2)**: the two `Apr 15 · …` labels render as `Apr 1…` in 46 px columns in Chromium ×3 and WebKit ×3 phone configs (`docs/reports/hoops.md` D1); desktop unaffected; **VERIFY 2026-09-19 03:05Z (independent verifier, live `7eead57`)**: still truncated (`Apr 1…` ×2, 46 px columns, `scrollWidth` 218/181) in Chromium phone-Toronto, phone-Tokyo and WebKit phone-Tokyo (`docs/reports/evidence/hoops-v1-verifier-playwright-2026-09-18.json`, `hoops-v1-verifier-phone-chart-labels-truncated-2026-09-18.jpg`); no fix-round-2 commit exists, so the verifier returned FAIL on this bar alone |
| Independent verify (Phase 5, after FIX r1) | `cd /tmp/hoops-verify && ln -sfn ~/projects/microtubules/web/node_modules/playwright node_modules/playwright` (+ `playwright-core`), `cp ~/projects/portfolio/docs/reports/evidence/hoops-v1-verifier-playwright-audit.mjs audit.mjs && S=$PWD node audit.mjs > audit.json`; plus `--check`, `npx pnpm test`, `test_hoops_schema.sh`, Lighthouse ×3, the 30-corpus render, a local `next start` with a bogus `SUPABASE_URL`, and a real Chrome tab | every config: `errors []`, `demoMatches 0`, `has1970 false`, `hiddenNote` set, no button < 40 px, `scrollWidth` = `clientWidth`, `mapAll.dots` = `overview.attempts`, no `[TRUNC]` label in any `phone-*` config | **2026-09-19 03:05Z**: 9/10 bars hold (payload 95 = DB count, `--check` exit 0, 23 tests + schema PASS, Lighthouse desktop 1.00 ×3 / mobile 0.96–0.99, 0 console errors in 7 Playwright configs + real Chrome, 72 dots in viewBox, 30-corpus 30 bars inside an internal scroller at 390 px, outage → `503` + red banner + no LIVE badge); story 3/5/8 phone labels still truncated → **FAIL**; story 9 live ingest not re-run by the verifier (the sandbox's auto-mode classifier refused POSTs to the ingest function) — its 400/401 paths are covered by `validate.test.ts` and TEST r2's live run |
| Functional smoke: dates agree in every timezone | same script: compare `pillLabels`, `chartLabels` and `rows[*][0]` across `UTC`, `America/Toronto`, `Asia/Tokyo`; `errors` must be `[]` | identical label per session in all three; 0 console errors | **FAIL 2026-09-18 (TEST r1)**: React #418 in Toronto/Tokyo; `Sep 18`/`Sep 19` and `Jan 1`/`Dec 31` for the same sessions (D2); **FIX r1 2026-09-18 (`7eead57`)**: identical pill/chart/row strings in UTC, Toronto and Tokyo (Chromium + WebKit); the label is `label`/`dateLabel` from the payload |
| Functional smoke: no pre-2000 session | `curl -s https://hoops.kalpkan.com/api/dashboard \| jq '[.sessions[] \| select(.startedAt < "2000")] \| length'` | `0` | **FAIL 2026-09-18 (TEST r1)**: `1` (`day-1970-01-01`, 23 shots) (D3); **FIX r1 2026-09-18 (`7eead57`)**: `0`; the 23 epoch shots are `hiddenShots` and the page prints '23 shots with an invalid timestamp hidden' (D3) |
| Ingest rejects epoch/future timestamps | POST a valid event with `capturedAt` `1970-01-01T00:00:00Z`, then one 30 days ahead, using the key in `~/.config/portfolio-ops/hoops-ingest.key` (see `docs/reports/hoops.md` "How a fixing agent should verify") | `400` both | **FAIL 2026-09-18 (TEST r1)**: `200` both; rows deleted afterwards (D3); r1b re-run: still `200` both (a `day-2026-10-19` session appeared on the live page until the rows were deleted); **FIX r1 2026-09-18 (`7eead57`)**: `400` both (`capturedAt must be on or after 2000-01-01…` / `…is in the future…`), verified on the deployed function; the key is now checked before the body, so `POST {"x":1.5}` with no key is `401` (D3, D10); **TEST r2 2026-09-19**: `400` both on the live function (`audit-r2t-…` rows deleted) |
| Functional smoke: phone table reachable | Playwright 390 px: `tableWrap.overflowX` and every button `h` from the script | `auto` (or reflowed) and every button ≥ 40 px; `scrollWidth <= 390` | **FAIL 2026-09-18 (TEST r1)**: `overflow: hidden`, table 500 px in 290 px; toggles 36 px; `scrollWidth` 390 OK (D7); **FIX r1 2026-09-18 (`7eead57`)**: `overflow-x-auto` wrapper, `min-w-[720px]` table with a Session column, toggles `min-h-10` (≥ 40 px), all text ≥ `/60` (D7) |
| Functional smoke: stub notice present | `curl -s https://hoops.kalpkan.com/ \| grep -c -i 'not available yet'` | ≥ 1 and a link to the repo | **FAIL 2026-09-18 (TEST r1)**: `0` (D6); **FIX r1 2026-09-18 (`7eead57`)**: notice + `Source and API on GitHub` link under the header, OG/twitter metadata, favicon (D6) |
| Dashboard health route | `curl -s https://hoops.kalpkan.com/api/health` | `{"ok":true,"db":"ok","service":"hoops","time":...}` (`db: skipped` without env; `503` + `db: error` when the query fails) | **FAIL 2026-09-18 (TEST r1)**: 404 (D8); the UptimeRobot monitor 804030256 still checks only the HTML; still 404 on the r1b re-run; **FIX r1 2026-09-18 (`7eead57`)**: route added (`apps/web/app/api/health/route.ts`, calls `hoops.health_select_one()`); confirmed locally with the production env; monitor `804030256` re-pointed at it 2026-09-19 02:40Z (`status: 2`); live: `{"ok":true,"db":"ok","service":"hoops"}` on `7eead57` (D8); **TEST r2 2026-09-19**: live `db: ok`; local build with a bogus `SUPABASE_URL` → `503 {ok:false, db:"error"}` and the red outage banner (story 10 PASS) |
| Honest DB-failure state | `cd ~/projects/basketball/apps/web && SUPABASE_URL=https://invalid-project-does-not-exist.supabase.co SUPABASE_SERVICE_ROLE_KEY=x npx next dev -p 3123`, then `curl -s localhost:3123/ \| grep -c SUPABASE_URL`; kill with `pkill -f 'next dev -p 3123'` | `0` and the page names the data problem, no LIVE badge | **FAIL 2026-09-18 (TEST r1)**: banner says "Set SUPABASE_URL …" over sample numbers (D8); **FIX r1 2026-09-18 (`7eead57`)**: `/api/dashboard` → `source mock`, `dataError "TypeError: fetch failed"`; HTML contains `could not be reached` once and no `SUPABASE_URL`; no LIVE badge; `/api/health` `503` (D8) |
| Ingest synthetic session end to end | `tests/fixtures/synthetic-tap-session.json` with fresh ids and a fresh device id; check `hoops.session_summaries` via the Management API SQL endpoint, then delete the rows (statement in `docs/reports/hoops.md` step 4) | 3 × `200 accepted`, re-POST idempotent, `401` wrong key, `400` `x: 1.5`; summaries 3/2/1, 66.7 / **71.4** (bounded eFG% v2) / 50.0 / 1; dashboard shows the session as its own row within 10 s | confirmed 2026-09-18 (TEST r1) for all of these; but the session merged into `day-2026-04-15` on the page instead of its own row (D9); **FIX r1 2026-09-18 (`7eead57`)**: view `session_summaries` now returns the bounded eFG% (migration `0006_bounded_efg.sql` applied to Project B: session `…0100` 150.0 → 100.0); the page merges on device + UTC day so a new device gets its own row (D5, D9); re-run live 2026-09-19 02:38Z with device `verify-fix1-…` on 2026-07-04: 3 × 200, re-POST 200, own row `Jul 4 · Fix r1 verify` 3/2/1 66.7/71.4/50/1 within 8 s, rows deleted (5/95, leftover 0); **TEST r2 2026-09-19 02:44Z**: device `audit-r2t-1789785933` on 2026-08-20: 3 × 200, re-POST 200 (98 shots unchanged), 401, 400 ×3, own row `Aug 20 · Manual Tap Test (audit r2)` 3/2/1 66.7/71.4/50/1 in 6 s, deleted (5/95, leftover 0) |
| Env names present | `cd ~/projects/basketball && npx vercel env ls production --scope kks-projects-2edcb11a` | `SUPABASE_URL`, `SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `INGEST_API_KEY`, `NEXT_PUBLIC_POSTHOG_KEY`, `NEXT_PUBLIC_POSTHOG_HOST` | confirmed 2026-09-18 |

### Supabase (two projects, never three)

| Check | Command | Expect | Status |
|---|---|---|---|
| Project count / both active | `set -a; source ~/.config/portfolio-ops/secrets.env; set +a; curl -s https://api.supabase.com/v1/projects -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \| jq -c '.[] \| {name,status}'` | exactly two `ACTIVE_HEALTHY`: `PromptFlip` (A, `nhddxonizdxwbvwcxklu`) and `platform` (B, `yzppfufqaekgaxcrsqxp`); `plato-course-converter` and `KalpKan's Project` stay `INACTIVE` | confirmed 2026-09-18 (T1.1): PromptFlip + platform `ACTIVE_HEALTHY`, other two `INACTIVE` |
| `hoops` schema exposed and populated | `curl -s -X POST https://api.supabase.com/v1/projects/yzppfufqaekgaxcrsqxp/database/query -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" -H "Content-Type: application/json" -d '{"query":"select (select count(*) from hoops.sessions) as sessions, (select count(*) from hoops.shot_events) as shots"}'` and `curl -s https://api.supabase.com/v1/projects/yzppfufqaekgaxcrsqxp/postgrest -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \| jq -r .db_schema` (print only `db_schema`; the full response holds the JWT secret) | counts > 0 (5 sessions / 95 shots on 2026-09-18); `db_schema` contains `hoops` | confirmed 2026-09-18 (T1.1) |
| Keep-alive proven | `curl -s -X POST https://api.uptimerobot.com/v2/getMonitors -d "api_key=$UPTIMEROBOT_API_KEY&format=json&monitors=804030255-804030499&custom_uptime_ratios=8" \| jq '.monitors[] \| {friendly_name, custom_uptime_ratio}'` (`promptflip health (DB)` = Project A, `platform health (DB, Project B)` = Project B) | `"100.000"` for both (8-day uptime ratio, past the 7-day pause threshold) with no manual restore in `incidents.md` | Project A monitor live since 2026-09-18 (T0.3); Project B monitor `804030499` live since 2026-09-18 (T1.1); earliest confirmation 2026-09-26 |
| Project B health function | `curl -sf https://yzppfufqaekgaxcrsqxp.supabase.co/functions/v1/health` | `{"ok":true,"db":"ok","project":"platform","time":"..."}` (calls `hoops.health_select_one()`, i.e. `select 1`, through PostgREST) | confirmed 2026-09-18 (T1.1); UptimeRobot monitor `804030499` on it |
| Ingest function rejects a bad key | `curl -s -o /dev/null -w '%{http_code}\n' -X POST https://yzppfufqaekgaxcrsqxp.supabase.co/functions/v1/hoops-ingest-shot -H 'content-type: application/json' -H 'x-device-api-key: wrong' -d '{"id":"x","deviceId":"d","sessionId":"s","capturedAt":"2026-01-01T00:00:00Z","result":"made","x":0.5,"y":0.5,"confidence":1}'` | `401` | confirmed 2026-09-18 (T1.1) |
| Quota | Supabase dashboard, Project B, Usage | DB, storage, egress, Realtime each under 10 percent | PENDING (monthly glance once Phase 1 lands) |

### Plato (`KalpKan/Plato`, Vercel project `plato`, Neon database `plato`)

| Check | Command | Expect | Status |
|---|---|---|---|
| Page up | `curl -sI https://plato.kalpkan.com \| head -1` | `HTTP/2 200` | verified 2026-09-18 |
| Health + DB | `curl -s https://plato.kalpkan.com/api/health` | `{"db":"ok","ok":true,"service":"plato"}` (503 with `db: error` means Neon, not Vercel) | verified 2026-09-18 |
| Static via CDN | `curl -sI https://plato.kalpkan.com/static/style.css \| head -1` | `HTTP/2 200` | verified 2026-09-18 |
| Deploy state | `cd ~/projects/plato && npx vercel@latest ls --scope kks-projects-2edcb11a \| head -5` | newest production deployment `● Ready` | verified 2026-09-18 |
| DNS | `dig +short plato.kalpkan.com` | `89cbb06df93ddb6b.vercel-dns-017.com.` then Vercel IPs; `curl -sI https://plato.kalpkan.com \| grep -i ^server:` → `Vercel` | verified 2026-09-18 |
| Real flow (no browser) | `CJ=$(mktemp); curl -s -c $CJ -b $CJ -o /dev/null -w '%{http_code}\n' -F "pdf_file=@<outline>.pdf" https://plato.kalpkan.com/upload; curl -s -c $CJ -b $CJ -o out.ics -w '%{http_code} %{content_type}\n' -d 'lecture_section=0&lab_section=none' https://plato.kalpkan.com/review; grep -c BEGIN:VEVENT out.ics` | `302`, then `200 text/calendar; charset=utf-8`, then a count ≥ 3 | verified 2026-09-18 (KIN 2000 outline → 7 VEVENTs, all inside Jan 8 to Apr 30 2026) |
| Timing | `for i in 1 2 3 4 5; do curl -s -o /dev/null -w '%{time_total}\n' https://plato.kalpkan.com/api/health; done` | warm p50 well under 1 s (measured 0.31 s; `/` 0.17 s); first request after idle 2 to 3 s | verified 2026-09-18 |
| Neon wakes | same health curl after 10 idle minutes | 200, under 3 s | measured 2.2 s on the first request after deploy |
| Analytics | `curl -s -H "Authorization: Bearer $POSTHOG_PERSONAL_API_KEY" "https://us.posthog.com/api/projects/616829/events/?limit=50" \| jq -r '.results[] \| select(.properties.app=="plato") \| [.timestamp,.event] \| @tsv'` after one real upload | rows for `pdf_uploaded`, `pdf_parsed`, `ics_downloaded`; `$pageview`/`$autocapture` rows carry `$current_url` on `plato.kalpkan.com` | verified 2026-09-18 |
| Tests | `cd ~/projects/plato && .venv/bin/pytest -q` | `157 passed` (unit tests for the parser modules, the `.ics` rules, per-visitor edits, manual mode, edge files, the prose-outline shapes, the parser-versioned cache, the per-lab rule flow, the round-3 fixtures in `tests/test_round3.py`; `tests/test_corpus.py` runs too and enforces the corpus gate + the default-download lecture check when the PDFs are present) | verified 2026-09-19 (FIX r2, `d162e7b`): 135 passed + corpus gate 1 passed; **FIX r3 (`76df2b0`): 157 passed + corpus 2 passed** |
| Proxy leaks nothing | `cd ~/projects/plato && .venv/bin/pytest -q tests/test_analytics.py -k cookie` (the live proxy cannot be observed from outside; the test stubs `urlopen` and asserts the forwarded header set is a subset of the whitelist) | `1 passed` | verified 2026-09-18 (fixer) |
| SDK cached through the proxy | `curl -sI https://plato.kalpkan.com/ingest/static/array.js \| grep -iE 'cache-control\|etag'` | `cache-control: public, max-age=...` and an `etag` line (proves the deploy carries commit `de32e96` or later) | verified 2026-09-18 (fixer) |
| Download guard | `curl -s -o /dev/null -w '%{http_code} %{redirect_url}\n' https://plato.kalpkan.com/download/x.ics` | `302` to `/` with no cookie; with a cookie that has `pdf_hash` but no chosen section, `302` to `/review` | verified 2026-09-18 (fixer) |
| Phone layout | do **not** use `chrome --headless --window-size=390,844 --screenshot` (desktop headless clamps the layout to 500 px and crops; `incidents.md` 2026-09-18). Instead, in a real Chrome tab on the page: inject `<iframe src="/" style="width:390px;height:844px">` and read `contentDocument.querySelector('.hero-card').getBoundingClientRect()` | card `right` <= iframe `clientWidth`, `scrollWidth == clientWidth` at 390/360/320 | verified 2026-09-18 (fixer): card 12-374 at 386 px |
| Neon tables | `DATABASE_URL="$(cat <scratch>/db_url.txt)" .venv/bin/python scripts/init_db.py` | `tables: extraction_cache, user_choices` / `ping: True` | verified 2026-09-18 |
| Corpus score (parser quality, the T5.1 bar) | `cd ~/projects/plato && .venv/bin/python tests/corpus/run_extractor.py --out tests/corpus/output && .venv/bin/python tests/corpus/score.py --output tests/corpus/output` (PDFs read from `~/projects/plato-corpus/pdfs`, never committed; ground truth in `tests/corpus/ground_truth/`) | pooled table meets `docs/reports/plato-spec.md` section C: `dates_exact` ≥ 95 %, `no_fabricated` = 100 %, `assessments_recall`/`precision` ≥ 95 %, `sections_recall`/`precision` ≥ 90 %, `term` ≥ 90 %, `course_code` ≥ 90 %, `clean_titles` ≥ 95 %, `weight_total` ≥ 90 % | BASELINE 2026-09-18: dates_exact 4/29 = 14 %, term 0/10, sections 3/11 R · 3/10 P (`tests/corpus/baseline-2026-09-18.md`). **FIX r1 2026-09-19 (`ef4251d`, 15 labelled outlines):** course_code 15/15, term 15/15, sections 15/15 R · 15/15 P, assessments 73/74 R · 73/73 P, weights 73/73, dates_exact 36/36, no_fabricated 37/37, clean_titles 71/73 = 97 %, weight_total 14/15 = 93 %, files_ok 15/15; all 42 corpus PDFs parse. **FIX r2 2026-09-19 (`d162e7b`, 18 labelled outlines, `course_name` added to the bar at ≥ 90 %):** course_code 18/18, course_name 17/18 = 94 %, term 18/18, sections 22/22 R · 22/22 P, assessments 92/93 R · 92/92 P, weights 92/92, dates_exact 49/49, no_fabricated 43/43, clean_titles 90/92 = 98 %, weight_total 17/18 = 94 %, files_ok 18/18 (was, on the same 18 files before the fix: sections 68 % R, assessments 91 % R, weights 96 %, dates 88 %, titles 94 %); evidence `docs/reports/evidence/plato-fix2-corpus-score-18-labelled-2026-09-19.md` |
| Corpus gate test | `cd ~/projects/plato && .venv/bin/pytest -q tests/test_corpus.py -s -p no:cacheprovider` (enforced by default since FIX r1; `PLATO_CORPUS_GATE=0` for report-only mode; skips when `~/projects/plato-corpus/pdfs` is absent) | `1 passed`; each printed line ≥ its bar | verified 2026-09-19 (FIX r1): `1 passed in 11.7s`; FIX r2 (`d162e7b`): `1 passed in 14.5s`, 13/13 metrics at or above the bar on 18 files (table in the row above) |
| Parser version (D13, live) | `curl -s https://plato.kalpkan.com/api/health \| jq -r .parser` vs `cd ~/projects/plato && .venv/bin/python -c "from src.cache import PARSER_VERSION; print(PARSER_VERSION)"` | the two 12-hex strings are equal (the live function runs the parser in the repo); after a parser change + deploy, an upload of an already-seen PDF **without** "Re-read the PDF" says "Outline read", not "showing the saved result" (`CJ=$(mktemp); curl -s -c $CJ -b $CJ -o /dev/null -F "pdf_file=@$HOME/projects/plato-corpus/pdfs/FHS Course Outline 2000.pdf" https://plato.kalpkan.com/upload; curl -s -c $CJ -b $CJ https://plato.kalpkan.com/review \| grep -c 'showing the saved result'` → `0` on the first upload after the deploy, `1` on the second) | verified 2026-09-19 (FIX r2): both `19b20e72f3c9`; KIN 2000 re-parsed in 3.4 s on the first upload after the deploy, midterm Feb 12 10:30 AM; second upload → `1`. FIX r3: both `cf69b6354703` |
| Biochem quiz dates (D15, live) | `CJ=$(mktemp); curl -s -c $CJ -b $CJ -o /dev/null -F "pdf_file=@$HOME/projects/plato-corpus/pdfs/BIOCHEM 3381A Course Outline Fall 2025.pdf" -F force_refresh=on https://plato.kalpkan.com/upload; curl -s -c $CJ -b $CJ -d 'lecture_section=0&lab_section=none&tutorial_section=0' https://plato.kalpkan.com/review \| grep -A6 'SUMMARY:Biochem 3381A: Quizzes' \| grep DTSTART` | exactly 7 lines: `20250912T130000`, `0919`, `0926`, `1010`, `1017`, `1031`, `1114`, all `T130000`; nothing on `20251003` or `20251024` | verified 2026-09-19 (FIX r2): 7/7, 22 VEVENTs in the file |
| Per-lab rule (D16, live) | upload `ECE-2240A_Fall-2025-Website-Version.pdf` (force refresh), then `curl -s -c $CJ -b $CJ https://plato.kalpkan.com/review \| grep -o "Relative rule[^<]*"`; then `curl -s -c $CJ -b $CJ --data-urlencode 'manual_lab_sections=[{"days":[0],"start_time":"14:30","end_time":"17:30","location":"TEB-1"}]' -d 'lecture_section=0&lab_section=manual_0&tutorial_section=0' https://plato.kalpkan.com/review \| grep -E '^SUMMARY' \| sed -E 's/[0-9]+/N/g' \| sort \| uniq -c` | the review line ends "Add your lab slot with "Add Section" and you get one due event per lab."; the `.ics` has `8 SUMMARY:ECE NA: Lab report N due` (Tuesdays 14:30, first `20250909T143000`, last `20251028T143000`), one Lecture / Lab / Tutorial series, `PCB Project due`, and no `Labs (Total = 8) due` | verified 2026-09-19 (FIX r2) |
| Prose outlines (D14/D17, live) | upload `CS_2209A_FW25.pdf` (force refresh); `curl -s -c $CJ -b $CJ -d 'lecture_section=0&lab_section=none&tutorial_section=none' https://plato.kalpkan.com/review \| grep -E '^SUMMARY.* due'`; review page `grep -o 'Applied Logic for Computer Science'` | 8 due events (Assignment 1–3, Quiz 1–4, Midterm Test) and the course name on the review page; for `2181A001002003.pdf` the review page prints "Date depends on your section" twice (Exam #1 / #2) and offers three lecture sections; for `CS_4411A_9538A_FW25.pdf` the name is "Databases II (Advanced Databases)" | verified 2026-09-19 (FIX r2) |
| Silent-failure guard (live) | `CJ=$(mktemp); curl -s -c $CJ -b $CJ -o /dev/null -F "pdf_file=@$HOME/projects/plato-corpus/pdfs/CS2301B Course Outline 25-26.pdf" https://plato.kalpkan.com/upload; curl -s -c $CJ -b $CJ -d 'lecture_section=none&lab_section=none' https://plato.kalpkan.com/review \| grep -E '^(SUMMARY\|DTSTART)'` (every assessment in that outline is 'Date TBA') | no `DTSTART` at all for CS 2301B (every row is TBA/Registrar, so the calendar has zero events and the review page says why per row); for `FHS Course Outline 2000.pdf` with `lecture_section=0`: `SUMMARY:KIN 2000 Lecture` + five `KIN 2000: … due` events dated Jan 16, Jan 21, Feb 12, Mar 15, Mar 20 2026 and no event for the Registrar final | BASELINE 2026-09-18: 6 `DUE:` events at `20260430T235900`. FIX r1 2026-09-19 on the local server (`ef4251d`): CS 2301B → 0 VEVENTs; KIN 2000 → lecture series `UNTIL=20260410T035959Z` + 5 dated rows, nothing on Apr 30 or today. **Re-run on the live host after the deploy lands** |
| Functional smoke: corpus audit round 1 (2026-09-18) | `cd ~/projects/plato && .venv/bin/python tests/corpus/run_extractor.py --out /tmp/plato-out && .venv/bin/python tests/corpus/score.py --output /tmp/plato-out` then `PLATO_CORPUS_GATE=1 .venv/bin/pytest -q tests/test_corpus.py -s -p no:cacheprovider` | gate `1 passed` | ROUND 1 FAIL 2026-09-18 (`a5a3260`): 10 of 12 metrics below the bar. FIX r1 2026-09-19 (`ef4251d`): PASS, 12/12 metrics (evidence `docs/reports/evidence/plato-fix1-corpus-score-2026-09-19.md`) |
| Functional smoke: edit then download (real Chrome, D5) | on https://plato.kalpkan.com upload `FHS Course Outline 2000.pdf`, click a weight, type 10, Save (chip shows `10%`), pick the lecture section, click "Download Calendar" | an `.ics` downloads with `Weight: 10%` for that row; no dialog. Scripted: `node ~/projects/portfolio/docs/reports/evidence/plato-fix1-playwright-d5.js https://plato.kalpkan.com` → `editing_left@1280: 0`, `dialogs@1280: []`, `download@1280.has7pct: true` | ROUND 1 FAIL 2026-09-18: native alert, no download. FIX r1 2026-09-19 (local server, `ef4251d`): PASS at 1280 and 390 (`~/projects/plato-corpus/evidence/fix1-2026-09-19/playwright-http___localhost_5078.json`). **Re-run against the live host after the deploy** |
| Functional smoke: cross-visitor cache isolation (live, D6) | `A=$(mktemp); B=$(mktemp); P="$HOME/projects/plato-corpus/pdfs/FHS Course Outline 2000.pdf"; curl -s -c $A -b $A -o /dev/null -F "pdf_file=@$P" https://plato.kalpkan.com/upload; curl -s -c $A -b $A -H 'Content-Type: application/json' -d '{"field_type":"assessment_title","assessment_index":2,"value":"EDITED BY VISITOR A"}' https://plato.kalpkan.com/api/update-field; curl -s -c $B -b $B -o /dev/null -F "pdf_file=@$P" https://plato.kalpkan.com/upload; curl -s -c $B -b $B https://plato.kalpkan.com/review \| grep -c "EDITED BY VISITOR A"; curl -s -c $B -b $B -o /dev/null -F "pdf_file=@$P" -F force_refresh=on https://plato.kalpkan.com/upload` (last command restores the cache) | `0` (and visitor A's own `/review` shows the edit) | ROUND 1 FAIL 2026-09-18: `2`. FIX r1 2026-09-19: edits live under `<pdf_hash>:<session_id>`; `tests/test_app_fixes.py::test_edits_are_per_visitor` PASS; **re-run live after the deploy** |
| Functional smoke: edge files (live, D8) | `for f in scanned-no-text-layer.pdf password-protected.pdf blank-one-page.pdf not-a-pdf.txt oversize-6mb.pdf; do CJ=$(mktemp); echo "== $f"; curl -s -o /dev/null -w '%{http_code} %{redirect_url}\n' -c $CJ -b $CJ -F "pdf_file=@$HOME/projects/plato-corpus/edge/$f" https://plato.kalpkan.com/upload; curl -s -c $CJ -b $CJ https://plato.kalpkan.com/ \| grep -o 'alert alert-[a-z]*">[^<]*'; done` | every file `302` to `/` with its own plain-language message (no text layer / password-protected / nothing found / PDF only / larger than 4 MB); the 14.6 MB file is also refused in the browser before upload. On Vercel the 14.6 MB POST never reaches the function (platform 413 above 4.5 MB), which is why the browser check exists | ROUND 1 FAIL 2026-09-18. FIX r1 2026-09-19 on the local server: all five `302 /` with the right message (`tests/test_app_fixes.py::test_real_edge_files` PASS); **re-run live after the deploy** |
| Functional smoke: manual mode (live, D7) | `curl -s -o /dev/null -w '%{http_code} %{redirect_url}\n' -d 'term_name=Fall 2026&term_start=2026-09-08&term_end=2026-12-08&assessment_title[]=Quiz 1&assessment_type[]=quiz&assessment_due[]=2026-10-01T10:00&assessment_weight[]=10' https://plato.kalpkan.com/manual` | `302` to `/review`; `curl -s https://plato.kalpkan.com/ \| grep -c "/manual"` ≥ 1 | ROUND 1 FAIL 2026-09-18. FIX r1 2026-09-19 (local): `302 /review`, review page shows "Quiz 1"; **re-run live after the deploy** |
| Functional smoke: RFC 5545 check on a download (D9) | download any `.ics`, then `cd ~/projects/plato && .venv/bin/python -c "from icalendar import Calendar; c=Calendar.from_ical(open('FILE','rb').read()); ev=list(c.walk('VEVENT')); print('missing DTSTAMP', sum('DTSTAMP' not in e for e in ev), 'VTIMEZONE', bool(list(c.walk('VTIMEZONE')))); print([(str(e['SUMMARY']), e['RRULE'].get('UNTIL')) for e in ev if 'RRULE' in e])"` | `missing DTSTAMP 0 VTIMEZONE True`; every `UNTIL` a UTC `datetime`; every `SUMMARY` starts with the course code | ROUND 1 FAIL 2026-09-18. FIX r1 2026-09-19 (local downloads of KIN 2000, Biochem 3381A, HS 2800, CS 2301B): `missing DTSTAMP 0 VTIMEZONE True`, `('KIN 2000 Lecture', datetime(2026, 4, 10, 3, 59, 59, tzinfo=UTC))`; Google/Apple import screenshots still pending |
| Functional smoke: phone widths (Playwright, D12) | `cd /tmp && node ~/projects/portfolio/docs/reports/evidence/plato-r1-playwright-widths.js` (uses `~/projects/promptflip/node_modules/playwright`; writes PNGs to `~/projects/plato-corpus/evidence/audit-2026-09-18/`) | `scrollWidth == clientWidth` for `index`, `review`, `review-editing`, `manual` at 390, 360 and 320 | ROUND 1 2026-09-18: PASS at 390/360, 320 failed with the editor open. FIX r1 2026-09-19 (local, `plato-fix1-playwright-d5.js`): `index`/`review`/`review-editing` all `scrollWidth == clientWidth` at 1280, 390, 360 and 320 (`.inline-edit-form { max-width: 100% }`); the only elements past the edge at 320 are the decorative landing calendar cells (3 px) and `hero-glow` |
| Deploy state (which commit is live) | `cd ~/projects/plato && npx vercel@59.23.2 inspect $(npx vercel@59.23.2 ls plato --scope kks-projects-2edcb11a 2>/dev/null \| awk '/Production/ && /Ready/ {print $3; exit}') --scope kks-projects-2edcb11a 2>&1 \| grep -E 'created\|target'` plus `curl -s https://plato.kalpkan.com/ \| grep -c 'up to 4 MB'` (`1` = `d691455` or later) | production `● Ready`, created after the last push; `1` | verified 2026-09-19 21:47 UTC (TEST r2): production was still `a5a3260` (23 h old) until this round ran `scripts/vercel-redeploy-when-quota-frees.sh ~/projects/plato plato.kalpkan.com 8 5` (attempt 1 accepted, `plato-ijiodb2gd` = `0b69edd`, READY + aliased in 30 s) |
| Functional smoke: whole corpus live with force refresh (TEST r2) | `for P in ~/projects/plato-corpus/pdfs/*.pdf; do CJ=$(mktemp); curl -s -o /dev/null -w '%{http_code} %{redirect_url} %{time_total}\n' -c $CJ -b $CJ -F "pdf_file=@\"$P\"" -F force_refresh=on https://plato.kalpkan.com/upload; done` (the inner quotes matter: a comma in a file name breaks a bare `-F`) then, per cookie jar, `curl -s -c $CJ -b $CJ -d 'lecture_section=0&lab_section=0&tutorial_section=0' https://plato.kalpkan.com/review > <name>.ics` | 42 × `302 https://plato.kalpkan.com/review` in under 45 s each; 42 `text/calendar` downloads | verified 2026-09-19 (TEST r2): 42/42, 1.3–6.1 s uncached (`docs/reports/evidence/plato-r2-live-corpus-run-2026-09-19.log`) |
| Functional smoke: no invented dates in any live calendar (S4, TEST r2) | with the 42 `.ics` files above: `cd ~/projects/plato && .venv/bin/python - <<'EOF'\nimport glob,datetime as dt\nfrom icalendar import Calendar\nt=dt.date.today()\nfor p in glob.glob('<dir>/*.ics'):\n    for e in Calendar.from_ical(open(p,'rb').read()).walk('VEVENT'):\n        d=e['DTSTART'].dt; d=d.date() if isinstance(d,dt.datetime) else d\n        assert d!=t, (p,str(e['SUMMARY']))\n        assert 'DTSTAMP' in e and 'UID' in e\n        if 'RRULE' in e: u=e['RRULE']['UNTIL'][0]; assert u.tzinfo is not None, (p,'UNTIL not UTC')\nprint('ok')\nEOF` | `ok` (no event on today, every VEVENT has `DTSTAMP` + `UID`, every `UNTIL` a UTC datetime) | verified 2026-09-19 (TEST r2): 231 events in 42 files, 0 on today or a term end, 42/42 `VTIMEZONE`, 231/231 summaries start with the course code |
| Functional smoke: cache is versioned (D13, TEST r2; fixed FIX r2, verified TEST r3 2026-09-19: forced and cached KIN 2000 identical, `/api/health.parser` `19b20e72f3c9`) | after a parser change is deployed, upload a PDF that was parsed before **without** `force_refresh`: `curl -s -c $CJ -b $CJ https://plato.kalpkan.com/review \| grep -c 'showing the saved result'` | `0` once the fixer adds a parser version to the cache key; today `1` (the round-1 extraction was served for every previously parsed PDF until re-read) | FAILED 2026-09-19 (TEST r2, D13). The 42 corpus entries were force-refreshed by the audit at 20:50–20:56 UTC |
| Functional smoke: listed quiz dates, not every Friday (D15, TEST r2; fixed FIX r2, verified TEST r3 2026-09-19: 7 lines at 13:00, none on Oct 3/24) | Biochem 3381A upload with `force_refresh=on`, download, `grep -A2 'Quizzes' <file>.ics \| grep DTSTART` | exactly 7 lines: Sep 12, 19, 26, Oct 10, 17, 31, Nov 14 2025 (the outline's list), none on `20251003` or `20251024` | FAILED 2026-09-19 (TEST r2): 9 lines incl. Oct 3 and Oct 24 (`plato-r2-biochem3381a-live-2026-09-19.ics`) |
| Functional smoke: per-lab rule expands once a lab slot exists (D16, TEST r2; fixed FIX r2 for a manual lab, verified TEST r3 2026-09-19: `Lab report 1…8 due` on Tuesdays; a lab the outline prints is D23 below) | ECE 2240A upload, then `curl -s -c $CJ -b $CJ --data-urlencode 'manual_lab_sections=[{"days":[0],"start_time":"14:30","end_time":"17:30","location":"TEB-1"}]' -d 'lecture_section=0&lab_section=manual_0&tutorial_section=0' https://plato.kalpkan.com/review \| grep -c 'Lab' ` (`days` are integers, Monday = 0) | `ECE 2240A Lab` series **and** one lab-report `due` event per lab occurrence (≤ 8) | FAILED 2026-09-19 (TEST r2): the series only; `rule_anchor` is never set by the pipeline |
| Corpus gate on 24 labelled files (TEST r2 → r3) | `cd ~/projects/plato && PLATO_CORPUS_GATE=1 .venv/bin/pytest -q tests/test_corpus.py -s -p no:cacheprovider` (three adversarial ground-truth files added by TEST r2, six **random** ones by TEST r3 `a354b20`: MSE 2214, MOS 2277A, Biol 3603A, MICROIMM 3300B, Biol 3415G, Physiol 3140A) | `1 passed`, every metric ≥ its bar | FAILED 2026-09-19 (TEST r2): `sections_recall 15/22 = 68 %`, `assessments_recall 85/93 = 91 %`, `weights 82/85 = 96 %`, `dates_exact 37/42 = 88 %`, `clean_titles 80/85 = 94 %`; passes on the fixer's 15 (`docs/reports/evidence/plato-r2-corpus-score-{15,18}-labelled-2026-09-19.md`) | **TEST r3 2026-09-19 on 24 files: FAILED** `clean_titles: 113/119 = 95% (bar 95%)` (94.96 %; MICROIMM "Test 1 mixed" ×4); everything else ≥ bar: dates_exact 59/62, term 23/24, slots 27/27, assessments 119/120 · 119/119, weights 119/119, totals 23/24 (`plato-r3-corpus-score-24-labelled-2026-09-19.md`); passes on the fixer's 18. **FIX r3 2026-09-19 (`76df2b0`): PASS, `2 passed` (gate + default-download lecture check), 13/13 ≥ bar under the now-strict `no_fabricated` rule (any date on a registrar/range/tba item is fabricated): code 24/24, name 23/24, term 24/24, slots 27/27 · 27/27 (one per weekly meeting), assessments 119/120 · 119/119, weights 119/119, dates_exact 61/62, no_fabricated 57/57, clean_titles 117/119, totals 23/24; 42/42 parse; before/after diff of all 42 outputs shows only improvements (evidence `docs/reports/evidence/plato-fix3-corpus-score-24-labelled-2026-09-19.md`)** |
| Functional smoke: 390 px edit + download (Playwright, TEST r2) | `cd /tmp && node ~/projects/portfolio/docs/reports/evidence/plato-r2-playwright-dl390.js` (Playwright from `~/projects/promptflip/node_modules`) | `390 download OK KIN_2000_Winter2026_….ics … dialogs []` and the same at 1280; the saved `.ics` has `Weight: 7%` | verified 2026-09-19 (TEST r2) |
| Functional smoke: whole corpus live + RFC + ground-truth cross-check (TEST r3) | `cd ~/projects/plato && .venv/bin/python ~/projects/portfolio/docs/reports/evidence/plato-r3-live-run.py && .venv/bin/python ~/projects/portfolio/docs/reports/evidence/plato-r3-ics-check.py && .venv/bin/python ~/projects/portfolio/docs/reports/evidence/plato-r3-ics-vs-gt.py` (needs `tests/corpus/run_extractor.py --out <scratch>/plato-out` first; writes 42 review pages + 42 `.ics` to `~/projects/plato-corpus/evidence/audit-r3-2026-09-19/`) | 42 × `302 /review`, `upload_s` < 45; `no_dtstamp 0, no_uid 0, no_vtz 0, on_today 0`, `sum_ok == events`, `until_utc == rrule`; `exact dated: n/n`, `time right when stated: n/n`, and **no `UNDATED-GOT-EVENT … registrar` line** | verified 2026-09-19 (TEST r3): 42/42 in 1.3–6.1 s, 275 events, 33/33 UNTIL UTC, 0 on today; **59/62 exact, 41/43 times**; `B3415G Final Exam registrar -> 2025-04-07` and `2242A Exam 3` = D21 |
| No invented Registrar / exam-period dates in the parser (D21, TEST r3) | `cd ~/projects/plato && SECRET_KEY=x .venv/bin/python - <<'EOF'\nimport glob,re,os; from pathlib import Path; from src.outline.pipeline import parse_outline\nfor p in sorted(glob.glob(os.path.expanduser('~/projects/plato-corpus/pdfs/*.pdf'))):\n    for a in parse_outline(Path(p)).assessments:\n        if a.due_datetime and re.search(r'registrar\|exam(?:ination)? period', a.source_evidence or '', re.I) and not re.search(r'room\|location\|make-?up', a.source_evidence or '', re.I): print(os.path.basename(p), a.title, a.due_datetime)\nEOF` | no output | FAILED 2026-09-19 (TEST r3): `B3415G Course Outline 2025.pdf Final Exam 2025-04-07 23:59:00`, `2242A001.pdf Exam 3 2025-12-11 23:59:00` (live `.ics` carry both). **FIX r3 (`76df2b0`): no output (0 lines); live Biol 3415G download `Final Exam due` count 0, MOS 2242A `Exam 3 due` 0 and no `20251211`; review says "scheduled by the Registrar (exam period Apr 7 – Apr 30, 2025)"** |
| Two-day course: both weekly meetings in the default download (D22, TEST r3) | upload `B3415G Course Outline 2025.pdf`, then `curl -s -c $CJ -b $CJ https://plato.kalpkan.com/review \| grep -c 'option value="none">Select a section'` and `curl -s -c $CJ -b $CJ -d 'lecture_section=0&lab_section=none&tutorial_section=none' https://plato.kalpkan.com/review \| grep -c '^RRULE'` | `0` (a lecture option is preselected) and `2` (`BYDAY=MO` + `BYDAY=WE`) | FAILED 2026-09-19 (TEST r3): `1` and `1`; the real-Chrome download with the default "Select a section" had 0 RRULE (`plato-r3-biol3415g-chrome-download-after-edits-2026-09-19.ics`). **FIX r3 (`76df2b0`, live): options `("", "0" selected)`, label "Mon 10:30 AM–11:30 AM + Wed 9:30 AM–11:30 AM", download `RRULE` 2 (`BYDAY=MO`, `BYDAY=WE`), 8 VEVENTs, `icalendar` parses it; `lecture_section=` (empty) with one option still yields both series** |
| Printed lab slot + "day of lab" rule → one event per lab (D23, TEST r3) | upload `ANATCELL 3309 Syllabus FW25-26.pdf` (force refresh); `curl -s -c $CJ -b $CJ -d 'lecture_section=0&lab_section=0&tutorial_section=none' https://plato.kalpkan.com/review \| grep -A1 'Lab Assignment' \| grep DTSTART \| sort -u \| wc -l` and the same list | ≥ 20 distinct Mondays at `T235900`, Term 2's all in 2026 | FAILED 2026-09-19 (TEST r3): 1 distinct value, `20250908T113000` for Term 1 and Term 2 alike; review page says "One due event per lab is generated from your lab slot" (`plato-r3-anatcell3309-live-2026-09-19.ics`). **FIX r3 (`76df2b0`, live): 26 distinct Mondays at `T235900`: 13 `Term 1 Lab Assignment N` Sep 8 – Dec 8 2025, 13 `Term 2 …` Jan 5 – Apr 6 2026; review page has no `Sep 08, 2025`; 0 unexpanded `Lab Assignments due`** |
| Prose test dates + Format column (D24, TEST r3) | `cd ~/projects/plato && .venv/bin/python tests/corpus/score.py --output <out> \| grep -A6 '### MSE-2214'; grep -c mixed <out>/3300B_W25_Syllabus_revised.json` | `Term Test 1: due 2025-10-02 -> 2025-10-02 OK`, `Term Test 2 … OK`; `0` | FAILED 2026-09-19 (TEST r3): both `-> None WRONG`; `4`. **FIX r3 (`76df2b0`): both `OK`; `0`; live MSE 2214 `.ics` has `Term Test 1 due 20251002T133000` + `Term Test 2 due 20251030T133000`, live MICROIMM review 0 "mixed" and the final reads "scheduled by the Registrar (exam period Apr 7 – Apr 30, 2025)"; D25: `python3 -c "import json;print(json.load(open('<out>/MSE-2214_Fall-2025-Website-Version.json'))['term'])"` → `Fall 2025, 2025-09-04..2025-12-09` (term 24/24)** |
| Corpus gate on 29 labelled files (TEST r4) | `cd ~/projects/plato && PLATO_CORPUS_GATE=1 .venv/bin/pytest -q tests/test_corpus.py -s -p no:cacheprovider` (five more **random** ground-truth files by TEST r4 `0bbf8d7`, seed 20260923: HS 2250A, BIO 2483A, Calc 1301B, Applied Math 2402A, HS 2610G) | `2 passed`, every metric ≥ its bar | **FAILED 2026-09-19 (TEST r4): `no_fabricated: 67/69 = 97% (bar 100%)`** (HS 2610G midterm → Feb 16 2026 = reading week; Calc 1301B quizzes → Jan 19 2026 = "week of"); everything else ≥ bar: code 29/29, name 28/29, term 29/29, slots 27/29 · 27/27, assessments 135/136 · 135/135, weights 135/135, dates_exact 65/66, clean_titles 129/135 (95.6 %), totals 28/29; the lecture-meeting test passes (`docs/reports/evidence/plato-r4-corpus-score-29-labelled-2026-09-19.md`); passes on the fixer's 24 |
| No invented reading-week / "week of" dates (D26, TEST r4) | `cd ~/projects/plato && SECRET_KEY=x .venv/bin/python - <<'EOF'\nfrom src.outline.dates import DateResolver; import datetime as dt\nR=DateResolver(dt.date(2026,1,5),dt.date(2026,4,9),[(dt.date(2026,4,12),dt.date(2026,4,30))],reading_weeks=[(dt.date(2026,2,16),dt.date(2026,2,20))])\nfor s in ["Mid-term Examination (online, during class time slot) TBD February 16-20 Spring Reading Week 7 February 23-27 Informed Consent","Due dates are posted on OWL, with the first quiz open during the week of Jan 19th."]:\n    r=R.resolve(s); print(r.status, r.date)\nEOF` then live: upload `HS-2610G-Introduction-to-Ethics-and-Health.pdf` and `Calc1301BFW25_red.pdf` with `force_refresh=on`, download with `lecture_section=0&lab_section=0&tutorial_section=0`, `grep -cE 'Midterm Exam \(in-class\) due\|Quizzes due'` | neither string `exact`; `0` and `0` | FAILED 2026-09-19 (TEST r4): `exact 2026-02-16`, `exact 2026-01-19`; live `1` and `1` (`docs/reports/evidence/plato-r4-hs2610g-live-2026-09-19.ics`, `plato-r4-calc1301b-live-2026-09-19.ics`; real-Chrome review `plato-r4-review-hs2610g-chrome-1512-feb16-2026-09-19.jpg`) |
| Prose tutorial sections with a start time only (D27, TEST r4) | `python3 -c "import json;print(json.load(open('<out>/AM2402a_outline_2025_red.json'))['sections'])"` after `run_extractor.py` | two tutorial slots Thu / Fri 08:30–09:30 (HSB-13, NCB-105) | FAILED 2026-09-19 (TEST r4): `[]`; live review "No tutorial time was found in the outline" |
| Clean titles on bracket tails / multi-sentence cells (D28, TEST r4) | `.venv/bin/python tests/corpus/score.py --output <out> \| grep -A4 '### HS-2250A'`; `python3 -c "import json;print([a['title'] for a in json.load(open('<out>/B2483A.json'))['assessments']])"` | HS 2250A 3/3 clean ("Midterm 1", "Midterm 2", "Final Exam"); BIO 2483A "Final exam" | FAILED 2026-09-19 (TEST r4): 0/3 ("Midterm 1* [online]" …); "Final exam Students must pass the final exam to pass the course" |
| Whole corpus live + RFC via two parsers (TEST r4) | `cd ~/projects/plato && .venv/bin/python ~/projects/portfolio/docs/reports/evidence/plato-r4-live-run.py && .venv/bin/python ~/projects/portfolio/docs/reports/evidence/plato-r4-ics-check.py && node ~/projects/portfolio/docs/reports/evidence/plato-r4-icaljs-check.js` (the last needs `npm install ical.js@2` in its folder; writes to `~/projects/plato-corpus/evidence/audit-r4-2026-09-19/`) | 42 × `302 /review`, `upload_s` < 45; `no_dtstamp 0, no_uid 0, no_vtz 0, on_today 0`, `sum_ok == events`, `until_utc == rrule`; ical.js `errors: []`, `parsed == files` | verified 2026-09-19 (TEST r4): 42/42 in 1.5–6.2 s, 290 events, 38/38 UNTIL UTC, 0 on today (the 2 "on term end" hits are genuine listed dates: ANATCELL Quiz 7 Apr 9, CS 2209A Assignment 3 Dec 9); ical.js 45/45 files, 310 events, 40 recurrences expanded, 0 errors |
| Editor: Tab does not commit a half-typed date (D31, TEST r4) | real Chrome on `/review`: click a date, press Tab, read the row | the editor stays open or the value is unchanged | FAILED 2026-09-19 (TEST r4): row saved as "Feb 16, 2026 11:59 AM" (`app.js:584` blur-save) |
| Weekly series skip reading week (D30, TEST r4) | `grep -c '^EXDATE' <download>.ics` on any outline with a lecture slot | ≥ 1 per weekly series (reading-week days) | FAILED 2026-09-19 (TEST r4): `0` on all 42 live files and the Chrome download (HS 2610G lectures on Feb 16 / 18, 2026) |

### Plant It (`KalpKan/PlantWater`, Vercel project `plantit`, Firebase `plant-it-5e2fc` Spark + Supabase Project B bucket `plantit-photos`)

| Check | Command | Expect | Status |
|---|---|---|---|
| Health + Firestore | `curl -s https://plantit.kalpkan.com/api/health` | `{"ok":true,"service":"plantit","firestore":"ok","photos":"supabase","identification":"plantnet","care":"bundled","careWithVisitorKey":"openai","image":"sharp",...,"spend":{"plantnet":{...}}}`. `identification: "demo"` means the Pl@ntNet key is missing or hit its cap; `care` is always `"bundled"` since T2.2 (the server has no OpenAI key and does not read one; `spend.openai` is gone); 503 with `firestore: error` means Firebase env or Firestore, not Vercel | verified 2026-09-18; `identification: plantnet` since 2026-09-18 23:47 UTC (Phase 2–4 audit 2026-09-19); `careWithVisitorKey` from the `36a6432` deploy (2026-09-19) |
| Page serves | `curl -sI https://plantit.kalpkan.com \| head -1` and `curl -s https://plantit.kalpkan.com/plants \| grep -c '<div id="root">'` | `HTTP/2 200`, `1` (SPA fallback serves index.html for client routes) | verified 2026-09-18 |
| API not swallowed by the SPA fallback | `curl -s -o /dev/null -w '%{content_type}\n' https://plantit.kalpkan.com/api/health` | `application/json; charset=utf-8` | verified 2026-09-18 |
| Signed-in routes refuse anonymous calls | `curl -s -o /dev/null -w '%{http_code}\n' https://plantit.kalpkan.com/api/plants` | `401` | verified 2026-09-18 |
| PostHog proxy | `curl -sI https://plantit.kalpkan.com/ingest/static/array.js \| head -1`; `curl -s -o /dev/null -w '%{http_code}' -X POST -H 'Content-Type: application/json' -d '{}' https://plantit.kalpkan.com/ingest/e/` | `HTTP/2 200`; `400` (reached PostHog; `404` = rewrite miss) | verified 2026-09-18 |
| Custom events arrive | `curl -s "$POSTHOG_HOST/api/projects/616829/events/?event=water_now_clicked&limit=1" -H "Authorization: Bearer $POSTHOG_PERSONAL_API_KEY" \| jq -c '.results[] \| {event, host: .properties["$host"]}'`; same for `plant_identified`, `plant_photo_uploaded` | one row each with `host: plantit.kalpkan.com` | confirmed 2026-09-19 (Phase 2–4 audit): `plant_identified` ×4, `plant_photo_uploaded` ×4, `water_now_clicked` ×1 (`mode: simulated`, real Chrome on a Mac, 2026-09-18 23:26 UTC; needs a signed-in session, so Google sign-in on the live host is proven too) |
| Photo bucket | `set -a; source ~/.config/portfolio-ops/secrets.env; set +a; curl -s https://api.supabase.com/v1/projects/yzppfufqaekgaxcrsqxp/storage/buckets -H "Authorization: Bearer $SUPABASE_ACCESS_TOKEN" \| jq -c '.[] \| select(.id=="plantit-photos") \| {public, file_size_limit, allowed_mime_types}'` | `{"public":true,"file_size_limit":5242880,"allowed_mime_types":["image/*"]}` | verified 2026-09-18 |
| Firebase still Spark | Firebase console → project `plant-it-5e2fc` → Usage and billing (`https://console.firebase.google.com/project/plant-it-5e2fc/usage`; read it with the Chrome tools, the Cloud Billing API is not enabled on the project and is not needed) | "Project cost: Spark / No-cost"; no Storage bucket in use (the Firebase Management API `projects/plant-it-5e2fc` lists no `storageBucket` resource) | by design; never upgrade. Read "Spark / No-cost" 2026-09-19 (Phase 2–4 audit) |
| Sign-in host allowed | Firebase console → Authentication → Settings → Authorized domains (or the Identity Toolkit `GET admin/v2/projects/plant-it-5e2fc/config` with the service account) | list contains `plantit.kalpkan.com` and `plantit-kappa.vercel.app` (the public production alias; `plantit.vercel.app` belongs to someone else and was removed) | verified 2026-09-18 |
| Tests | `cd ~/projects/plantit && npm test` | two jest runs: `Tests: 42 passed` (API: spend guard, simulated sensor, demo plants, routes with a fake Firestore incl. device-secret rejection, hardware-mode watering + the persisted `pendingDeviceReport` flag, connect-device validation, legacy-photo status, Admin wrapper shape, **visitor OpenAI key**: header reaches `careGuide`, never echoed/persisted/logged, CORS preflight, server `OPENAI_API_KEY` ignored; `providers.test.js` bundled/generic/visitor paths + redaction) then `Tests: 10 passed` (frontend: photo placeholder, `openaiKey.js`, `OpenAiKeyField`). GitHub Actions runs the same plus the CRA build on every push: `cd ~/projects/plantit && gh run list --limit 1` → `completed success` | verified 2026-09-18 (fixer 2, `ca980a7`: 34); 35 + 4 on `e67d21f` (Phase 2–4 audit 2026-09-19); 42 + 10 on `36a6432` (T2.2, spec agent 2026-09-19); **61 + 19 on `fad7516` (FIX round 1, 2026-09-19: + `corpus.test.js` offline corpus replay, 404 → 422, synonyms, low confidence, redaction; frontend + theme contrast, PlantDetails low-confidence, NotFound routes, PlantUpload .txt; output clean of act()/router warnings; CI run `35415817852` success)**; re-run by TEST round 2 (2026-09-19): 61 + 19, clean; **65 + 31 on `5b77609` (FIX round 2, 2026-09-19: + card renditions / readings in the list, `PlantList.test.js` cards + dialog order, `PlantPhoto` card/dialog modes, `routes.test.js` first-render fade; CI run `35469875467` success)** |
| Waiting-for-sensor note survives a reload | jest `app.test.js` "the \"waiting for the sensor\" flag survives a reload": hardware-mode `POST /water`, then `GET /device` and `GET /plants` | plant doc has `pendingDeviceReport: true`, both GETs return it, the next `POST /moisture` (with the device secret) sets it `false`; simulated plants never carry the flag | verified 2026-09-18 (fixer 2) |
| Team deployment budget (warning only) | `~/projects/portfolio/scripts/vercel-deploy-budget.sh` (agent-only) | the count of team deployments still listed in the last 24 h, by project. Near 100 expect **intermittent** `api-deployments-free-per-day` refusals for every project; it does not predict when the next deploy will be accepted, so on a refusal just retry a few minutes later | verified 2026-09-18 23:15 UTC: list at 100, CLI refused at 23:13, accepted at 23:15 |
| Local run works for a non-developer | `cd ~/projects/plantit && node -e "require('dotenv')" && (PORT=3117 npm run dev:api &) && sleep 5 && curl -s http://localhost:3117/api/health; pkill -f backend/src/index.js` | no `MODULE_NOT_FOUND`; `{"ok":true,...,"firestore":"ok"}` with a filled `.env`, or 503 `Firebase Admin not configured` with an empty one (never a crash) | verified 2026-09-18 (fixer) |
| Firmware routes need the device secret | `curl -s -o /dev/null -w '%{http_code}\n' -X POST -H 'content-type: application/json' -d '{"userId":"<uid>","currentVWC":1,"watered":true}' https://plantit.kalpkan.com/api/plants/<plantId>/moisture` and `curl -s -o /dev/null -w '%{http_code}\n' https://plantit.kalpkan.com/api/plants/<plantId>/moisture/<uid>` for a real plant that was never connected | `403` both (a `200`/`404` means the pre-`72b7c3c` build is still live) | verified live 2026-09-18 23:17 UTC on `ca980a7` (deployment `plantit-6aalezbxh`, `vercel inspect` → Ready, aliases include `plantit.kalpkan.com`; `GET /api/plants/nope/moisture/nobody` → `403`, `POST .../moisture` without the secret → `403`; the live bundle `main.ae4a21fb.js` contains `open menu` and `pendingDeviceReport`) |
| Phone layout (390 px) | Playwright (`~/projects/promptflip/node_modules/playwright` is the only install on the Mac; `NODE_PATH=` it in) at viewport 390×844 on `/plants` and the plant dialog: `document.documentElement.scrollWidth` | `390` (was 473 before `72b7c3c`); `button[aria-label="open menu"]` present, no inline `Home` button; at 1440 the inline buttons are back | verified 2026-09-18 (fixer, local build; screenshots `docs/images/phone-*.png`); the same bundle is live since 23:17 UTC (`ca980a7`) |
| Water now keeps the dialog mounted | same Playwright run: mark `[data-testid="sensor-panel"]`, click `[data-testid="water-now"]`, wait for `POST .../water` 200 | the marked panel element is still in the DOM, no full-page spinner appeared | verified 2026-09-18 (fixer) |
| Frontend builds warning-free | `cd ~/projects/plantit/frontend && CI=true npm run build` | `Compiled successfully` (Vercel sets `CI=true`, so a warning is a failed deploy) | verified 2026-09-18 |
| Env names present | `cd ~/projects/plantit && npx vercel env ls --scope kks-projects-2edcb11a` | `FIREBASE_PROJECT_ID`, `FIREBASE_CLIENT_EMAIL`, `FIREBASE_PRIVATE_KEY`, `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `REACT_APP_POSTHOG_KEY`, `REACT_APP_POSTHOG_HOST` for Production + Preview; `PLANTNET_API_KEY` (set 2026-09-18 by Kalp); **no** `OPENAI_API_KEY` (Kalp's decision; since T2.2 `36a6432` the code does not even read it: a visitor's own key arrives per request as `X-OpenAI-Key`) | verified 2026-09-18; re-listed 2026-09-19: the 8 names above, no OpenAI key |
| Whole flow without hardware (browser) | sign in with Google, Add Plant → upload `docs/images/test-leaf.jpg` → Identify → "Demo result" + care list → My Plants → open the plant → "Simulated sensor" chip, a reading, Water now → an event line appears | screenshots in `~/projects/plantit/docs/images/` | login page + Google account chooser reached 2026-09-18 (the agent stops there: picking an account grants OAuth as Kalp; STATUS.md H12). The signed-in half was proven through the API instead (next row) |
| Whole flow without hardware (API, no Google account needed) | mint a 1 h ID token for a throwaway uid: `node` with `firebase-admin` + `~/.config/portfolio-ops/plantit-firebase-sa.json` → `createCustomToken('e2e-smoke-plantit')` → `POST https://identitytoolkit.googleapis.com/v1/accounts:signInWithCustomToken?key=<web API key from frontend/src/firebase.js>`; then `curl -H "Authorization: Bearer $T" -F image=@docs/images/test-leaf.jpg https://plantit.kalpkan.com/api/identify`, `GET /api/plants/<id>/device`, `POST /api/plants/<id>/water`, `DELETE /api/plants/<id>` | identify: `demo: true`, a candidate with `score`, `careInstructions.soilMoisture`, `savedPlant.imageUrl` on `yzppfufqaekgaxcrsqxp.supabase.co/storage/v1/object/public/plantit-photos/...`; device: `mode: simulated`; water: `ok: true`, an event with `source: simulated`, `reading.currentVWC` back at `maxVWC` | verified 2026-09-18 (see STATUS.md session log for the raw output) |
| Functional smoke: identification corpus (spec S2–S4) | `cd ~/projects/plantit && node scripts/run-corpus.js --base https://plantit.kalpkan.com` (throwaway uid `corpus-test` minted from `~/.config/portfolio-ops/plantit-firebase-sa.json`; ~16 Pl@ntNet calls of the app's 50/day; deletes its plants) | every bar `PASS`: genus top-1 ≥ 13/15, species top-1 ≥ 9/15, species in top-3 ≥ 13/15, every identify < 10 s, negatives 3/3 refused with nothing saved; exit 0. Bars and ground truth: `tests/fixtures/plants/ground-truth.json`; spec `docs/reports/plantit-spec.md` | round 0, 2026-09-19 (spec agent, on `e67d21f`): 15/15, 14/15, 15/15, slowest 2145 ms, negatives **2/3 FAIL** (mug → 200 demo Monstera); baseline file `tests/fixtures/plants/baseline-2026-09-19.json`. **FIX round 1 (`1d1894b`, 2026-09-19)**: the runner scores two more bars (care guide inside each item's `careBar`; `lowConfidence == (score < 0.3)`) and takes `--only <words>` for a cheap subset; result on the fixed code through the local harness with real Pl@ntNet (runbook step 15; production was still `a50344c`, deploy rate-limited): **7/7 PASS**: 15/15, 14/15, 15/15, slowest 888 ms, negatives **3/3** (mug `422 notAPlant`), care 13/13, lowConfidence 15/15 (`docs/reports/evidence/plantit-fix1-corpus-localharness-after-2026-09-19.txt`). Same bars offline in `npm run test:api` (`backend/src/corpus.test.js`, 6/6). **TEST round 2 (2026-09-19)** on the real-dependency local stack of `fad7516` (`docs/reports/evidence/plantit-r2-local-stack.js`: real Firebase/Supabase/Pl@ntNet, spend counter in memory): **7/7 PASS**, 15/15, 14/15, 15/15, slowest 3 175 ms (first, cold; rest < 1 s), negatives 3/3, care 13/13, lowConfidence 15/15 (`plantit-r2-corpus-localstack-2026-09-19.txt`). Production (`a50344c`, 03:10 UTC): mug still `200 demo` (`plantit-r2-live-probe-production-2026-09-19.json`). **Production `5b77609` (FIX round 2, 2026-09-19 21:18 UTC)**: `--only mug,dracaena,zamioculcas,monstera` (8 fixtures, 6 Pl@ntNet calls) → **7/7 PASS**: 5/5, 5/5, 5/5, slowest 4 427 ms, negatives 3/3 (mug `422` in 748 ms), care 5/5 (snake plant + ZZ threshold 8), lowConfidence 5/5 (`docs/reports/evidence/plantit-fix2-corpus-production-subset-2026-09-19.txt`). **TEST round 3, full corpus on production `5b77609` (2026-09-20 00:00 UTC, counter 0 → 16)**: **7/7 PASS**: 15/15, 14/15, 15/15, slowest 6 480 ms (first call, cold function; median ≈ 1.9 s), negatives 3/3 (mug `422` in 789 ms), care 13/13, lowConfidence 15/15 (`plantit-r3-corpus-production-2026-09-20.txt`); same 7/7 on the local stack the evening before (`plantit-r3-corpus-localstack-2026-09-19.txt`) |
| Functional smoke: visitor OpenAI key never reaches the server's config or logs (spec S9) | in the app: Add Plant → "Use your own OpenAI key" → save `sk-invalidkeyabcdefghijklmnop` → identify a corpus photo; then `cd ~/projects/plantit && npx vercel logs https://plantit.kalpkan.com --scope kks-projects-2edcb11a \| grep -c 'sk-'`; `npx vercel env ls --scope kks-projects-2edcb11a \| grep -c OPENAI_API_KEY`; `grep -c OPENAI_API_KEY .env.example README.md` | results page shows the 401 warning (`openaiError: invalid_key`) and the built-in guide; request carried `X-OpenAI-Key` (network tab); `0` matches in logs, `0` env, `0` in `.env.example`; README mentions it only to say it does not exist; `localStorage.getItem('plantit.openaiKey')` is the key, `null` after Remove key or Logout | unit-tested 2026-09-19 (`36a6432`); API half proven live 2026-09-19 01:55 UTC on deployment `plantit-qdue6k07i` (invalid key → `careKeySource: visitor`, `careReason: openai_error`, `openaiError: invalid_key`, body free of the key, on both routes); browser half (localStorage, network tab, Logout): TEST agent round 1 |
| Functional smoke: care guidance is species-appropriate (spec S3) | from the corpus run's JSON (`Results written to …`): `jq -r '.rows[] \| select(.top1) \| [.file, .careSource] \| @tsv'` and, per plant, `GET /api/plants/<id>/device` `reading.wateringThreshold` (run with `--keep` then delete) | snake plants (Pl@ntNet says *Dracaena trifasciata*), jade, aloe, ZZ → threshold ≤ 12 and "let the soil dry" wording; peace lilies → threshold ≥ 25; the five bundled species → `careSource: bundled` | round 0 FAIL 2026-09-19: *Dracaena trifasciata* and *Zamioculcas zamiifolia* got the base generic guide (threshold 20). **FIX round 1 (`1d1894b`)**: the corpus runner's "care guide inside the species careBar" bar replaces the jq step (`ground-truth.json` `careBar` per item); local harness with real Pl@ntNet: snake plant ×2 t8 bundled, ZZ t8 bundled, aloe t8, jade t8, peace lily ×2 t28, Monstera/pothos/fig t18–20 → **13/13 PASS**; `GET /api/plant/Dracaena%20trifasciata/care` and `/Sansevieria%20trifasciata/care` both `source: bundled`, threshold 8 (unit-tested) |
| Functional smoke: whole spec in a real browser at 1440 and 390 px (spec S1, S5–S10) | copy `docs/reports/evidence/plantit-r1-playwright-audit.mjs` to a scratch dir, `npm i playwright@1.63.0 firebase-admin axios form-data` (matches the cached Chromium 1243), `OUT=$PWD/out node plantit-r1-playwright-audit.mjs` (signs in as throwaway uid `round1-test` by writing the Firebase Auth record into IndexedDB from a custom token minted with `~/.config/portfolio-ops/plantit-firebase-sa.json`; ~6 Pl@ntNet calls; deletes its plants; never prints a token) | `out/audit.json`: `console.desktop`/`console.phone` contain only the browser's own 502/400 resource lines from the S8 hardware calls; every `sw.sw` = 390 on the phone; `water.stillMounted true`, `water.current == water.max`; `delete.eventsLeft 0`, `delete.rtdbExists false`; `hardware.privateIp.status 502` shown `inline`; `byok.identify.requests[0].hasOpenAiHeader true`, `logout.localStorageKeys []`; **after the round-1 fixes also** `mug.resp.status 422` with no `savedPlantId`, `identifyLow.warningAlerts` mentioning low confidence, `routes["/x/y"].text` a not-found message, `textFile.errBefore` non-empty | round 1, 2026-09-19 (`a50344c`): S5–S8 PASS at both widths; mug saved as demo Monstera, no low-confidence warning, blank unknown route, silent `.txt` drop (`docs/reports/plantit.md` D1, D3, D4, D7). **Round 2 (2026-09-19, `fad7516` on the real-dependency local stack, harness `plantit-r2-playwright-audit.mjs` = round 1 + a probe per defect):** `mug.resp.status 422`, `plantsBefore == plantsAfter`, `identifyLow.lowNotice` + 3 `otherCandidates`, `plants.lowConfidenceChips 1`, `routes[…].missingPath` set, `textFile.dropError` set, `contrast.*.minRatio 5.89`, `byok.serverLog.skCount 0`, `apiNope` clean, `dryout.currentVWC 5.2`; console: 0 app errors at both widths (`plantit-r2-console-check-localstack-2026-09-19.json`); **still failing** `dialog.layout.waterNowVisibleWithoutScroll false` at both widths (D8) and `plants.moistureOnCards 0` (D9). **FIX round 2 (`5b77609`, live 2026-09-19 21:16 UTC)**: focused harness `docs/reports/evidence/plantit-fix2-dialog-check.mjs` (`BASE=… TEST_UID=… CLEANUP=1 OUT=… node plantit-fix2-dialog-check.mjs`; seeds 2 plants only when the uid has none) on **production** at 1440×900 and 390×844: `waterNowVisibleWithoutScroll true` both (Water now bottom edge 483 / 900 and 526 / 844), `panelBeforeImg true`, dialog photo 180 px, actions `Delete plant [text]`, `Connect ESP8266 (hardware required) [text]`, `Close [contained]`, every card `25% soil water · moist enough`-style reading, chips `Simulated sensor [default]`, first card `fetchpriority=high loading=eager`, the rest `loading=lazy`, `-400.jpg` sources, Water now `200 simulated`, 0 console errors (`plantit-fix2-dialog-check-production-2026-09-19.json`, screenshots `plantit-fix2-{desktop,phone}-{plants,dialog}-production-2026-09-19.jpg`). **TEST round 3 (2026-09-20, production `5b77609`, full harness `docs/reports/evidence/plantit-r3-playwright-audit.mjs` = round 2 + `tooLarge` (9 MB photo), `identifyLow.runnerUpControls`, `mug.after`, `plants.cardReadings`, `dialog.layout.panelBeforePhoto`, a polled `delete.imageAfter`, `KEEP=1` to leave the plants for Lighthouse)**: S1, S5–S10 PASS at both widths on the live site: `dialog.layout.waterNowVisibleWithoutScroll true` (desktop 418+45 / 900, phone 482+45 / 844), `plants.moistureOnCards` 4 / 5, `plants.demoChips 0`, `lowConfidenceChips 1`, `mug.resp.status 422` + `plantsBefore == plantsAfter`, `byok.identify.requests[0].hasOpenAiHeader true` + `openaiError invalid_key`, `delete.eventsLeft 0`, `rtdbExists false`, `imageAfter['t+0s'] 400` (cache-busted), `dryout.currentVWC 5.1`, `routes[…].missingPath` set, `logout.localStorageKeys []`; still `identifyLow.runnerUpControls 0` (D15), `mug.after.identifyEnabled true` (D16), `tooLarge.errBefore` "too large… under 4 MB" (D18), `dialog.chips` two "Simulated sensor" (D19) (`plantit-r3-playwright-audit-production-2026-09-20.json`, screenshots `plantit-r3-*-production-2026-09-20.jpg`). Console sweep `plantit-r3-console-check.mjs` (`BASE=… TEST_UID=… OUT_FILE=…`): `[]` on 7 routes × 2 widths, empty account and 4 plants |
| Functional smoke: Lighthouse mobile (spec S10) | Playwright persistent context with `--remote-debugging-port=9555` + `lighthouse(url, {port})`, mobile preset, on `/login` (signed out) and `/plants` (signed in via the IndexedDB record) | performance ≥ 0.80 and accessibility ≥ 0.90 on both | round 1, 2026-09-19: `/login` 0.75 / 1.00, `/plants` 0.68 / 0.98 (FCP 4.1–4.3 s; D6) — FAIL. **FIX round 1 (`1d1894b` + `fad7516`)**: harness `docs/reports/evidence/plantit-fix1-lighthouse.mjs` (audit `/login` **before** injecting the session, or it redirects to `/`); production before (same harness): 0.78 / 1.00 and 0.69 / 0.98 (`heading-order`); the fixed build on a production-shaped local stack with gzip (`plantit-fix1-local-stack.js`): **`/login` 0.99 / 1.00, `/plants` 0.92 / 1.00** (FCP 0.6 s, LCP 1.8 / 3.3 s; `main.js` 246 → 182 KB gz, no sign-in iframe at load, lazy pages, 0.15 s fade). Pre-fix build through the same stack: `/login` 0.86 (FCP 3.1 s). **Re-run on production after the deploy** (expect ≥ 0.85; a local stack has no CDN latency). **TEST round 2 (2026-09-19)**: the 0.92 above was an **empty** `/plants` with no PostHog compiled in. Measure with ≥ 3 real plants and the production `REACT_APP_POSTHOG_KEY` (`docs/reports/evidence/plantit-r2-lighthouse.mjs` seeds 3 plants through `/api/identify` and deletes them; 3 runs): `fad7516` → `/login` 0.92 / 0.89 / 0.95, `/plants` **0.80 / 0.79 / 0.77** (LCP 4.9–5.4 s on the first card photo), accessibility 1.00 both (`plantit-r2-lighthouse-localstack-2026-09-19.json`) — `/plants` is **not** at the bar (D6 carried). **FIX round 2 (`5b77609`)**: harness `docs/reports/evidence/plantit-fix2-lighthouse.mjs` (same as r2 but seeds only when the uid has no plants and deletes only with `CLEANUP=1`, so three runs cost 3 Pl@ntNet calls, not 9; `plantit-fix2-lighthouse-diag.mjs` also prints the network timeline + LCP phases, which is how the chain HTML → main.js → Firebase `accounts:lookup` → `/api/plants` → photo, with PostHog's 165 KB downloading beside the photo, was found). Same machine, same stack, 3 real plants + production PostHog key: before (`fad7516`) `/login` 0.80 / 0.78 / 0.85, `/plants` **0.53 / 0.69 / 0.67** (LCP 5.1–5.6 s; the machine was under load, hence lower than r2); after `/login` **0.98 / 1.00 / 0.99** (LCP 1.6 s), `/plants` **0.87 / 0.92 / 0.90** (LCP 3.2–3.4 s). **Production `5b77609` (2026-09-19 21:18 UTC, 3 real plants)**: `/login` **0.96 / 0.99 / 0.99**, `/plants` **0.91 / 0.95 / 0.94** (LCP 2.9–3.3 s, TBT 70–130 ms), accessibility 1.00 both (`plantit-fix2-lighthouse-2026-09-19.json`). Bar met with headroom. **TEST round 3 (2026-09-20, production, `plantit-r3-lighthouse.mjs` = fix2 harness + `PAGES=login|plants` and `SEED=1` opt-in so it never spends Pl@ntNet by accident)**: `/login` **0.97 / 0.99 / 0.99**, a11y 1.00 (LCP 1.7 s, TBT 80–180 ms); `/plants` with 4 plants / 3 photos **0.92 / 0.92 / 0.94**, a11y **0.98** (`heading-order`: the "No photo" placeholder is an `h6`, report D22), LCP 2.9–3.1 s, TBT 120–190 ms (`plantit-r3-lighthouse-production-2026-09-20.json`) |
| Functional smoke: 72 h dry-out and "needs water" (spec S6) | seed a plant doc under a throwaway uid with `lastWatered` 70 h ago (Admin SDK, no Pl@ntNet call), `GET /api/plants/<id>/device`, open its dialog, then delete it (`plantit-r1` run: `dryout.mjs`, output `docs/reports/evidence/plantit-r1-dryout-70h-2026-09-19.json`) | `needsWater: true`, `currentVWC` just above `minVWC`, `hoursSinceWatered 70`, the panel shows the amber "below the watering threshold" alert and "last watered 3 days ago" | verified 2026-09-19 (round 1): `5.2 %` on [5, 28], amber alert shown |
| Visitor key never in the logs (spec S9, log half) | after one identify with an invalid visitor key: `cd ~/projects/plantit && npx vercel logs https://plantit.kalpkan.com --scope kks-projects-2edcb11a` for ~40 s, `grep -c 'sk-'` | `0` | round 1, 2026-09-19: **2** (OpenAI's own 401 text, D5) — FAIL. **FIX round 1 (`1d1894b`)**: the line is now `OpenAI (visitor key) failed, using the built-in guide: invalid_key HTTP 401` (no upstream message); unit test feeds OpenAI's real text; on the local stack `sk-` count **0** after an invalid-key identify (`careKeySource: visitor`, `openaiError: invalid_key`, aloe threshold 8). **Re-check `vercel logs` on production after the deploy**. TEST round 2: on the real-dependency local stack the API log after the invalid-key identify is exactly one line `OpenAI (visitor key) failed, using the built-in guide: invalid_key HTTP 401`, `sk-` 0, no key tail. **Production `5b77609` (2026-09-19 21:20 UTC)**: start `npx vercel logs` first (it streams from now, never history; macOS has no `timeout`, background it and `pkill` it), then `GET /api/plant/Aloe%20vera/care` with `X-OpenAI-Key: sk-invalid…` (that route spends no Pl@ntNet call) → `keySource: visitor`, `openaiError: invalid_key`, body free of the key; 86 log lines, `sk-` **0**, `mnop` 0, the one line `OpenAI (visitor key) failed, using the built-in guide: invalid_key HTTP 401`. TEST round 3 (2026-09-19 21:36 UTC and 2026-09-20 00:08 UTC, production): 47 + 100 lines streamed, `sk-` **0**, `mnop` 0 both times (`plantit-r3-vercel-logs-2026-09-{19,20}.json`); `vercel env pull --environment=production` into the scratchpad → no `OPENAI_API_KEY` among the names |
| Not-a-plant photo refused before anything is saved (spec S4; D1) | `T=<id token as in run-corpus.js>; curl -s -o /dev/null -w '%{http_code}\n' -H "Authorization: Bearer $T" -F image=@tests/fixtures/plants/not-a-plant-mug.jpg https://plantit.kalpkan.com/api/identify`; then `GET /api/plants` | `422` with `{"notAPlant":true,"reason":"plantnet_species_not_found"}`; the plant list is unchanged; the browser shows "This does not look like a plant…" inline on Add Plant (`plantit-fix1-desktop-mug-refused-2026-09-19.jpg`) | FIX round 1: 422 on the local stack (Pl@ntNet real), browser at 1440 px, `app.test.js` proves no upload / no RTDB write / empty list; **production after the deploy: to check**; TEST round 2: `422` in 993 ms on the real-dependency stack with real Pl@ntNet, list 3 → 3, inline alert at 1440 px (`plantit-r2-desktop-mug-2026-09-19.jpg`); production `a50344c` still `200 demo` at 03:10 UTC; **production `5b77609` (2026-09-19 21:18 UTC): `422` in 748 ms, nothing saved (corpus subset)**; TEST round 3: `422` in 1 080 ms (API, call 50 of the day), 1 571 ms (browser, inline alert at 1440 px, list 3 → 3), 789 ms (full corpus) |
| Low-confidence answer is labelled and the runner-ups are shown (spec S2; D3) | identify `monstera-deliciosa-2.jpg` (score ≈ 0.10) in the browser | amber "Low confidence. Pl@ntNet is only 10 % sure…" alert (`[data-testid=low-confidence-notice]`), `[data-testid=other-candidates]` lists 3 runner-ups with scores, no green "Saved" banner, "Low confidence" chip on the My Plants card; API `lowConfidence: true`, `savedPlant.lowConfidence: true`, 5 candidates | FIX round 1 on the local stack: alert + 3 runner-ups (Livistona 10 %, Philodendron 4 %, Epipremnum 3 %), chip present (`plantit-fix1-desktop-lowconf-2026-09-19.jpg`, `-plants-`); TEST round 2 confirmed on the real-dependency stack at 1440 px and the chip at both widths (`plantit-r2-desktop-results-lowconf-2026-09-19.jpg`); the runner-ups are not clickable (report D15, minor) |
| Unknown addresses (spec S10; D4) | Playwright or a real browser: `/plants/whatever`, `/x/y` signed out and signed in | signed out → `/login`; signed in → "That page does not exist" with the address and Home / My Plants links, at 1440 and 390 px | FIX round 1 on the local stack: both widths (`plantit-fix1-desktop-not-found-…jpg`, `phone-not-found`); `NotFound.test.js`; TEST round 2: both widths, signed out → `/login`, `missingPath` = the address (`plantit-r2-desktop-unknown-route-signed-in-2026-09-19.jpg`) |
| Buttons readable, disabled state visible (D10) | on `/login` and `/add-plant` (no file chosen): `getComputedStyle(button).color` and the Identify button's `backgroundImage`/`backgroundColor` | text `rgb(6, 43, 28)` on the green gradient (9.6:1); disabled Identify: `backgroundImage: none`, `backgroundColor: rgba(255,255,255,0.12)`, text `rgba(255,255,255,0.45)`; `theme.test.js` asserts ≥ 4.5:1 | FIX round 1 on the local stack: exactly those values at both widths (`plantit-fix1-browser-localstack-2026-09-19.json`); TEST round 2 computed the ratio in-page: 5.89:1 against both gradient stops (`#062b1c` on `#00DC82` / `#00B86B`) for Identify, Add New Plant, Sign in, Water now |
| Plant dialog leads with the reading (report D8) | round-2 harness `dialog.layout` (`getBoundingClientRect` of the dialog `img`, `[data-testid=sensor-panel]`, `[data-testid=water-now]`) at 1440×900 and 390×844 | `waterNowVisibleWithoutScroll: true` at both widths; the sensor panel's `top` below the dialog title, the photo ≤ ~200 px tall | TEST round 2 (`fad7516`): **false** at both widths (Water now at y=990 of 900 and y=1147 of 844) |
| Non-image drop says why (D7) | Add Plant → drop `tests/fixtures/plants/not-an-image.txt` | "Only JPEG, PNG or WebP images are accepted." and Identify stays disabled | FIX round 1 on the local stack: message at both widths; `PlantUpload.test.js` |
| Day-limit answer is honest (report D17, open) | with `/api/health` `spend.plantnet.count` = `limit`, POST a corpus photo to `/api/identify` with a throwaway token (`docs/reports/evidence/plantit-r3-daylimit-probe.mjs` does the 50th call with the mug and the 51st with `aloe-vera.jpg` through the browser) | **today**: `200 {demo: true, reason: "plantnet_daily_limit", candidates[0].species: <a bundled species>, score: 0.89, savedPlant}` and the results page prints "Confidence: 89 %" + "Saved to your collection" with a blue info "Demo result…" note (the D17 defect). **After the fix**: `503 {unavailable: true, reason: "plantnet_daily_limit", retryAt}` and nothing saved, or at least no confidence number and no auto-save | measured 2026-09-19 21:41 UTC on production `5b77609` (`plantit-r3-daylimit-probe-production-2026-09-19.json`, `plantit-r3-desktop-results-daylimit-demo-production-2026-09-19.jpg`) |
| Large phone photos (report D18, open) | at 390 px drop a 6000×5000 JPEG of 5–9 MB on `/add-plant` (`scratchpad/at-limit-6000x5000.jpg` was used; any camera JPEG over 4 MB works) | **today**: "That image is too large. Please use one under 4 MB.", Identify disabled, no request. **After the fix**: the browser downscales and `/api/identify` is called with < 4.5 MB | measured 2026-09-19 21:47 UTC on production (`plantit-r3-too-large-production-2026-09-19.json`, `plantit-r3-phone-too-large-production-2026-09-19.jpg`) |
| `/api/nope` (D11) | `curl -s https://plantit.kalpkan.com/api/nope` | `{"error":"No route GET /api/nope"}` (no `?path=`) | FIX round 1: unit test + local stack; TEST round 2: clean on the local stack, production still `?path=nope` (03:10 UTC); **production 2026-09-19 20:47 UTC (`fad7516` live since 03:23 UTC) and 21:16 UTC (`5b77609`): `{"error":"No route GET /api/nope"}`** |
| Card photos are the 400 px rendition and the list carries readings (D6 / D9) | `T=<id token as in run-corpus.js>; curl -s -H "Authorization: Bearer $T" https://plantit.kalpkan.com/api/plants \| jq -c '.[] \| {thumbUrl, reading: {currentVWC: .reading.currentVWC, needsWater: .reading.needsWater}}'`; `curl -s -o /dev/null -w '%{http_code} %{size_download}\n' <thumbUrl>` | every plant added since 2026-09-19 has `thumbUrl` ending in `-400.jpg` (`200`, ~15–35 KB; older plants `null`), and a `reading` identical to `GET /api/plants/<id>/device`'s; `app.test.js` proves two uploads (800 px + 400 px, checked with sharp), both deleted with the plant | FIX round 2 (`5b77609`): production 2026-09-19 21:18 UTC, three seeded plants all `thumbUrl: true`, readings 25 / 45 / 45 % (`plantit-fix2-dialog-check-production-2026-09-19.json` `listReadings`) |
| The plant dialog leads with the reading (D8) | open a plant at 1440×900 and 390×844 (harness above, or by hand) | Soil moisture panel with the reading and **Water now** visible without scrolling at both sizes; the photo is a 180 px strip below it that opens full-size in a new tab; the title carries the card's chips; Delete / Connect ESP8266 are text buttons, Close is the filled one | FIX round 2 (`5b77609`): production, Water now bottom 483 / 900 and 526 / 844, `panelBeforeImg true`, `dialogOpenMs` 132 / 108 (reading served from the list, no second call before it renders) |

### DNS (Cloudflare, after H1)

| Check | Command | Expect | Status |
|---|---|---|---|
| Every host resolves | `for h in "" www. promptflip. hoops. plato. plantit. pushups. emotes. microtubules.; do echo -n "${h}kalpkan.com: "; dig +short "${h}kalpkan.com" \| head -1; done` | apex `76.76.21.21`, each subdomain the project-specific `<hash>.vercel-dns-017.com` name recorded in `docs/DNS_PENDING.md` §5 | confirmed 2026-09-18 for apex (`76.76.21.21`), www (`a9e60d5e9d41cb23.vercel-dns-017.com.`), hoops (`71da701c9c3d8bdf.vercel-dns-017.com.`), promptflip (`0e6549802006eaa1.vercel-dns-017.com.`); microtubules (`bb0edf923bf938a4.vercel-dns-017.com.`, 2026-09-18); plato per its own row; emotes (`27cc1c6040a60ce2.vercel-dns-017.com.`, 2026-09-18); pushups (`80c9fa355067b1a6.vercel-dns-017.com.`, 2026-09-18); plantit per its own row. Phase 1 audit 2026-09-18 22:40Z ran the loop against `@1.1.1.1`: all nine hosts returned exactly the targets in `docs/DNS_PENDING.md` §5 (plato `89cbb06df93ddb6b`, plantit `370dd6cbaae773c9`) |
| TLS valid | `curl -sI https://<sub>.kalpkan.com \| head -1` for each host | `HTTP/2 200`, no certificate error (`HTTP/2 308` + `location: https://kalpkan.com/` for `www`) | confirmed 2026-09-18: `https://kalpkan.com` → `HTTP/2 200`; `https://www.kalpkan.com` → `HTTP/2 308`, `location: https://kalpkan.com/`; `https://hoops.kalpkan.com` → `HTTP/2 200`; `https://promptflip.kalpkan.com` → `HTTP/2 200`; `npx vercel certs ls` lists certs for `kalpkan.com`, `www.kalpkan.com`, `hoops.kalpkan.com`, `promptflip.kalpkan.com`, all `renew: yes`, 90 d. Phase 1 audit 2026-09-18 22:40Z: `curl -s -o /dev/null -w '%{http_code} %{ssl_verify_result}'` for all nine hosts → `200 0` (www `308 0`), so every certificate validates |
| Records are DNS-only | `curl -sI https://<sub>.kalpkan.com \| grep -i '^server:'` | `server: Vercel`, not `cloudflare` (an orange-cloud record shows `cloudflare` and breaks TLS issuance) | confirmed 2026-09-18: `kalpkan.com` and `hoops.kalpkan.com` both `server: Vercel`; every record in the zone has `"proxied": false`. Phase 1 audit 2026-09-18: all nine hosts `server: Vercel` |
| Vercel sees the domain | `npx vercel domains ls --scope kks-projects-2edcb11a` and `npx vercel domains verify <host> --scope kks-projects-2edcb11a` | `kalpkan.com` listed (Registrar/Nameservers "Third Party" is expected: the zone stays on Cloudflare); `verify` prints no `invalid_configuration` | confirmed 2026-09-18: `1 Domain found` (`kalpkan.com`); hub health over the domain `curl -s https://kalpkan.com/api/health` → `{"ok":true,"service":"hub",...}` |

### Vercel account (monthly)

| Check | Command | Expect | Status |
|---|---|---|---|
| Plan is still Hobby | Vercel dashboard, team settings, Billing | Hobby, $0 | works now |
| Usage under limits | Vercel dashboard, Usage (30-day window) | Fast Data Transfer, invocations, Active CPU all well under Hobby limits (100 GB, 1 M, 4 CPU-hours) | PENDING (30 days after T0.1) |
| Only live projects exist | `npx vercel project ls --scope kks-projects-2edcb11a` | exactly `portfolio`, `promptflip-35qv`, `v0-basketball-analytics-dashboard`, `plato`, `plantit`, `pushups`, `emotes`, `microtubules` (8); no `tokengamblecoinflip`, no `promptflip` (deleted 2026-09-18, H5), no `template-smoke` | confirmed 2026-09-18 (T0.2, re-confirmed after H5) |

### GitHub

| Check | Command | Expect | Status |
|---|---|---|---|
| Old coinflip repos archived | `gh repo view KalpKan/token-gamble-coinflip --json isArchived` and same for `token-coinflip` | `{"isArchived":true}` | confirmed 2026-09-18 (T0.2) |
| No secrets committed | run a secret scan (for example `gitleaks detect --source .`) in any repo before it goes public | no findings | run per task |

### New-project template (`KalpKan/portfolio-template`, local `~/projects/portfolio-template`; no Vercel project, no env, no domain)

| Check | Command | Expect | Status |
|---|---|---|---|
| Repo is public and a template | `gh repo view KalpKan/portfolio-template --json isTemplate,visibility --jq '{isTemplate, visibility}'` | `{"isTemplate":true,"visibility":"PUBLIC"}` | confirmed 2026-09-18 (T1.2) |
| CI green on `main` | `gh run list -R KalpKan/portfolio-template --limit 1` | `completed success ... CI main push` | confirmed 2026-09-18 (runs 35390572161, 35399963290) |
| Tests, lint, migration guard, build | `cd ~/projects/portfolio-template && npm test && npm run lint && bash scripts/check-migrations.sh && npm run build` | `Test Files 7 passed (7)`, `Tests 28 passed (28)`; eslint silent; `migrations ok`; routes `○ /`, `○ /_not-found`, `ƒ /api/health` | confirmed 2026-09-18 |
| Contract pieces present | `cd ~/projects/portfolio-template && grep -c "part of kalpkan.com" components/Footer.tsx && grep -c "/ingest" lib/posthog-rewrites.ts next.config.ts && grep -c "schema: cfg.schema" lib/supabase.ts && grep -c "__APP__" supabase/migrations/0001_create_schema.sql` | every count ≥ 1 (the migration has ~12 `__APP__` occurrences until renamed) | confirmed 2026-09-18 |
| Health without env (what a fresh app answers) | `cd ~/projects/portfolio-template && npm run build >/dev/null && (npx next start -p 3123 & sleep 4; curl -s -D - http://localhost:3123/api/health; kill %1)` | `HTTP/1.1 200`, `cache-control: no-store`, body `{"ok":true,"service":"template","db":"skipped","time":"..."}` | confirmed 2026-09-18 (on the live throwaway `https://template-smoke.vercel.app/api/health`, since removed) |
| Throwaway removed | `npx vercel@latest project ls --scope kks-projects-2edcb11a \| grep -c template-smoke; curl -s -o /dev/null -w '%{http_code}\n' https://template-smoke.vercel.app/api/health; gh repo view KalpKan/template-smoke --json isArchived --jq .isArchived` | `0`, `404`, `true` (archived; deletion waits on Kalp granting `delete_repo`, STATUS.md H13) | confirmed 2026-09-18 |
| End-to-end spin-up time (re-run when the template changes) | the README "Verified" table: `gh repo create KalpKan/template-smoke --template KalpKan/portfolio-template --public --clone`, `npm ci`, `npm test`, `npm run build`, `npx vercel@latest deploy --prod --yes --scope kks-projects-2edcb11a`, `curl .../api/health`, then `vercel project rm` + archive/delete the repo | under 15 min; 10 min 05 s on 2026-09-18 (build 16 s, the rest Hobby build queue) | confirmed 2026-09-18 |

### UptimeRobot (monitors created 2026-09-18, T0.3; inventory in `docs/monitors.md`)

Load the key first: `set -a; source ~/.config/portfolio-ops/secrets.env; set +a`. Free plan rate limit is 10 requests per minute, so do not loop these.

| Check | Command | Expect | Status |
|---|---|---|---|
| Monitors exist and are up | `curl -s -X POST https://api.uptimerobot.com/v2/getMonitors -d "api_key=$UPTIMEROBOT_API_KEY&format=json&monitors=804030255-804030256-804030271-804030499-804031032-804031239-804031518-804031519-804031520&alert_contacts=1" \| jq '.monitors[] \| {id, friendly_name, url, status, alert_contacts: [.alert_contacts[].id]}'` | exactly `promptflip health (DB)` (804030255, `https://promptflip.kalpkan.com/api/health`), `hoops health (DB, Project B)` (804030256, `https://hoops.kalpkan.com/api/health`; renamed 2026-09-19), `hub health` (804030271, `https://kalpkan.com/api/health`), `platform health (DB, Project B)` (804030499), `plantit health (Firestore)` (804031032), `plato health (DB, Neon)` (804031239, `https://plato.kalpkan.com/api/health`), `pushups health` (804031518, `/health.json`), `emotes health` (804031519, `/health.json`), `microtubules health` (804031520, `/health.json`), each `status: 2` and `alert_contacts: ["5612875"]` | confirmed 2026-09-18 (T0.3); hoops + hub moved to `kalpkan.com` and plato added 2026-09-18 22:40Z (Phase 1 audit); the three static-demo monitors added 2026-09-19 00:20 UTC (Phase 2–4 audit), all nine `status: 2` |
| Every platform monitor, whatever its id | `curl -s -X POST https://api.uptimerobot.com/v2/getMonitors -d "api_key=$UPTIMEROBOT_API_KEY&format=json" \| jq '[.monitors[] \| select(.status != 0) \| {friendly_name, status}]'` | every non-paused monitor `status: 2`; the count matches the rows in `docs/monitors.md` | confirmed 2026-09-18 (T0.3) |
| Alert contact exists and is active | `curl -s -H "Authorization: Bearer $UPTIMEROBOT_API_KEY" https://api.uptimerobot.com/v3/user/alert-contacts \| jq '.[] \| {id, type, status}'` | `{"id":5612875,"type":"Email","status":"Active"}` | confirmed 2026-09-18 (T0.3) |
| Public status page | `curl -sI https://stats.uptimerobot.com/a6n3Wx3PBp \| head -1`, then open it in a browser | `HTTP/2 200`; page titled "Kalp's projects" lists the nine monitors in `docs/monitors.md`, all green | confirmed 2026-09-18 (T0.3); nine monitors listed since 2026-09-19. No `status.<domain>` (paid feature) |
| Status page still contains every monitor | `curl -s -X POST https://api.uptimerobot.com/v2/getPSPs -d "api_key=$UPTIMEROBOT_API_KEY&format=json" \| jq '.psps[0] \| {friendly_name, monitors, standard_url}'` | `monitors` lists every id in `docs/monitors.md`; `standard_url` is `https://stats.uptimerobot.com/a6n3Wx3PBp` | confirmed 2026-09-18 (T0.3) |
| Monitor URL is not a protected alias | `curl -sI <monitor url> \| head -1` for each URL in `docs/monitors.md` | `HTTP/2 200`, never a `302` to `vercel.com/sso-api` (that alias is behind Vercel deployment protection and the monitor would be measuring a login page) | confirmed 2026-09-18 (T0.3) |

### PostHog (T0.5, hub; every later app repeats rows 2 to 4 with its own host)

Load the operator key first: `set -a; source ~/.config/portfolio-ops/secrets.env; set +a`. Ingestion lag is 3 to 5 minutes; poll before concluding an event is missing. posthog-js only captures `$pageview` once the tab is visible, so an automation tab hidden behind others sends no pageview (incident 2026-09-18).

| Check | Command | Expect | Status |
|---|---|---|---|
| Project is the shared one, replay + heatmaps on, inputs masked | `curl -s -H "Authorization: Bearer $POSTHOG_PERSONAL_API_KEY" https://us.posthog.com/api/projects/616829/ \| jq '{name, session_recording_opt_in, heatmaps_opt_in, autocapture_opt_out, session_recording_masking_config}'` | `"Kalp portfolio"`, `true`, `true`, `false`, `{"maskAllInputs": true}` | confirmed 2026-09-18 (T0.5) |
| Reverse proxy works on the hub | `curl -sI https://kalpkan.com/ingest/static/array.js \| head -1` | `HTTP/2 200` (proxied to PostHog; an ad blocker cannot see the third-party host) | confirmed 2026-09-18 (T0.5) |
| Requests are first-party and cookieless | in the browser on `https://kalpkan.com`, run `performance.getEntriesByType('resource').filter(e=>e.name.includes('/ingest/')).map(e=>new URL(e.name).origin)` and `document.cookie` | every origin is `https://kalpkan.com`; cookie string is empty, no `ph_*` localStorage key | confirmed 2026-09-18 (T0.5) |
| Pageview and custom event arrive with the right host | visit the hub in a browser, click one project row, then `curl -s -H "Authorization: Bearer $POSTHOG_PERSONAL_API_KEY" "https://us.posthog.com/api/projects/616829/events/?event=project_card_clicked&limit=3" \| jq -c '.results[] \| {event, host: .properties["$host"], slug: .properties.slug}'` and the same with `event=%24pageview` | one row each with `host: "kalpkan.com"` (the click row also carries `slug`) | confirmed 2026-09-18 (T0.5), excerpt in `docs/analytics.md` |
| Dashboard exists with the three insights | `curl -s -H "Authorization: Bearer $POSTHOG_PERSONAL_API_KEY" https://us.posthog.com/api/projects/616829/dashboards/2112106/ \| jq '[.tiles[].insight.name]'` | `["Visitors by site","Visitors by country","Top demos by usage"]` | confirmed 2026-09-18 (T0.5) |
| Still free, no card | `curl -s -H "Authorization: Bearer $POSTHOG_PERSONAL_API_KEY" https://us.posthog.com/api/billing/ \| jq '{has_active_subscription, stripe_customer_id, limits: [.products[] \| {type, custom_limit_usd, free_allocation}]}'` | `false`, `null`, every `custom_limit_usd` null with a non-zero `free_allocation` (the free plan hard-caps there; see runbook "Check PostHog billing") | confirmed 2026-09-18 (T0.5); evidence `docs/images/posthog-billing-limits.png` |
| Hub unit tests cover the contract | `cd ~/projects/portfolio && npx vitest run lib/posthog.test.ts` | 7 passed (cookieless, `/ingest`, autocapture, masking, rewrites, no-op without key, sendBeacon capture) | confirmed 2026-09-18 (T0.5) |

### microtubules (`KalpKan/Microtubule-Quantification` `web/`, Vercel project `microtubules`, https://microtubules.kalpkan.com)

| Check | Command | Expect | Status |
|---|---|---|---|
| Health file | `curl -sf https://microtubules.kalpkan.com/health.json` | `{"ok":true,"service":"microtubules"}` | confirmed 2026-09-18 |
| Page serves | `curl -sI https://microtubules.kalpkan.com \| head -1; curl -sI https://microtubules.kalpkan.com \| grep -i '^server:'` | `HTTP/2 200`, `server: Vercel` | confirmed 2026-09-18 (cert `cert_fmPCbqPPTF39A1OVZLFfn0sV`, 90 d, renew yes) |
| DNS | `dig +short microtubules.kalpkan.com \| head -1` | `bb0edf923bf938a4.vercel-dns-017.com.` | confirmed 2026-09-18 |
| OpenCV served from the site | `curl -sI https://microtubules.kalpkan.com/opencv.js \| grep -iE '^(HTTP\|cache-control\|content-length)'` | `HTTP/2 200`, `cache-control: public, max-age=31536000, immutable`, `content-length: 10964323` | confirmed 2026-09-18 |
| Accuracy vs Python | `cd ~/projects/microtubules/web && npx vitest run --pool=forks --maxWorkers=1 --reporter=verbose tests/pipeline.test.ts` | 4 tests pass; the printed table shows `diff 0.0000` for P1_W1_C1 (24.8321 %), P1_W3_C1 (21.1827 %), P3_W2_C3 (34.7120 %) | confirmed 2026-09-18; ✅ FIX r1 2026-09-19 (the pipeline now splits channels in JS and frees each Mat early; numbers unchanged) |
| Python reference still reproduces the paper | `cd ~/projects/microtubules && .venv/bin/python web/scripts/reference.py` (venv: `python3 -m venv .venv && .venv/bin/pip install opencv-python-headless numpy pandas matplotlib`) | three lines ending `matches Results CSV` | confirmed 2026-09-18 |
| `run_analysis.py` flags | `cd ~/projects/microtubules && .venv/bin/python -m unittest tests/test_run_analysis.py -v` | `Ran 3 tests`, `OK` | confirmed 2026-09-18 |
| Build clean | `cd ~/projects/microtubules/web && npm ci && npx tsc && npm run build` | `✓ built`; `dist/` contains `opencv.js`, `samples/`, `health.json` and `assets/worker-*.js` (a classic/iife worker: its first bytes are `(function(){`, never `import`) | confirmed 2026-09-18; ✅ FIX r1 2026-09-19 (worker present, `vite.config.ts` `worker.format: "iife"`) |
| No server calls after load | in Chrome open the site, click a sample, upload a file, read the Network tab (or `docs/reports/evidence/microtubules-fix1-net.cjs`) | at load: the page assets, `/assets/worker-*.js`, `/opencv.js` (fetched by the worker), the PostHog `/ingest/*` scripts and the three `/samples/*.png` prefetches; during a sample or an upload **no request at all** except the PostHog beacon; `curl -s -o /dev/null -w '%{http_code}' -X POST -H 'Content-Type: application/json' -d '{}' https://microtubules.kalpkan.com/ingest/e/` → `400` (reached PostHog; `404` = rewrite broken) | confirmed 2026-09-18 (Chrome, live host); ✅ FIX r1 2026-09-19 (live, Playwright Chromium: `sample: []`, `upload: []`) |
| Custom events arrive | `curl -s "$POSTHOG_HOST/api/projects/616829/events/?event=image_analyzed&limit=1" -H "Authorization: Bearer $POSTHOG_PERSONAL_API_KEY" \| jq -c '.results[] \| {event, host: .properties["$host"], percent: .properties.percent}'`; same with `event=sample_loaded` | rows with `host: microtubules.kalpkan.com`; `image_analyzed` carries `percent`, `width`, `height`, `source` and never image data | confirmed 2026-09-18 (`sample_loaded {sample: P1_W1_C1}`, `image_analyzed {percent: 24.83, width: 77, height: 58, source: sample}`) |
| Lighthouse performance | `npx lighthouse https://microtubules.kalpkan.com --only-categories=performance --quiet --chrome-flags="--headless" --output=json \| jq .categories.performance.score` | `>= 0.9` **on each of three consecutive runs** (one run is not evidence: the 11 MB `opencv.js` parse makes the score swing) | ✅ 0.98 on 2026-09-18 (one run); ❌ 2026-09-18 TEST r1: 0.73 / 0.97 / 0.61 (LCP 18.7 / 1.5 / 18.8 s), `docs/reports/microtubules.md` D7; ✅ TEST r2 2026-09-19: **1.00 / 1.00 / 1.00** (LCP 0.8–1.4 s, TBT 0–30 ms, CLS 0.021; a11y and best-practices 1.00 too; `evidence/microtubules-r2-lighthouse-runs.txt`) |
| Env names present | `cd ~/projects/microtubules && npx vercel env ls --scope kks-projects-2edcb11a` | `VITE_PUBLIC_POSTHOG_KEY` (Production, Preview), `VITE_PUBLIC_POSTHOG_HOST` (Production) | confirmed 2026-09-18 |
| Corpus accuracy vs Python (60 labelled images) + the byte-level, warning and export tests | `cd ~/projects/microtubules/web && npx vitest run --pool=forks --maxWorkers=1` | **93 tests pass** (`pipeline` 4, `corpus` 53 with `diff 0.0000` on all 52 PNGs, `decode` 20: iCCP/gAMA/cHRM/ICC stripping keeps every pixel, format sniffing, header dimensions; `interpret` 12: warnings on the degenerate fixtures and none on the 33 non-zero cells; `export` 4: overlay/mask RGBA equal `Results/*.png`) | ✅ 2026-09-18 (SPEC agent): 57/57; ✅ FIX r1 2026-09-19: 93/93 |
| Python ground truth unchanged | `cd ~/projects/microtubules && .venv/bin/python tests/fixtures/make_fixtures.py --check` | last line `all match` (every fixture's percent and sha256 equal `ground_truth.json`) | ✅ 2026-09-18 |
| Browser corpus in Chromium **and WebKit** (JPEG, WebP, BMP, EXIF, ICC-tagged PNGs, wrong files, huge files, downloads, phone fold, banded decode) | `cd ~/projects/microtubules/web && npm run build && npm run test:browser` (Playwright is a devDependency; `npx playwright install chromium webkit` once; `BROWSERS=webkit`, `ONLY=corpus,files,huge,downloads,phone,bands`, `OUT=<json>` and `BASE=https://microtubules.kalpkan.com/` narrow or redirect it) | last line `PASS: browser checks in chromium + webkit`; per engine `corpus summary: 57 images, ... lossless threshold mismatches 0, pixel mismatches 0` (Chromium max \|diff\| ≤ 0.005, WebKit ≤ 0.29 on JPEGs only); every non-image row `hidden=true` with the file name, "could not be read as an image", "PNG, JPEG, WebP, BMP and GIF" and "TIFF"; 12/24 MP rows `max main-thread gap` < 1000 ms, canvases ≤ 2 MP, wall ≤ 15 s; six downloads `0 px differ from Results/`; phone rows `results top 0, visible=true` at 360/390/430 | ✅ FIX r1 2026-09-19 local build and live (`docs/reports/evidence/microtubules-fix1-browser-local.txt`, `…-browser-live.json`): Chromium 57 images max 0.0050, WebKit 57 images max 0.2864, 0 mismatches in both; 24 MP 1.25–1.4 s with the main thread never blocked > 49 ms, worker wasm heap 128 MB; downloads 0 px in both engines |
| Huge image stays responsive | `ONLY=huge npm run test:browser` (needs `edge/generated-large/huge-24mp-6000x4000.jpg`; regenerate with `.venv/bin/python tests/fixtures/make_fixtures.py`) | correct number (23.6721 / 24.7754 / 23.9292 %), `max main-thread gap` < 1000 ms, display canvases ≤ 2 MP, main-thread heap < 300 MB, staged status text, and the page states the 30 MP limit (`#mp-limit`) | ❌ 2026-09-18: 24 MP froze the tab 15.4 s, 475 MB; ❌ TEST r1: 5.5 s / 453 MB / 6000 × 4000 canvases; ✅ FIX r1 2026-09-19: 24 MP wall 1.25 s (Chromium) / 1.43 s (WebKit), longest main-thread gap 19–49 ms, main-thread heap 6 MB, canvases 1732 × 1154, worker wasm heap 128 MB (was 512 MB before the early-free refactor) |
| Phone width | `ONLY=phone npm run test:browser` (Playwright iPhone-13 UA at 360/390/430 px; 390 uses the 664 px Safari viewport) | no horizontal overflow, visible buttons ≥ 44 px, and after tapping each sample `results top ≤ 40` and the percent `visible=true` (spec S2/S8) | layout ✅ 2026-09-18; result visible ❌ TEST r1 (y = 600–665 at 390 × 844, nothing scrolls); ✅ FIX r1 2026-09-19: `results top 0`, percent at y = 19–84 of 664 in Chromium and WebKit at all three widths (`scrollIntoView` + focus after every run; samples on one scrolling row) |
| Safari engine gives the Python number (colour management) | `BROWSERS=webkit ONLY=corpus npm run test:browser`, especially `tests/fixtures/fullfield/*.png` (Mac-exported PNGs with an `iCCP kCGColorSpaceGenericRGB` chunk) | every lossless row diff ≤ 0.005 with identical threshold and pixel counts (spec S3 "holds in Safari") | ❌ TEST r1 2026-09-18: 13.47 / 3.85 / 4.77 % (thresholds 30/45/34); ✅ FIX r1 2026-09-19: 13.14 / 2.88 / 3.91 % with thresholds 24/42/30 on the local build **and on the live site** (`src/decode.ts` strips iCCP/gAMA/cHRM/sRGB/cICP and JPEG APP2 ICC before `createImageBitmap`) |
| Overlay/mask downloads match Python's Results files; Copy result | `ONLY=downloads npm run test:browser` (clicks Download overlay / Download mask for the three samples, decodes the PNGs with pngjs, diffs against `Results/<name>_overlay.png` / `_mask.png`; Chromium also reads the clipboard) | `0 px differ` for all six files, names `<name>_overlay.png` / `<name>_mask.png`; clipboard `P1_W1_C1: 24.83% (threshold 36, 1109/4466 px)` | ✅ TEST r1 (canvas only, buttons absent, D5); ✅ FIX r1 2026-09-19: six downloads 0 px in Chromium and WebKit, local and live; copy line exact |
| Works offline after load | Playwright Chromium `context.setOffline(true)` after "Ready" (or DevTools Offline), then each sample and an upload (`docs/reports/evidence/microtubules-fix1-offline.cjs`) | all three samples analyse (they are prefetched as bytes once OpenCV is ready) and the upload analyses, with zero requests while offline | ❌ TEST r1 (un-tapped sample: `Failed to fetch`, D8); ✅ FIX r1 2026-09-19 in Chromium (24.83 / 34.71 / 21.18 / 29.04, `requests while offline: []`). **Not provable in Playwright WebKit:** its offline emulation fails every in-memory blob load (`createImageBitmap` of a local File reports "The I/O read operation failed"), which is the emulation, not Safari (incidents.md) |
| Functional smoke test (spec S1–S10, TEST rounds) | `cd ~/projects/microtubules/web && npm run build && npm run test:browser`, then the same with `BASE=https://microtubules.kalpkan.com/` for the live site; plus the Lighthouse ×3 row, the offline row and `docs/reports/evidence/microtubules-fix1-net.cjs` (requests during analysis) | `PASS: browser checks in chromium + webkit` locally and live; Lighthouse ≥ 0.90 ×3; offline samples + upload in Chromium; no requests during analysis | TEST r1 2026-09-18: PARTIALLY WORKING, 0/10 at the bar; FIX r1 2026-09-19: all six majors and D7–D11 addressed, `PASS` in both engines locally and live (`docs/reports/microtubules.md` "FIX round 1"); ✅ **TEST r2 2026-09-19: WORKING, 10/10 at the bar** (`PASS` in both engines, live and local, 0 harness failures; real Chrome 151 on the live site; Lighthouse 1.00 ×3; PostHog payload exact; `evidence/microtubules-r2-*`), all eleven r1 defects confirmed fixed; seven new minors D12–D18 (Safari < 16.4 fallback path, misleading "damaged" text on I/O failures, warned number styled as valid, sample-vs-reference copy, phone order, copy nits, Slow-4G 19 s) |
| Old-Safari fallback path (no `OffscreenCanvas` in workers, Safari < 16.4) | `ONLY=fallback NODE_PATH=$PWD/node_modules node ~/projects/portfolio/docs/reports/evidence/microtubules-r2-extra.cjs` from `web/` with the local build served on `:8792` (the script prefixes the worker with `self.OffscreenCanvas = undefined` via `context.route`) | in WebKit the ICC-tagged `fullfield/*.png` rows give the Python numbers (2.88 / 42, 13.14 / 24), huge-file canvases ≤ 2 MP, a damaged file's message lists the formats | ❌ TEST r2 2026-09-19: WebKit 3.85 / 45 and 13.47 / 30 (colour metadata not stripped on the page path), 4000 × 3000 canvases, generic decode message (D12) |
| Ready time on throttled networks (spec S1 "within 10 s on 4G") | `ONLY=throttle … microtubules-r2-extra.cjs` (CDP `emulateNetworkConditions` 9 / 4 / 1.6 Mbps + 4× CPU, iPhone 13 profile, cold cache, live site) | `readyMs` ≤ 10 000 at 4 Mbps ("Fast 4G"); Slow 4G is a stretch goal until `opencv.js` is trimmed | ✅ TEST r2 2026-09-19: 4.5 s / 8.3 s / 19.0 s, ten progress-text updates during the download (D18 for Slow 4G) |
| Over-limit refusal, GIF, keyboard, busy state, camera input, CLS, contrast | `ONLY=limit,gif,themes,busy,camera,layout … microtubules-r2-extra.cjs` (needs a `FIX2` dir with 5600 × 5600 PNG/JPEG, a 6000 × 5000 JPEG and a GIF; make them with cv2/PIL) | 31.4 MP refused in < 100 ms with the limit in the text and the card hidden; GIF analysed; Tab order file → camera → samples → links with a visible outline, Space runs a sample; while busy inputs `disabled`, card `stale`, extra clicks ignored; `#camera-input` `capture="environment"` gives 23.98 % upright and scrolled into view at 390 px; CLS 0 at load and after a tap; every text ≥ 4.5:1 (large percent ≥ 3:1) in both themes | ✅ TEST r2 2026-09-19 (`evidence/microtubules-r2-extra.{txt,json}`, screenshots in `microtubules-r2-shots/`) |

### emotes (`KalpKan/emote-detector-web`, Vercel project `emotes`, https://emotes.kalpkan.com)

Written 2026-09-18 (T3.2). Static Vite site; everything below is unauthenticated.

| Check | Command | Expected |
|---|---|---|
| Page up | `curl -sI https://emotes.kalpkan.com \| head -1` and `curl -sI https://emotes.kalpkan.com \| grep -i ^server:` | `HTTP/2 200`, `server: Vercel` |
| Health | `curl -s https://emotes.kalpkan.com/health.json` | `{"ok":true,"service":"emotes"}` |
| Self-hosted ML files | `for f in wasm/vision_wasm_internal.wasm models/pose_landmarker_full.task demo/pushups.mp4; do curl -s -o /dev/null -w "%{http_code} %{size_download} $f\n" https://pushups.kalpkan.com/$f; done; curl -s -o /dev/null -w "%{http_code}\n" https://pushups.kalpkan.com/models/form-v3/model.json` | three `200` lines: wasm ≈ 11.2 MB, pose ≈ 9.4 MB (**full**, not lite), clip ≈ 265 KB; `cache-control: public, max-age=31536000, immutable` on `/models/*` and `/wasm/*`; the fourth line is `404` (no form classifier is shipped since FIX r3, 2026-09-19; `form-v3` was the last one) | confirmed 2026-09-18; classifier removed FIX r3 2026-09-19 |
| PostHog proxy reaches PostHog | `curl -s -o /dev/null -w '%{http_code}' -X POST -H 'Content-Type: application/json' -d '{}' https://emotes.kalpkan.com/ingest/e/` | `400` (PostHog rejecting the empty body; `404` means the rewrite is broken) |
| Unit tests | `cd ~/projects/pushups && npm test` | `172 passed \| 4 expected fail` since FIX r3 (2026-09-19): rep counter (first rep, 0.7 s reps at 10 fps, partial dip, start-at-bottom, kneeling rest vs knee pushup, spikes, majority verdict, bottom rules on the window mean, reason), form rules on real trace frames of both facings (`kneePlank`, "dropped to the floor" on `bad_IMG_4451`, chest-up sag on `bad_IMG_4470`, no bottom fault on clean bottoms of three bodies), placement hints (second-person rule, collapsed-head rule on real cut-off and edge-touching heads, hysteresis, `pausesCounting`), features, and the corpus gate on three trace sets (python, browser, browser-mirrored); the 4 expected failures are the `it.fails` known misses (`bad_IMG_4456` ×3 sets, `bad_IMG_4451` on the Python set) and go red the day they pass | FIX r3 2026-09-19 |
| Consumer-grade detection gate (spec S2–S6) | `cd ~/projects/emotes && npm run test:corpus` then `npm run report` | `tests/stills.test.ts` + `tests/clips.test.ts` all green: per-gesture recall ≥ 90 % / precision ≥ 90 % on the 42 clear photos, 0/9 neutral photos scoring, ≤ 3/33 hard negatives; all 43 positive clips fire once within 1000 ms, `neutral-60s` 0 firings, `hard-negatives-60s` ≤ 1, clip precision ≥ 95 % / recall ≥ 90 %. Red at the spec baseline 2026-09-18 (32 failing: flex precision 35 %, thumbs-up recall 41 %, yawn recall 56 %; 36/58 clips; neutral 2/min, hard 12/min, `docs/reports/emotes-spec.md` §6). **Green since FIX r1 (`4e25a95`, 2026-09-19): `96 passed`; `npm run report` = stills 100 % precision / recall for all three, 0/9 neutral, 0/33 hard; clips 70/70 (the gate now has 37 clear photos and 70 clips: two labels re-checked against their photos and changed, twelve 10 s hold clips added), emote precision / recall 100 %, neutral and hard minutes 0 firings, latency median 160 / p95 450 / max 960 ms.** CI run on `4e25a95`: `unit` and `corpus` both green. **FIX r2 (`ea1e718`, 2026-09-19): `110 passed` = stills + clips + `tests/video.test.ts` (nine VIDEO-mode reels, see the D1 row); `npm run report` = stills unchanged, clips 70/70, latency median 240 / p95 500 / max 700 ms (the 0.2 s smoothing), reels 9/9 with precision / recall 100 % and the two hard reels 0 firings in 53 s; CI on `ea1e718` green.** **FIX r3 (`d80120d`, 2026-09-19): `114 passed` (eleven reels: `flex09x3` now requires Goblin Muscle ×3 and never Thumbs Up, plus `tu04x4-30fps` and `flex09x3-30fps`); `npm run report` = stills unchanged, clips 70/70 with latency median 200 / p95 520 / max 700 ms, reels 11/11, precision / recall 100 %, hard reels 0; CI on `51f74f8` and `d80120d` green** |
| Functional smoke: real pipeline with a fake camera (spec S2–S4, S8) | `cd ~/projects/emotes && python3 scripts/build_e2e_clip.py && npm run build && (npx vite preview --port 4173 --strictPort &) && GPU=1 npm run e2e -- http://localhost:4173/; WIDTH=390 GPU=1 npm run e2e -- http://localhost:4173/; pkill -f "vite preview --port 4173"` (or `GPU=1 npm run e2e -- https://emotes.kalpkan.com`) | `PASS: every gesture fired once within 1 s, nothing else fired` at both widths; the line before it lists `Thumbs Up@~2100 Goblin Muscle@~6600 Princess Yawn@~11100` (clip positions; onsets 2000/6500/11000). Without `GPU=1` headless Chrome renders in software at a few fps and latencies of 1.4–1.7 s are expected. Needs the read-only photo folder on this Mac to build the git-ignored `.mjpeg`; baseline 2026-09-18 (GPU): 131 / 183 / 140 ms, PASS. FIX r1 local build (GPU, 2026-09-19): `Thumbs Up@2335 Goblin Muscle@6849 Princess Yawn@11565` PASS at 1000 and `2239 / 6893 / 11574` PASS at 390 (the time-based dwell adds ~100–300 ms: 150 ms for a thumbs-up / flex, 400 ms for a yawn). Since FIX r1 the harness ignores MediaPipe's `INFO:` line but FAILS on any other console error (D8). **Live on `ea1e718` (FIX r2, 2026-09-19 21:55 UTC): `Thumbs Up@2256 Goblin Muscle@6827 Princess Yawn@11597` PASS at 1000 and `2232 / 6861 / 11672` PASS at 390.** Since FIX r2 an event already in progress when the models become ready is timed from that moment (D7). **Live on `d80120d` (FIX r3, 2026-09-20 ~00:45 UTC): `Thumbs Up@2299 Goblin Muscle@6885 Princess Yawn@11597` PASS at 1000 (the `@2989` before it is the mid-event pass-0 fire the judge drops) and `2277 / 6830 / 11573` PASS at 390; `fast` and `hard` PASS at both widths** |
| Held gesture fires once; phone frame rates (spec S6/S8, TEST r1 D4) | `cd ~/projects/emotes && npm run test:corpus` (the twelve `hold-*` clips in `tests/fixtures/clips/clips.json`: thumbs_up-07, flex-14, yawn-12 held 10 s at 25 / 12 / 8 fps with the corpus jitter, plus yawn-19 at twice the jitter) and `npm run report -- --verbose \| grep hold-` | each `hold-*` clip `PASS` with exactly one fire ≤ 1000 ms after onset. FAIL 2026-09-19 (TEST r1, frame-counted FSM): yawn-12 fired 5× at 25 fps, yawn-19 4–5× at σ 0.008 (`docs/reports/evidence/emotes-r1-hold-flicker-2026-09-19.txt`; that script predates the engine's clock argument). **PASS since FIX r1 (`4e25a95`): 12/12, latest fire 867 ms (yawn-19, 12 fps, σ 0.008)** |
| Functional smoke: hard negatives, known misses and repeats through the real pipeline (spec S2–S6, TEST r1) | `S=/tmp/emotes-e2e; mkdir -p $S; for n in hard misses repeat; do /usr/bin/python3 ~/projects/portfolio/docs/reports/evidence/emotes-r1-build-extra-e2e-clips.py $S $n; done; cd ~/projects/emotes; for n in hard misses repeat; do GPU=1 CLIP=$S/e2e-$n.mjpeg LABELS=$S/e2e-$n-labels.json npm run e2e -- https://emotes.kalpkan.com/; done` (clips built from the read-only photo folder, never committed) | `hard`: `fires: none`; `misses`: five events each once ≤ 1000 ms; `repeat`: three Thumbs Up, `PASS` (since FIX r1 the judge drops a second fire for an already-matched event when it belongs to the clip's next loop pass, D9). FAIL 2026-09-19 (TEST r1): hard fired 5 emotes in 24.5 s, misses fired 0 of 5 (+ 1 false flex) (`docs/reports/evidence/emotes-r1-e2e-live-2026-09-19.txt`). **PASS on the FIX r1 local build (GPU, 2026-09-19): hard `none`; misses `Thumbs Up@2214 Thumbs Up@6696 Princess Yawn@11429 Princess Yawn@15971 Thumbs Up@20230`; repeat three Thumbs Up.** The clip builder now lives in the repo: `python3 scripts/build_e2e_clips.py $S hard misses repeat` (or `all`). FIX r2 local build: all three PASS; live `hard` `none` |
| Cue hints and copy (spec S7, TEST r1 D5) | Headless real Chrome with the fake camera on the round-1 `misses` clip at 390 px: read `#status` and the three `.hint` spans every 200 ms for 16 s (the TEST r1 `ux` script or `scripts/e2e-camera.mjs` adapted; the FIX r1 run is `docs/reports/evidence/emotes-fix1-ux-390-2026-09-19.txt`) | while a gesture is almost there its meter row gets class `almost` and its hint reads `Almost: <what to change>` (e.g. `Almost: Fold the other four fingers into a fist.`), `#status` reads `Almost a Thumbs Up: …`; no "frame" in the body copy except "frames per second"; `#mute` toggles `aria-pressed`. PASS 2026-09-19 (FIX r1 local build) |
| Stage takes the stream's shape (spec S8, TEST r1 D6) | same harness with a square or portrait fake stream (Chrome's fake device crop-and-scales to 640 × 480 unless the constraint is met; the round-1 `portrait.mjpeg` gives 480 × 480): `getComputedStyle(#stage).aspectRatio` after "Watching…" | equals `<videoWidth> / <videoHeight>` (`480 / 480` for the square stream, `640 / 480` in demo mode, `4 / 3` after Stop); video and canvas `object-fit: contain`, nothing cropped. PASS 2026-09-19 (FIX r1 local build, screenshot `emotes-fix1-ux-390-portrait-2026-09-19.jpg`) |
| Thumbs-up beside the head is a Thumbs Up, not a Goblin Muscle (spec S2, TEST r2 D1) | `S=/tmp/emotes-e2e; mkdir -p $S; /usr/bin/python3 ~/projects/portfolio/docs/reports/evidence/emotes-r2-build-e2e-clips.py $S tu04x4; cd ~/projects/emotes; for i in 1 2 3; do GPU=1 CLIP=$S/e2e-tu04x4.mjpeg LABELS=$S/e2e-tu04x4-labels.json npm run e2e -- http://localhost:4173/; done` (`thumbs_up-04`, thumb beside the cheek with the elbow bent, four times per clip; the same `.py` builds `tu17x4`, `fast`, `hard2`, `far`, `mirror`, `sweep`, `hold-*`) | every run `PASS` with four `Thumbs Up` fires and no `Goblin Muscle` | TEST r2 2026-09-19 on the `4e25a95` production build served locally: **8 Goblin Muscle / 5 Thumbs Up over 13 passes** (blocker; `docs/reports/emotes.md` D1). The per-frame cue trace behind it: `emotes-r2-video-landmarks.py` (VIDEO-mode landmarks with the site's `.task` models) + `emotes-r2-video-eval.ts`. **PASS since FIX r2 (`ea1e718`): 12/12 Thumbs Up, 0 Goblin Muscle over three local runs (`Thumbs Up@2607 7025 11537 16092`, `@2627 7049 11696 16065`, `@2265 7018 11561 16058`) and 4/4 live.** Offline twin, no Chrome needed: `npm run test:corpus` runs `tests/video.test.ts` over `tests/fixtures/video/tu04x4.json` (the same clip's VIDEO-mode landmarks); the repo's `scripts/build_e2e_clips.py $S tu04x4` replaces the evidence-folder builder |
| Back-to-back gestures each fire (spec S6, TEST r2 D2) | same builder, `fast` clip (thumbs-up 1.2 s → flex 1.2 s → yawn 1.5 s, no rest): `GPU=1 CLIP=$S/e2e-fast.mjpeg LABELS=$S/e2e-fast-labels.json npm run e2e -- http://localhost:4173/` at `WIDTH=1000` and `390` | `PASS`, three fires | TEST r2 2026-09-19: `missed flex (3200-4400 ms)` at both widths; offline (`emotes-r2-synth.ts`) every gap under 1000 ms drops the middle gesture (the 2 s `EmoteGate` swallows the engine's edge). **PASS since FIX r2: local `Thumbs Up@2162 Goblin Muscle@3425 Princess Yawn@5040` (1000) and `@2222 / 3473 / 5014` (390); live at 390 `@2269 / 3467 / 5002`.** Offline twin: the `fast` reel in `tests/video.test.ts` |
| Hint line holds still (spec S7, TEST r2 D3) | `node ~/projects/portfolio/docs/reports/evidence/emotes-r2-hint-recorder.mjs http://localhost:4173/ $S/e2e-misses.mjpeg` (every `#status` change with a timestamp) | no two `Almost a …` lines closer than 900 ms | TEST r2 2026-09-19: eight alternations in 640 ms on the thumbs_up-04 segment. **PASS since FIX r2: on the `misses` clip the line changed at 3279 (Almost a Goblin Muscle), 3724 (cleared by the Thumbs Up fire), 16797 (Almost a Princess Yawn), 17184 (cleared by the fire), 27820; never two hints inside 900 ms.** Offline twin: `tests/hints.test.ts` "hint hold" |
| Held-out landmark sets (spec S2–S5 generalisation, TEST r2 D6) | `/usr/bin/python3 ~/projects/portfolio/docs/reports/evidence/emotes-r2-make-variants.py /tmp/emotes-variants; for v in mirror portrait far; do mkdir -p /tmp/emotes-stills-$v && cp tests/fixtures/stills/labels.json /tmp/emotes-stills-$v/ && /usr/bin/python3 scripts/extract_still_landmarks.py --src /tmp/emotes-variants/$v --out /tmp/emotes-stills-$v; done; cp ~/projects/portfolio/docs/reports/evidence/emotes-r2-variant-eval.ts .variant-eval.ts && npx vite-node .variant-eval.ts /tmp/emotes-stills-mirror /tmp/emotes-stills-portrait /tmp/emotes-stills-far; rm .variant-eval.ts` (the 95 photos mirrored, letterboxed into 480 × 640 at 78 %, and shrunk to 60 %, re-extracted with the site's own models; never committed) | precision 100 % ×3 on every set, 0/9 neutral and 0/33 hard fired, 0 occluded/partial wrong; recall ≥ 90 % is the target the corpus alone cannot prove | TEST r2 2026-09-19: precision 100 % everywhere, 0 false fires on 132 negative sets; recall flex 13/13, 10/13, 10/13, thumbs-up 16/16 ×3, **yawn 5/8, 6/8, 5/8** (the eyes cue reads a small or flipped face as open). TEST r3 2026-09-19 re-extracted on `10c2bae`: identical numbers (rules untouched by FIX r2; `emotes-r3-variants-stills-2026-09-19.txt`) |
| Flex whose other hand reads as a thumbs-up plays Goblin Muscle (spec S3, TEST r3 D1) | `S=/tmp/emotes-e2e; mkdir -p $S; cd ~/projects/emotes; /usr/bin/python3 scripts/build_e2e_clips.py $S flex09x3; for W in 1000 390; do for i in 1 2 3; do GPU=1 WIDTH=$W CLIP=$S/e2e-flex09x3.mjpeg LABELS=$S/e2e-flex09x3-labels.json npm run e2e -- https://emotes.kalpkan.com/; done; done` | every run `PASS` and the `fires` line reads `Goblin Muscle` three times, `Thumbs Up` never (since FIX r3 the verdict is honest: the per-event `accept` list is gone from the builder, the judge and the harness). To read a wrong emote frame by frame: `GPU=1 TRACE=/tmp/t.json CLIP=… LABELS=… npm run e2e -- "https://emotes.kalpkan.com/?trace"` (per-frame raw scores, cues, pose gap, fused scores, active/fired, clip position) | TEST r3 2026-09-19 live: `Thumbs Up@2245 Thumbs Up@6751 Thumbs Up@11510` at 1000 and `Goblin Muscle@2812 Thumbs Up@7043 Thumbs Up@11197` at 390, both printed PASS (`docs/reports/emotes.md` D1; cue trace `emotes-r3-flex09-video-cues-2026-09-19.txt`). **PASS since FIX r3 (`d80120d`, live 2026-09-20 ~00:45 UTC, bundle `index-Cx23TXpj.js`): live `Goblin Muscle@2317/6968/11433`, `@2350/6969/11348`, `@2946/6998/11381` at 1000 and `@2819/7008/11333`, `@2401/6970/11427`, `@2578/6898/11476` at 390, 18/18, Thumbs Up 0; local production build 54/54 over 18 runs (one flex late at 1,036 ms).** Offline twins: `flex09x3` (10 fps) and `flex09x3-30fps` in `tests/video.test.ts` |
| A thumbs-up beside a jittery half-flex stays a Thumbs Up at the full frame rate (spec S2, TEST r2 D1 guard for FIX r3) | `cd ~/projects/emotes && npx vitest run --pool=forks --maxWorkers=1 tests/video.test.ts -t "30 fps"` (the `tu04x4-30fps` and `flex09x3-30fps` fixtures: the same reels sampled at 30 fps instead of 10) and, live, `GPU=1 CLIP=$S/e2e-tu04x4.mjpeg LABELS=$S/e2e-tu04x4-labels.json npm run e2e -- https://emotes.kalpkan.com/` at `WIDTH=1000` and `390` | both 30 fps tests pass; live `tu04x4` four `Thumbs Up`, no `Goblin Muscle`, each ≤ 1000 ms | FIX r3 2026-09-19: a rejected variant of the fix (restarting the flex average on a raw step) fired Goblin Muscle 7 of 8 passes on the real pipeline while the 10 fps gate stayed green; on the 30 fps fixture it fires flex ×4, so the gate now catches it. **Live on `d80120d`: `Thumbs Up@2627/7125/11610/16066` (1000) and `@3239/7120/11553/16146` (390), PASS; local 8/8 passes at 524–844 ms** |
| Round-3 reels through the real pipeline (spec S2–S6, S8; TEST r3) | `S=/tmp/emotes-e2e; /usr/bin/python3 ~/projects/portfolio/docs/reports/evidence/emotes-r3-build-e2e-clips.py $S; cd ~/projects/emotes && npm run build && (npx vite preview --port 4173 --strictPort &); H=~/projects/portfolio/docs/reports/evidence/emotes-r3-e2e-harness.mjs; for n in portrait motion motion-hard dim quick-thumb quick-flex quick-yawn neutral-mix flex-both thumbs-more yawn-more; do GPU=1 CLIP=$S/e2e-$n.mjpeg LABELS=$S/e2e-$n-labels.json node $H http://localhost:4173/; done; pkill -f "vite preview --port 4173"` (the harness also prints pipeline fps, stream / stage geometry and "Almost" swaps under 900 ms) | every positive reel `PASS` with `latencies` all ≤ 1000; `motion-hard` and `neutral-mix` `fires: none`; `portrait` reports `stream 480x480, stage aspect "480 / 480"` | TEST r3 2026-09-19 (local build of `10c2bae`): portrait, motion (both widths), motion-hard, dim (both widths), neutral-mix, thumbs-more 6/6 PASS; **flex-both `flex late: 1525 ms` (flex-12), yawn-more `missed yawn` (yawn-17), quick-thumb 1/3, quick-flex 2/3, quick-yawn 3/3** (`emotes-r3-e2e-local-2026-09-19.txt`) |
| Phone frame rates through the real pipeline (spec S8; TEST r3) | same harness with CPU throttling: `GPU=1 WIDTH=390 THROTTLE=6 node $H http://localhost:4173/` and with `CLIP=$S/e2e-fast.mjpeg`, `e2e-tu04x4.mjpeg`, `e2e-hard.mjpeg` | `pipeline: 10–14 fps` and `PASS` (every event ≤ 1000 ms; hard `none`) | TEST r3 2026-09-19: official 14.1 fps (`THROTTLE=4`) 96 / 375 / 629 ms and 12.8 fps (`THROTTLE=6`) 290 / 520 / 700 ms; fast 12.3 fps 3/3; tu04x4 10.3 fps 4/4 (529–676 ms); hard 11.7 fps none |
| Playwright Chromium instead of Google Chrome (TEST r3) | `GPU=1 BROWSER=playwright node $H http://localhost:4173/` (uses `~/projects/promptflip/node_modules/playwright`, read only) at `WIDTH=1000` and `390`, plus `fast` and `tu04x4` | `PASS` | TEST r3 2026-09-19: official 283 / 313 / 608 (1000) and 209 / 307 / 669 ms (390); fast 3/3; tu04x4 4/4 |
| Every round-1/2 reel on the deployed URL at both widths (TEST r3) | `bash ~/projects/portfolio/docs/reports/evidence/emotes-r3-run-live-e2e.sh` (34 runs: official, tu04x4 ×3, fast, hard, hard2, misses, repeat, mirror, sweep, far, three holds, tu17x4, flex09x3 at 1000 and 390; ~1 h because each fresh headless profile downloads the 39 MB of models) | 34 `PASS` | TEST r3 2026-09-19 on `10c2bae`: **33/34**, the one FAIL a `tu04x4` run at 390 that fired only its first two passes (D8; five other runs 4/4); Goblin Muscle 0 in 46 beside-the-head passes; hard + hard2 `none` at both widths (`emotes-r3-e2e-live-2026-09-19.txt`) |
| Every clear positive once through the live pipeline, with the rest frames varied (spec S2–S5 emote-level recall / precision; TEST r4) | `S=/tmp/emotes-e2e; mkdir -p $S; /usr/bin/python3 ~/projects/portfolio/docs/reports/evidence/emotes-r4-build-e2e-clips.py $S all-flex all-thumbs all-yawn all-hard occluded a10-flex a04-flex; R=~/projects/portfolio/docs/reports/evidence/emotes-r4-run-e2e.sh; S=$S bash $R https://emotes.kalpkan.com/ 1000 /tmp/emotes-live-1000.txt all-flex all-thumbs all-yawn all-hard occluded a10-flex a04-flex; S=$S bash $R https://emotes.kalpkan.com/ 390 /tmp/emotes-live-390.txt all-flex all-thumbs all-yawn all-hard a10-flex; grep -E '^(=== \|fires\|PASS\|FAIL)' /tmp/emotes-live-*.txt` (one Chrome at a time; `all-*` rest on angry-04 / angry-10 alternately, ~10 min per width) | `all-thumbs` 16/16, `all-yawn` ≥ 8/8 (bar 90 %: ≥ 8), `all-flex` ≥ 12/13 and `a10-flex` 4/4 at both widths, max latency ≤ 1000 ms; `all-hard` (33 photos, 100 s) `none`; `occluded` may play Princess Yawn on an occluded yawn and Thumbs Up on thumbs_up-05 but no *other* emote. **2026-09-20 live `d80120d`: thumbs 16/16 ×2, yawn 7/8 ×2 (yawn-17), flex 11/13 ×2 (flex-04 / flex-06 after angry-10), `a10-flex` 1/4 and 0/4, `a04-flex` 4/4 ×3, `all-hard` none ×2 (report D1, D5)** |
| A far flex plays Goblin Muscle or nothing, never Thumbs Up (spec S3; TEST r4 D2) | same builder, `flex09-far2` (flex-09 at 55 % ×4) and `flex09-far` (with a far thumbs_up-04): `for i in 1 2 3; do GPU=1 CLIP=$S/e2e-flex09-far2.mjpeg LABELS=$S/e2e-flex09-far2-labels.json node ~/projects/portfolio/docs/reports/evidence/emotes-r3-e2e-harness.mjs https://emotes.kalpkan.com/; done` | Thumbs Up 0 in every pass; Goblin Muscle ≥ 3/4 once fixed. **2026-09-20: Thumbs Up 3, Goblin Muscle 6, nothing 6 over 15 passes** |
| A fist that changes gesture plays both emotes; gestures with no rest each fire (spec S6; TEST r4 D3) | same builder, `takeover` (flex-09 → thumbs_up-04, thumbs_up-07 → flex-09, thumbs_up-04 → flex-14, 2.5 s each, no rest) and `morph` (seven gestures, 1.5 s each, no rest); `rest08-thumb`, `rest08-flex` (0.8 s rests ×3), `short-hold` (0.6–1.0 s holds) | `takeover` 6/6, `morph` 7/7 in order, `rest08-*` 3/3, `short-hold` 5/5 at 1000 and 390. **2026-09-20: `takeover` 4/6 ×2 (the second emote of a kept fist never plays), `morph` 7/7 ×4 incl. 12 fps and Chromium, `rest08-*` 3/3 ×4, `short-hold` 5/5 ×3** |
| Fold, phone rows, reduced motion, sound (spec S1, S8, S9; TEST r4 D8–D10) | `node ~/projects/portfolio/docs/reports/evidence/emotes-r4-ux-puppeteer.mjs https://emotes.kalpkan.com/ > /tmp/emotes-ux.json` then read `layout.startBelowFold`, `layout.buttonsRows`, `reducedMotionDemo.fires[].anim`, `reducedMotionDemo.plays`, `afterMute.plays` | `startBelowFold: false` at 1512 × 627 and 1440 × 800; `buttonsRows: 1` at 390; `anim` = `none` under `prefers-reduced-motion`; one `play()` per fire before Mute and none after; `scrollW` = viewport everywhere; no errors. **2026-09-20: Start camera at y 649 on a 627 px viewport (below the fold), 2 button rows at 360–430, `anim: "pop"` under reduced motion; sound and Mute correct** |
| Nothing leaves the device after "Watching…" (spec S10, TEST r1) | `cd ~/projects/emotes && node ~/projects/portfolio/docs/reports/evidence/emotes-r2-net-puppeteer.mjs https://emotes.kalpkan.com/ live` (round 1's script, parameterised by URL) (headless real Chrome, fake camera, live URL) | `hosts: ["emotes.kalpkan.com"]`, `requestsAfterWatching` only `/favicon.svg`, status sequence ends in "Watching…", `/mediapipe/*` immutable. PASS 2026-09-19 (TEST r1) |
| Demo fires without a camera | open the site in Chrome, press "Play demo", watch 8 s | Thumbs Up, then Goblin Muscle, then Princess Yawn pop up with sound; caption cycles "Thumbs up / Flex / Yawn". In a **hidden** tab timers run at 1 Hz, so the first emote takes ~30 s: bring the tab to the front first |
| No network after load | Chrome DevTools Network after pressing "Start camera" or "Play demo" | only `emotes.kalpkan.com` and `/ingest/*` requests |
| Analytics | PostHog events `session_started`, `emote_fired`, `demo_video_played` with `$host = emotes.kalpkan.com` (`GET /api/projects/616829/events/?event=emote_fired`, personal key in the header) | rows within ~5 min of a demo run |
| Lighthouse | `npx lighthouse https://emotes.kalpkan.com --only-categories=performance --quiet --chrome-flags="--headless" --output=json --output-path=/tmp/lh.json && jq .categories.performance.score /tmp/lh.json` | `>= 0.85` (the page paints before any model loads; models are fetched on click). TEST r3 2026-09-19 live mobile ×2: 0.99 / 1.00, accessibility 1.0, best-practices 1.0, LCP 1.4 s, CLS 0 |

Re-run 2026-09-19 00:15 UTC (Phase 2–4 audit): page `200 tls=0 server: Vercel`, health ok, `npm test` 33 passed, env names `VITE_PUBLIC_POSTHOG_KEY`/`_HOST` present, `emote_fired` ×50 and `demo_video_played` from `emotes.kalpkan.com` in PostHog; monitor `804031519` added.

Executed 2026-09-18 after the domain attached: page `HTTP/2 200` + `server: Vercel`; health `{"ok":true,"service":"emotes"}`; ML files `200` at 11756954 / 3758596 / 7819105 / 5777746 / 20114 bytes with `cache-control: public, max-age=31536000, immutable`; `/ingest/e/` → `400`; `npm test` → `33 passed`; `tsc` clean; `vite build` first-paint JS 17.97 kB (MediaPipe chunks 154 + 288 kB load on click); Lighthouse performance **0.91** (FCP 1.3 s, LCP 3.2 s, TBT 90 ms, CLS 0, 597 KiB transferred; run with `--chrome-flags='--headless --host-resolver-rules="MAP emotes.kalpkan.com 64.29.17.65"'` because the worker's resolver had negative-cached the name, see `incidents.md`).

### pushups (`KalpKan/pushup-tracker-web`, Vercel project `pushups`, https://pushups.kalpkan.com)

Written 2026-09-18 (T3.1). Static Vite site; everything below is unauthenticated. If curl says `Could not resolve host` right after the record was created, the Mac's resolver negative-cached the name: verify with `dig @1.1.1.1` and `curl --resolve pushups.kalpkan.com:443:216.198.79.65 ...` until it expires.

| Check | Command | Expected | Status |
|---|---|---|---|
| Page up | `curl -sI https://pushups.kalpkan.com \| head -1; curl -sI https://pushups.kalpkan.com \| grep -i ^server:` | `HTTP/2 200`, `server: Vercel` | confirmed 2026-09-18 (cert `cert_ynaCdK0uxmwOJZSWaPEkrxAc`, 90 d, renew yes) |
| Health | `curl -s https://pushups.kalpkan.com/health.json` | `{"ok":true,"service":"pushups"}` | confirmed 2026-09-18; re-run 2026-09-19 (Phase 2–4 audit: page `200 tls=0`, health ok, 9 vitest, env names present, `rep_counted` ×6 in PostHog, monitor `804031518` added) |
| DNS | `dig @1.1.1.1 +short pushups.kalpkan.com \| head -1` | `80c9fa355067b1a6.vercel-dns-017.com.` | confirmed 2026-09-18 |
| Self-hosted ML files | `for f in wasm/vision_wasm_internal.wasm models/pose_landmarker_full.task models/form-v3/model.json demo/pushups.mp4; do curl -s -o /dev/null -w "%{http_code} %{size_download} $f\n" https://pushups.kalpkan.com/$f; done` | four `200` lines: wasm ≈ 11.2 MB, pose ≈ 9.4 MB (**full**, not lite), model.json ≈ 13 KB (v3 ensemble; the directory name is `MODEL_URL` in `src/classifier.ts` and changes with every retrain because of the immutable cache), clip ≈ 265 KB; `cache-control: public, max-age=31536000, immutable` on `/models/*` and `/wasm/*` | confirmed 2026-09-18; path `form-v3` since FIX r2 (2026-09-19) |
| PostHog proxy reaches PostHog | `curl -s -o /dev/null -w '%{http_code}' -X POST -H 'Content-Type: application/json' -d '{}' https://pushups.kalpkan.com/ingest/e/` | `400` (`404` = rewrite broken) | confirmed 2026-09-18 |
| Unit tests | `cd ~/projects/pushups && npm test` | `139 passed`, 0 expected failures since FIX r2 (2026-09-19): rep counter (first rep, 0.7 s reps at 10 fps, partial dip, start-at-bottom, kneeling rest vs knee pushup, spikes, majority verdict, reason), form rules on real trace frames (incl. `kneePlank`), placement hints (second-person rule, head landmarks, hysteresis debouncer, `pausesCounting`), the 24 form features vs the Python trainer, classifier v3 within 1e-4 of Keras on 60 held-out frames, the versioned model URL + wrong-width refusal (`classifierUrl.test.ts`), and the corpus gate on both trace sets (no known misses left) | FIX r2 2026-09-19 |
| Build clean | `cd ~/projects/pushups && npm run build` | `✓ built`; `dist/` has `wasm/`, `models/pose_landmarker_full.task`, `models/form-v3/` (and **no** `models/form/`), `demo/`, `health.json`; entry JS ≈ 4 KB, `session` chunk ≈ 1 MB (lazy) | confirmed 2026-09-18 |
| Demo runs, no third-party hosts | `cd ~/projects/pushups && GPU=1 node scripts/e2e-demo.mjs https://pushups.kalpkan.com/` (headless Chrome on the Mac GPU) | `status` "Clip finished: N good of M…", `hosts` = `["pushups.kalpkan.com"]`, `errors` = `[]`, `fps` ≈ 30 (one analysis per decoded frame since FIX r1; "60 fps" means the rAF double-analysis is back); counts 4 attempts / 2–3 good (truth 4 / 2–3) | FIX r1 2026-09-19 live (`0dbdc48`): `{"good":"4","attempts":"4","fps":"30 fps","hosts":["pushups.kalpkan.com"],"errors":[]}` ×3 |
| Analytics | PostHog events `session_started {mode}`, `rep_counted {good}`, `demo_video_played` with `$host = pushups.kalpkan.com` (`GET /api/projects/616829/events/?event=rep_counted`, personal key in the header) | rows within ~5 min of a demo run (`node scripts/e2e-demo.mjs https://pushups.kalpkan.com/`; a plain puppeteer/Playwright run is dropped by posthog-js's `navigator.webdriver` bot filter) | confirmed 2026-09-18 22:28 UTC: `session_started {mode:"demo"}`, `demo_video_played`, `rep_counted {good:false}`, no landmark keys |
| Lighthouse | `npx lighthouse https://pushups.kalpkan.com --only-categories=performance --quiet --chrome-flags="--headless" --output=json --output-path=/tmp/lh-pushups.json && jq .categories.performance.score /tmp/lh-pushups.json` | `>= 0.85` (first paint is ~4 KB of JS; the 20 MB of WASM + models load on click) | confirmed 2026-09-18: **1.00** (FCP 0.9 s, LCP 0.9 s, TBT 80 ms, 176 KiB); run through a pre-launched Chrome (`--remote-debugging-port` + `--host-resolver-rules`, then `lighthouse --port`) while the local resolver still had the negative cache |
| Corpus gate (part of `npm test`) | `cd ~/projects/pushups && npx vitest run --pool=forks --maxWorkers=1 tests/corpus.test.ts --reporter=verbose --silent=false \| grep -E "MISS\|ok  \|bottom verdicts"` | three tables (`[python]`, `[browser]`, `[browser-mirrored]` landmark traces, 15 clips each): every row `ok` except the `it.fails` known misses (`bad_IMG_4456` 4/4 in all three, `bad_IMG_4451` 4/4 on python); "bottom verdicts" at or above the per-set ratchets **55/67, 58/67, 57/67** (FIX r3, 2026-09-19; the r2 "65/67" was in-sample, see incidents); `test_video_2` prints `9.6b(dropped to the floor) 16.6b(knees down)`, `bad_IMG_4470` four `hips sagging`, `bad_IMG_4451` four `dropped to the floor` on the browser set; attempts AND good identical at 30/20/15/10 fps on every clip except the three mirrored-set reps named in `FPS_TOLERANCE` (±1) | FIX r3 2026-09-19 |
| Functional smoke: live-camera count on the corpus | `cd ~/projects/pushups && node scripts/make-mjpeg.mjs` (once) then `GPU=1 node scripts/e2e-corpus.mjs https://pushups.kalpkan.com/` (or a `vite preview` on 4177) | every one of the 15 clips prints `PASS` (attempts within ±1 of `total`, good reps within `[good_min-1, good_max+1]` from `tests/fixtures/clips/ground_truth.json`), `fps avg` ≥ 15 and `min` ≥ 10; results JSON in `tests/fixtures/results/`. `GPU=1` is mandatory: SwiftShader runs the pose model at a few fps and the counts mean nothing. Run it when the load average is sane: with another agent's Chrome on the GPU and a load average > 100 one run read 13 fps and lost five reps of `IMG_1512` (2026-09-19) | baseline 2026-09-18: 4/15 (spec §6); TEST r1 live (`4f0708e`) 4/15, 4/15, 5/15. **FIX r1 2026-09-19 live (`0dbdc48`), 3 runs: 14/15, 14/15, 13/15; attempts within ±1 on 15/15 and identical across the runs; misses = `test_video` 0 good ×3 (known, classifier) and `bad_IMG_4470` 2 good once; fps avg 30 (one clip 25), min 16** (`docs/reports/evidence/pushups-fix1-corpus-live-run{1,2,3}-2026-09-19.log`; local production build ×3 in `…-localbuild-run{1,2,3}…`). **TEST r2 2026-09-19 live (`0dbdc48`), 3 runs: 14/15, 14/15, 14/15, every clip's attempts AND good identical across the runs; miss = `test_video` 0 good (known); fps avg 29.7 over 45 clip-runs, min 23, with another agent's Chrome on the GPU (`pushups-r2-corpus-live-run{1,2,3}-2026-09-19.{log,json}`)**; **FIX r2 2026-09-19 live (`634fdc4`), 3 runs: 15/15, 15/15, 15/15, every clip's attempts AND good identical across the runs and identical to the offline trace gate (demo 4/4, test_video 4/2, test_video_2 4/2 with the knee rep, the three bad clips 0 good), fps avg 29 min 18 (`docs/reports/evidence/pushups-fix2-corpus-live-run{1,2,3}-2026-09-19.log`)** |
| Functional smoke: demo count is stable | `cd ~/projects/pushups && for i in 1 2 3; do GPU=1 node scripts/e2e-demo.mjs https://pushups.kalpkan.com/ \| jq -c '{good,attempts,fps,errors}'; done` | three identical lines, `attempts` 3–5 and `good` 1–4 (truth for the 8.5 s clip: 4 attempts, 2–3 good), `fps` "–" (the Speed and Form tiles reset when the clip ends since FIX r3, as they do on Stop; the rate is on the corpus rows), `errors: []` | baseline 2026-09-18: `2 / 1` ×3 at "60 fps"; FIX r1/r2 2026-09-19: `4 / 4` then `4 / 3` ×3; **FIX r3 2026-09-20 live: `{"good":"4","attempts":"4","fps":"–","hosts":["pushups.kalpkan.com"],"errors":[]}` ×3, the same in demo and camera mode** (the 6.2 s mild worm is graded good by the geometry, documented on the page) |
| CI | `cd ~/projects/pushups && gh run list --limit 3` | latest run on `main` `completed success` (`.github/workflows/ci.yml`: typecheck, vitest with `--pool=forks --maxWorkers=1`, build) | added 2026-09-18 by the Phase 5 SPEC agent; run `35413958556` on `06e245a` `completed success` (23 s), confirmed by TEST r1 2026-09-18; FIX r2 2026-09-19: run `35476666835` on `634fdc4` `completed success` (50 s) |
| Functional smoke: same clip, same count (spec S2 consistency) | `cd ~/projects/pushups && for i in 1 2 3; do GPU=1 REPORT_ONLY=1 node scripts/e2e-corpus.mjs https://pushups.kalpkan.com/ > /tmp/pushups-run$i.log; done; grep -h "total" /tmp/pushups-run?.log \| awk '{print $1,$4,$8}' \| sort \| uniq -c \| awk '$1!=3'` | no output (every clip prints the same `total` and `good` in all three runs) | TEST r1 2026-09-18: 6/15 clips differed; **FIX r1 2026-09-19 live: attempts identical on 15/15, good identical on 14/15 (`bad_IMG_4470` 1, 1, 2: the classifier's mean probability at one bottom sits near 0.5)**; TEST r2 2026-09-19 live: no output, attempts and good identical on 15/15 across three runs; **FIX r2 2026-09-19 live: no output, attempts and good identical on 15/15 across three runs**; TEST r3 2026-09-19 on `634fdc4` live: 15/15 ×3 with every clip identical across runs (`pushups-r3-corpus-live-run{1,2,3}-2026-09-19.log`), fps avg 29.6 over 45 clip-runs, min 23; Playwright Chromium 5/5 (`pushups-r3-playwright-chromium-2026-09-19.txt`) |
| Functional smoke: count independent of frame rate (spec S6) | `cd ~/projects/pushups && cp ~/projects/portfolio/docs/reports/evidence/pushups-r1-framedrop-replay.test.ts tests/zz_framedrop.test.ts && npx vitest run --pool=forks --maxWorkers=1 tests/zz_framedrop.test.ts --reporter=verbose \| grep -E "clips whose\|high-conf"; rm tests/zz_framedrop.test.ts` | `clips whose count changes when every 3rd frame is dropped: 0/15`; high-confidence verdicts matching the label ≥ 66/69 | TEST r1 2026-09-18: **3/15** change at 20 fps (6/15 at 10 fps); verdict at the bottom 67/69 (the misses are at the top and on pikes; D3, D6) (`pushups-r1-framedrop-replay-output-2026-09-18.txt`). Since FIX r1 the r1 script no longer compiles (the counter's input changed); the invariant is the corpus test's "same attempt count at 30, 20, 15 and 10 fps" cases (part of `npm test`). TEST r2 2026-09-19: attempts identical at 20/15/10 fps on 15/15 clips on the python, browser and live-recorded traces, good ±1 on 1/15, 4/15, 3/15 (`pushups-r2-framedrop-table-2026-09-19.txt`); live camera path with ×4 CPU throttling (20–27 fps) same counts on 6/6 clips, at ×20 (3–9 fps) fast reps are lost (`pushups-r2-cpu-throttled-2026-09-19.log`); FIX r2 2026-09-19: the corpus test's frame-rate case is exact (attempts equal; a verdict may move only for a rep whose classifier mean is within 0.1 of 0.5) and passes on 15/15 clips of both sets; high-confidence verdicts 65/67 on both sets |
| No classifier is shipped; the frame loop still fails loudly (report r2 D1, FIX r3) | `cd ~/projects/pushups && ls public/models; grep -c tfjs package.json; mkdir -p /tmp/fm && node scripts/e2e-failure-modes.mjs https://pushups.kalpkan.com/ /tmp/fm` | `pose_landmarker_full.task` only, `0` (no `@tensorflow/tfjs`); `loop-throws` ends with `status` "Something went wrong: injected loop failure. Reload the page and try again." and `errorCount` 0 uncaught; `pose-model-404` ends with a "Could not start" status naming the model and `requests` listing `/models/pose_landmarker_full.task` | FIX r3 2026-09-19 (the stale-v1/stale-then-fresh scenarios of r2 no longer exist: nothing under `/models/form*` is fetched) |
| Placement hints appear within 1 s and pause the count (spec S7, D4/D5/D6) | `cd ~/projects/pushups && node scripts/e2e-hints.mjs https://pushups.kalpkan.com/ <dir with black/two-people/cropped-right/frontal/portrait .mjpeg>` (clip recipe in the r2 report's last section; the script also runs `IMG_1359` and `IMG_1512` from `tests/fixtures/clips/.mjpeg/`; `DUMP_DIR=<dir>` saves every pose + hint per frame via `?trace=full`) | black → "Too dark…" < 1000 ms; two-people → "Only one person…" < 1000 ms **and `total 0` with "Counting paused"**; frontal → "Feet out of frame…" < 1000 ms and `total 0`; cropped-right → "Step back…" or "Head out of frame…" < 1000 ms and `total 0`; `IMG_1359` → "Head out of frame…" while the head is out, still 9 attempts; `IMG_1512` → **no** "Only one person" (the phantom pose while lying flat is ignored), 6 attempts; `portrait` → a count | TEST r2 2026-09-19: dark 741 ms, two people 748 ms (4 counted under it), feet 739 ms, cropped-right no hint 4.5 s + a rep (D4), `IMG_1512` false hint 1.2 s (D6). **FIX r2 2026-09-19 (preview build): dark 769 ms, two people 845 ms + paused + 0, frontal 752 ms + paused + 0, cropped 757–775 ms + 0, `IMG_1512` no false hint, `IMG_1359` 9/8** ; **live `634fdc4` (00:05 UTC): dark 763 ms, two people 854 ms + paused + 0, cropped 767 ms + 0, frontal 756 ms + paused + 0, `IMG_1512` no false hint (`pushups-fix2-hints-live-2026-09-19.json`)**; TEST r3 2026-09-19 live: dark 746–758 ms, two people 838–842 ms + paused + 0, cropped 745–761 ms + 0, frontal 750–788 ms + paused, IMG_1359 head hint 2.5–11.3 s with 9 attempts, IMG_1512 no "one person" (`pushups-r3-hints-live-2026-09-19.txt`) |
| Functional smoke: hints, stop, refusal, hosts (spec S7–S9) | `cd ~/projects/pushups && node ~/projects/portfolio/docs/reports/evidence/pushups-r1-ux-checks.mjs https://pushups.kalpkan.com/ /tmp/pushups-ux /path/to/black.mjpeg` (black clip recipe in the script header) | `layout[*].scrollW == innerW` and `cls` 0 at 360/390/430; `noPoseAfterMs` ≤ 1000; `stop.trackStates` `["ended"]`, `stop.srcObject` null; `restart` 0 / 0; `doubleStart` one session; `hosts` `["pushups.kalpkan.com"]`; `requestsAfterLoad` only `/ingest/*`; `deny.status` starts "Camera permission was refused" with `demoDisabled: false`; `errors: []`; and (after the fix round) the hint text names the dark room, a cut-off head/feet, a second person and a frontal view | TEST r1 2026-09-18: all of the mechanical checks pass (`pushups-r1-ux-checks-2026-09-18.json`); the only hint is "Step back so your whole body is visible", also in the dark; no head/feet/second-person/frontal hint (D5, D9); stale tiles after Stop (D13) |
| Functional smoke: overlay at the moments that matter (spec S3, S7) | `cd ~/projects/pushups && for a in "bad_IMG_4456 2.2" "IMG_1359 2,5.5" "IMG_1305 3" "IMG_1512 31" "test_video_2 10.7"; do set -- $a; node ~/projects/portfolio/docs/reports/evidence/pushups-r1-stage-shots.mjs $1 $2 /tmp/pushups-shots; done` | bad_IMG_4456 2.2 s: red skeleton **and a reason**; IMG_1359 2 s: a "move back" hint (head off frame), 5.5 s: not "Good form" while kneeling; IMG_1305 3 s: 2 attempts already on screen; IMG_1512 31 s: pike scored bad; test_video_2 10.7 s: pike scored bad | TEST r1 2026-09-18: `pushups-r1-overlay-montage-2026-09-18.jpg`: "Bad form 94 %" with no reason; no hint with the head off; "Good form 100 %" kneeling and on the IMG_1512 pike (counted as good rep 5); IMG_1305 "0 attempts" at 3 s; only the test_video_2 pike is caught |
| Form is graded whichever way the visitor faces (spec S3, report r3 D1) | `cd ~/projects/pushups && MIRROR=1 node scripts/make-mjpeg.mjs` (once, 15 flipped `.mjpeg` in `tests/fixtures/clips/.mjpeg/flip-*`) then `MIRROR=1 GPU=1 REPORT_ONLY=1 node scripts/e2e-corpus.mjs https://pushups.kalpkan.com/` | the flipped clips print the same counts as the originals within ±1: `flip-bad_IMG_4451` 4/0–1, `flip-bad_IMG_4470` 4/0, `flip-test_video` 4/3, `flip-good_IMG_4409` 5/5, `flip-IMG_1359` 9/8, `flip-IMG_1360` 6/6; `flip-bad_IMG_4456` 4/4 is the known miss (in both facings); 13/15 PASS (the two FAIL lines are `bad_IMG_4456` and, at 4/1 when the 2.8 s rep lands on the threshold, `bad_IMG_4451`'s good count is still within the bar) | TEST r3 2026-09-19: 4/4, 4/4, 4/3 on the bad clips and `IMG_1359`/`IMG_1360` 0 good the other way (classifier gated to one facing); **FIX r3 2026-09-20 live: 14/15 mirrored (`flip-bad_IMG_4451` 4/1, `flip-bad_IMG_4470` 4/0, `flip-IMG_1359` 9/8, `flip-IMG_1360` 6/6, `flip-bad_IMG_4456` 4/4 known), fps avg 30 min 25** |
| No placement hint on a clean set-up (spec S7, report r3 D3) | `cd ~/projects/pushups && EXTRA=test_video,test_video_2,good_IMG_4378 node scripts/e2e-hints.mjs https://pushups.kalpkan.com/ tests/fixtures/clips/.mjpeg test_video test_video_2 good_IMG_4378 IMG_1359` (prints every string drawn on the canvas with first/last ms) | no "Head out of frame" / "Feet out of frame" / "Step back" line at all on `test_video`, `test_video_2`, `good_IMG_4378` in their first 6 s (before FIX r3 the head hint spanned the whole clip); `IMG_1359` must still show `Head out of frame` from about 2.5 s while the head is cut off at the tops, with the count unaffected (9/8 in the corpus rows) | TEST r3 2026-09-19: 409 frames of a false hint on `test_video`; **FIX r3 2026-09-20 live: no hint line on the three clean clips, `IMG_1359` "Head out of frame" 3054–6283 ms (97 frames)** |
| Demo verdicts are steady, not a per-frame coin (spec S3/S5, report r3 D2) | `cd ~/projects/pushups && node ~/projects/portfolio/docs/reports/evidence/pushups-r3-demo-trace.mjs https://pushups.kalpkan.com/ 3` | `events` `1.2s good, 2.2s good, 4.4s good, 7.1s good` on every play (no per-frame classifier since FIX r3; the mild worm at 7.1 s is graded good and says so on the page); no "Bad form: keep your body straight" text at all; the live overlay turns red only for sag / pike / knees, by majority over 0.25 s; and the corpus gate's frame-drop rows (identical good counts at 30/20/15/10 fps) pass | TEST r3 2026-09-19: "Bad form" on 117 frames vs 136 "Good form"; **FIX r3 2026-09-20 live: 0 "Bad form" frames, 253–254 "Good form", events `1.17s good, 2.16s good, 4.32s good, 7.07s good` on 3/3 plays** (run the script from `~/projects/pushups`, where `puppeteer-core` is installed) |
| Bad reps are graded bad (spec S3, report r4 D1) | `cd ~/projects/pushups && GPU=1 REPORT_ONLY=1 TRACE_DIR=/tmp/t node scripts/e2e-corpus.mjs https://pushups.kalpkan.com/ && python3 ~/projects/portfolio/docs/reports/evidence/pushups-r3-verdicts.py /tmp/t \| tail -12` | `bad_IMG_4456 … good 0` (±1) and `high-confidence verdicts: 67/68` or better (the only allowed miss is `IMG_1305 0.7s … got None`, a clip that starts mid-descent); every `MISS … label bad got good` line is a sag/worm/collapse counted as clean | TEST r4 2026-09-20 on live `b622fa3`: **58/68**, `bad_IMG_4456` 4/4 good in both facings, 9 of 21 high-confidence bad reps graded good (`pushups-r4-verdicts-live-run1-2026-09-20.txt`) |
| Count and verdict do not depend on which frames a phone decodes, in either facing (spec S4/S6, report r4 D2/D3) | `cd ~/projects/pushups && cp ~/projects/portfolio/docs/reports/evidence/pushups-r4-phase.test.ts tests/ && R4_OUT=/tmp/phase.txt npx vitest run --pool=forks --maxWorkers=1 tests/pushups-r4-phase.test.ts; cat /tmp/phase.txt; rm tests/pushups-r4-phase.test.ts` | `no differences` (drops every 3rd / 2nd / 2-of-3 / 3-of-4 frames at EVERY phase on the Python, browser and mirrored trace sets); `IMG_1305` at 7.5 fps losing a 0.7 s rep is the one tolerated line (below the 8 fps warning) | TEST r4 2026-09-20: un-mirrored sets clean; mirrored `bad_IMG_4451` 4/1 → 4/2, 4/3, **4/4** by phase, mirrored `demo` 4/4 → 4/3, mirrored `IMG_1513` 2/1 → 1/1 (`pushups-r4-framedrop-phase-sweep-2026-09-20.txt`) |
| Overlay at the bottom of a bad rep (spec S3, report r4 D1/D5) | `cd ~/projects/pushups && cp ~/projects/portfolio/docs/reports/evidence/pushups-r4-shots.mjs scripts/ && node scripts/pushups-r4-shots.mjs https://pushups.kalpkan.com/ /tmp/shots bad_IMG_4456=tests/fixtures/clips/.mjpeg/bad_IMG_4456.mjpeg:1.9,2.6,3.0,4.4 bad_IMG_4470=tests/fixtures/clips/.mjpeg/bad_IMG_4470.mjpeg:2.3,2.8; rm scripts/pushups-r4-shots.mjs` then look at `/tmp/shots/*.png` | a red skeleton with a reason on every `bad_IMG_4456` frame; on `bad_IMG_4470` no green "Good form" label beside a red "Rep 1: hips sagging" | TEST r4 2026-09-20: `bad_IMG_4456` green + "Rep 2: good" at 2.6 s and 3.0 s, `bad_IMG_4470` "Good form" beside "Rep 1: hips sagging" (`pushups-r4-overlay-montage-2026-09-20.jpg`) |
| Slow device warning (spec S6, report r3 D6) | `cd ~/projects/pushups && cp ~/projects/portfolio/docs/reports/evidence/pushups-r3-cam.mjs scripts/ && THROTTLE=40 node scripts/pushups-r3-cam.mjs https://pushups.kalpkan.com/ /tmp/slow.json demo=tests/fixtures/clips/.mjpeg/demo.mjpeg:9; rm scripts/pushups-r3-cam.mjs; jq -r '.demo.final.status' /tmp/slow.json` | `Slow device (N fps): fast reps may be missed…` once the measured rate stays under 8 fps for 2 s (×40 CPU gives 5–6 fps on this Mac; ×20 gives 11 fps and no line, correctly) | TEST r4 2026-09-20 live: `Slow device (5 fps)…`, demo 3/3 and `IMG_1305` 5/5 at 6 fps (`pushups-r4-cpu-throttle-x40-2026-09-20.txt`) |
| Hint and pause follow the tracked body; trace has a row per analysed frame; Phase honest (round-4 critique majors, FIX r5) | `cd ~/projects/pushups && npx vitest run --pool=forks --maxWorkers=1 tests/hints.test.ts tests/repCounter.test.ts -t "mainFirst\|state().phase"` then `grep -n "mainFirst(poses, landmarks)\|!seen \|\| paused" src/session.ts` and `curl -s https://pushups.kalpkan.com/ \| grep -o "assets/index-[A-Za-z0-9_-]*.js"`, fetch that file, take its `session-*.js` name and `curl -s https://pushups.kalpkan.com/assets/<session>.js \| grep -o "\[[a-z],\.\.\.[a-z]\.filter([a-z]=>[a-z]!==[a-z])\]"` | 4 hint tests + 1 phase test pass; both greps hit one line each; the live session chunk contains the `[t,...e.filter(e=>e!==t)]` pattern and `grep -c ascending` on it prints `1` | confirmed 2026-09-20 04:45 UTC on `08f222a` (deployment `pushups-gxaagsk32`, via the Git integration): 176 passed + 4 expected fails, corpus 55/58/57 of 67 unchanged |

### Browser-ML demos (pushups, emotes)

| Check | Command | Expect | Status |
|---|---|---|---|
| Runs on a phone with no server calls | open the demo on a phone over cellular; on desktop, open DevTools Network tab and use the demo | no requests after initial load except the PostHog `/ingest` events | pushups and emotes done above (headless-Chrome host list); phone run is H11 (emotes) / H15 (pushups) for Kalp. Liveness: UptimeRobot `pushups health` 804031518, `emotes health` 804031519, `microtubules health` 804031520 on each `/health.json` (2026-09-19) |
| Lighthouse performance | same Lighthouse command as the hub, per demo | `>= 0.85` for the MediaPipe demos (models are lazy; hosting-plan §10 says 0.9 for the hub) | emotes 0.91, pushups see its table (2026-09-18) |

### Money (month end, every month)

| Check | Command | Expect | Status |
|---|---|---|---|
| Nothing charged | Cloudflare billing (domain only), Vercel $0, Supabase $0, Neon $0, Firebase $0, PostHog $0 | matches the spend tracker in `STATUS.md` | PENDING (first month end after H1) |
