# Settings map

Every environment variable and secret in the platform: which app uses it, which dashboard holds the real value, who can rotate it, and what breaks without it. **Names only, never values.** Real values live only in the owning dashboard (Vercel project settings, Supabase, Cloudflare, and so on) or in the local MCP configuration on Kalp's machine; never in a repo, never in this file, never in `STATUS.md` or a chat transcript.

Every task that introduces, renames, or removes a variable updates this table before it counts as done.

promptflip is one Vercel project, `promptflip-35qv` (H5 resolved 2026-09-18; the duplicate `promptflip` project is gone), and `~/projects/promptflip` is linked to it, so `npx vercel env ...` run there targets the right project. Every app repo also carries a `.env.example` listing its names with empty values, which must agree with this table.


## Application variables

| Variable | Which app | Where it lives | Who can rotate it | What breaks without it |
|---|---|---|---|---|
| `NEXT_PUBLIC_SUPABASE_URL` | promptflip | Vercel, project `promptflip-35qv`, Settings, Environment Variables (all environments). Value comes from Supabase Project A, Settings, API. Public (shipped to the browser) | anyone with Vercel team access; the value only changes if the Supabase project is recreated | the app cannot reach Supabase at all: sign-in fails, `/api/health` reports `db` not ok |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | promptflip | Vercel, project `promptflip-35qv`. Value from Supabase Project A, Settings, API, `anon` key. Public; safe to expose because RLS scopes it | Supabase dashboard (Project A, Settings, API, regenerate) then re-set in Vercel and redeploy | same as above: no browser-side Supabase access |
| `SUPABASE_SERVICE_ROLE_KEY` | promptflip (server routes and the daily cron only) | Vercel, project `promptflip-35qv`, server-side only (never `NEXT_PUBLIC_`). Value from Supabase Project A, Settings, API, `service_role` key. **Bypasses RLS: treat as root** | Supabase dashboard (Project A) then Vercel, redeploy | server-side admin operations and the cron fail; user-facing pages may still load |
| `KEY_ENCRYPTION_SECRET` | promptflip | Vercel, project `promptflip-35qv`, server-side only. Generated locally (random bytes); not held by any provider | anyone with Vercel access, **but rotating it makes every stored user API key undecryptable**; requires a re-encryption migration, so it is a human checkpoint | users' encrypted OpenRouter keys cannot be decrypted; every model call fails |
| `CRON_SECRET` | promptflip | Vercel, project `promptflip-35qv`, server-side only. Vercel sends it as a bearer token to the cron route defined in `vercel.json`; generated locally | anyone with Vercel access; set the new value and redeploy, no data impact | the once-per-day cron route rejects Vercel's call with 401, so the daily job silently stops |
| `NEXT_PUBLIC_APP_URL` | promptflip | Vercel, project `promptflip-35qv`, Production only. Value `https://promptflip.kalpkan.com` since 2026-09-18 (changed with `vercel env rm` + `env add` from `~/projects/promptflip`, then `vercel redeploy`; it is a build-time `NEXT_PUBLIC_` value, so it only takes effect on a fresh build). Must match the Supabase Auth Site URL (runbook "Re-point OAuth redirects") | anyone with Vercel access | OAuth redirects and absolute links point at the wrong host; Google sign-in returns to the old URL or fails with `redirect_uri_mismatch` |
| `SECRET_KEY` | Plato | Vercel, project `plato`, Settings, Environment Variables (production + preview). Generated locally with `secrets.token_hex(32)`; not held by any provider. **No default in code**: the app raises at import without it | anyone with Vercel access; rotating it only invalidates in-flight visitor cookies (they start over) | the function fails to start (`RuntimeError: SECRET_KEY ...`), every request 500s |
| `DATABASE_URL` | Plato | Vercel, project `plato` (production + preview). Value is the **pooled** Neon URI for database `plato`, role `plato_owner` (runbook "Create a Neon database"); fetch a fresh one from the Neon API, never paste it anywhere else | Kalp / agent via Neon API `reset_password`, then update Vercel and redeploy | `/api/health` returns 503 `db: error`; uploads fail because extraction results cannot be cached |
| `POSTHOG_API_KEY` | Plato (also the name the other apps should reuse) | Vercel, project `plato` (production + preview). Value is the **public** project token (`phc_...`) of PostHog project "Kalp portfolio" (id 616829); it ships to the browser, so it is not a secret | PostHog dashboard, project settings | analytics off: no snippet is rendered and server events are skipped; the app itself works |
| `POSTHOG_HOST` | Plato | Vercel, project `plato`. `https://us.i.posthog.com` (server-side ingest and the `/ingest` proxy target) | anyone with Vercel access | defaults to the same value in code |
| `POSTHOG_UI_HOST` | Plato | optional; defaults to `https://us.posthog.com` in code | n/a | n/a |
| `PLATO_TMP_DIR` | Plato | optional; defaults to `/tmp` in code (the only writable path on Vercel). Only set locally or in tests | n/a | n/a |
| `SUPABASE_URL` | basketball (`hoops` dashboard) | Vercel, project `v0-basketball-analytics-dashboard`, Settings, Environment Variables (Production). Value is `https://yzppfufqaekgaxcrsqxp.supabase.co` (Supabase Project B `platform`). Set 2026-09-18 (T1.1); an older copy from April also exists for Development/Preview | anyone with Vercel access; only changes if Project B is recreated (never) | dashboard falls back to sample data and shows the "Demo data" banner |
| `SUPABASE_ANON_KEY` | basketball | Vercel, project `v0-basketball-analytics-dashboard`, Production. From Management API `GET /v1/projects/yzppfufqaekgaxcrsqxp/api-keys` (`anon`). Not used by the dashboard code yet (server reads with the service role); present for future browser-side reads | Supabase dashboard (Project B, Settings, API) then Vercel | nothing today |
| `SUPABASE_SERVICE_ROLE_KEY` | basketball (server route `/api/dashboard` only) | Vercel, project `v0-basketball-analytics-dashboard`, Production, server-side only. **Bypasses RLS: treat as root.** A legacy `service_role_key` (same value, April 2026) is still set for Development/Preview/Production and is read as a fallback by `apps/web/lib/supabase-admin.ts` | Supabase dashboard (Project B) then Vercel, redeploy | dashboard falls back to sample data ("Demo data" banner) |
| `INGEST_API_KEY` | basketball: the `hoops-ingest-shot` Edge Function checks it against the `x-device-api-key` header; the phone app will hold it | Vercel, project `v0-basketball-analytics-dashboard`, Production **and** Supabase Project B, Edge Functions, Secrets (both hold the same value, generated with `openssl rand -hex 32` on 2026-09-18; a local copy for agent test ingests sits in `~/.config/portfolio-ops/hoops-ingest.key`, mode 600, never in a repo) | anyone with Vercel + Supabase access: generate a new value, set it in both places (`supabase secrets set --env-file`), give it to the phone app | ingest returns `401 Unauthorized`; the dashboard keeps serving old rows |
| `POSTHOG_KEY` | basketball: `hoops-ingest-shot` Edge Function sends `shot_ingested` to PostHog | Supabase Project B, Edge Functions, Secrets. Same public `phc_` value as `NEXT_PUBLIC_POSTHOG_KEY` | PostHog dashboard | the `shot_ingested` event silently stops; ingest still works |
| `NEXT_PUBLIC_POSTHOG_KEY`, `NEXT_PUBLIC_POSTHOG_HOST` | basketball (and hub) | Vercel, project `v0-basketball-analytics-dashboard`, Production, type `config` (public by design; the CLI demands `--type config` for a `NEXT_PUBLIC_` token). Host is always `/ingest` | PostHog dashboard (project "Kalp portfolio", Settings, project API key) then Vercel, redeploy | analytics stop for the dashboard; app unaffected |
| `VITE_PUBLIC_POSTHOG_KEY`, `VITE_PUBLIC_POSTHOG_HOST` | microtubules (`KalpKan/Microtubule-Quantification`, folder `web/`) | Vercel, project `microtubules`, Production + Preview (key) / Production (host), set 2026-09-18 (T1.4) by piping the value into `npx vercel env add`. Value is the **public** `phc_` project token of PostHog project "Kalp portfolio" (id 616829); host is always `/ingest`. Vite only exposes `VITE_`-prefixed names and inlines them at build time, so a change needs a redeploy. Names listed in the repo's `web/.env.example` | PostHog dashboard, then `npx vercel env rm` + `env add` and redeploy (runbook "Rotate the PostHog key") | analytics silently off for the demo (`initAnalytics` is a no-op); the analysis itself is unaffected |
| `VITE_PUBLIC_POSTHOG_KEY`, `VITE_PUBLIC_POSTHOG_HOST` | emotes (`KalpKan/emote-detector-web`, repo root) | Vercel, project `emotes` (`prj_mgoUdU4NQ9X8sfP7Jhcy4xBBM6pr`), Production + Preview (both names), set 2026-09-18 (T3.2) by piping the value into `npx vercel env add ... --force`. Same public `phc_` token of PostHog project "Kalp portfolio" (id 616829); host is always `/ingest`. Vite inlines them at build time, so a change needs a redeploy. Names listed in the repo's `.env.example` | PostHog dashboard, then `npx vercel env add ... --force` and redeploy (runbook "Rotate the PostHog key") | analytics silently off (`initAnalytics` never imports posthog-js); detection, sounds and demo unaffected |
| `NEXT_PUBLIC_POSTHOG_KEY` | hub (`portfolio`) | Vercel, project `portfolio`, Production + Preview, type `config` (set 2026-09-18, T0.5). Value is the **public** `phc_` project token of PostHog project "Kalp portfolio" (id 616829), printed in `docs/analytics.md`; it ships to the browser, so not a secret, but still not in git | PostHog dashboard, Project settings, then `vercel env rm` + `env add --type config` and redeploy (runbook "Rotate the PostHog key") | analytics silently off for the hub (`initPostHog` is a no-op); the site itself is unaffected |
| `NEXT_PUBLIC_POSTHOG_HOST` | hub (`portfolio`) | Vercel, project `portfolio`, Production + Preview. Always `/ingest` (the app proxies to PostHog through its own origin; see `next.config.ts` rewrites) | anyone with Vercel access | defaults to `/ingest` in code, so nothing |
| `FIREBASE_PROJECT_ID`, `FIREBASE_CLIENT_EMAIL`, `FIREBASE_PRIVATE_KEY` | plantit (`KalpKan/PlantWater`, Express API) | Vercel, project `plantit`, Production + Preview (set 2026-09-18, T2.1). Values are `project_id` / `client_email` / `private_key` of the Firebase service account for project `plant-it-5e2fc` (local copy `~/.config/portfolio-ops/plantit-firebase-sa.json`, mode 600, never in a repo). **The private key is root for that Firebase project** | Firebase console → project settings → Service accounts → Generate new private key, then re-set the three names in Vercel and redeploy; delete the old key in Google Cloud IAM | `/api/health` → 503 `firestore: error`, every signed-in route 503; the static site still loads |
| `FIREBASE_DATABASE_URL` | plantit | optional; defaults in code to `https://plant-it-5e2fc-default-rtdb.firebaseio.com` | n/a | n/a |
| `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY` | plantit (photo uploads to Storage bucket `plantit-photos` in Project B, server-side only) | Vercel, project `plantit`, Production + Preview. Same Project B values as basketball (`https://yzppfufqaekgaxcrsqxp.supabase.co`, Management API `api-keys` → `service_role`). **Bypasses RLS: treat as root** | Supabase dashboard (Project B) then Vercel, redeploy | plants save without a photo (`imageUrl: null`); `/api/health` → `photos: unconfigured` |
| `SUPABASE_PHOTOS_BUCKET` | plantit | optional; defaults to `plantit-photos` | n/a | n/a |
| `PLANTNET_API_KEY` | plantit (species identification) | Vercel, project `plantit`. **Not set as of 2026-09-18** (STATUS.md "Needs Kalp"); from https://my.plantnet.org/ (free tier 500/day) | Kalp (Pl@ntNet account) | identification runs in demo mode (bundled 5 plants, labelled "Demo result"); `/api/health` → `identification: demo` |
| `OPENAI_API_KEY` (+ optional `OPENAI_MODEL`, default `gpt-4o-mini`) | plantit (care guides for species outside the bundled 5) | Vercel, project `plantit`. **Not set as of 2026-09-18** (STATUS.md "Needs Kalp"); from https://platform.openai.com/ with a low monthly budget set there | Kalp (OpenAI dashboard) | care guides come from the bundled library or generic guidance; `/api/health` → `care: bundled` |
| `PLANTNET_DAILY_LIMIT`, `OPENAI_DAILY_LIMIT` | plantit spend guard | optional; default 50 per UTC day each, counted in Firestore `spend/<service>_<day>`; past the cap the demo/bundled path answers | anyone with Vercel access | defaults apply |
| `CORS_ORIGINS`, `DEVICE_DISCOVERY_SUBNET` | plantit | optional; extra browser origins / home-network prefix for ESP8266 discovery when the API runs locally. Unset on Vercel | n/a | same-origin only; discovery reports "hardware required" |
| `REACT_APP_POSTHOG_KEY`, `REACT_APP_POSTHOG_HOST` | plantit (CRA frontend) | Vercel, project `plantit`, Production + Preview (set 2026-09-18). Public `phc_` token of PostHog project "Kalp portfolio"; host always `/ingest`. CRA bakes `REACT_APP_*` in at build time, so a change needs a redeploy | PostHog dashboard, then `vercel env rm` + `env add`, redeploy | analytics silently off; app unaffected |
| `REACT_APP_API_URL` | plantit | optional; empty means same-origin `/api` (the Vercel layout). Only for hosting the API elsewhere | n/a | same-origin |
| `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `NEXT_PUBLIC_APP_SCHEMA`, `NEXT_PUBLIC_APP_NAME` (optional), `NEXT_PUBLIC_POSTHOG_KEY`, `NEXT_PUBLIC_POSTHOG_HOST` | every app started from `KalpKan/portfolio-template` (T1.2, 2026-09-18); listed by name in the template's `.env.example` and README "Where the settings live" | Vercel, the new app's project, Settings, Environment Variables (Production + Preview). Supabase values come from Project B `platform` (`https://yzppfufqaekgaxcrsqxp.supabase.co`, Management API `api-keys` → `anon`); `NEXT_PUBLIC_APP_SCHEMA` is the app's own schema name (lowercase identifier, zod-validated in `lib/env.ts`; the client sends it as `Accept-Profile`/`Content-Profile`); PostHog key is the public `phc_` token (`--type config`), host always `/ingest`. All are public by design (`NEXT_PUBLIC_`), still never in git. The template itself has no Vercel project and no env set anywhere (the smoke deploy ran with none) | anyone with Vercel access, then redeploy (build-time values); Supabase anon key via Project B dashboard, PostHog via runbook "Rotate the PostHog key" | with the three Supabase names empty the app runs with no database and `/api/health` says `db: "skipped"` (200); with them wrong it says `db: "error"` (503); with the PostHog names empty analytics is silently off |

## Operator credentials (local machine only, never in any repo)

These are the one-time keys Kalp supplies at human checkpoint H0. They live in the local MCP configuration or shell environment on Kalp's Mac and are used by agents to drive the provider APIs. They are never committed, never printed, and never placed in Vercel.

| Variable | Used for | Where it lives | Who can rotate it | What breaks without it | Status |
|---|---|---|---|---|---|
| `CLOUDFLARE_API_TOKEN` | creating DNS records and R2 buckets via the Cloudflare API (T0.4 execution after H1) | local MCP config / shell env on Kalp's Mac. Created at Cloudflare, My Profile, API Tokens, "Edit zone DNS" template | Kalp (Cloudflare dashboard) | agents cannot add or fix DNS records; the site keeps serving on existing records | PENDING (H0) |
| `UPTIMEROBOT_API_KEY` | creating and editing monitors and the public status page, reading uptime, via the UptimeRobot API. Works as the v2 `api_key` form field and as the v3 `Authorization: Bearer` token (use v3 for writes: v2 `newMonitor` is rejected on this account, see `docs/monitors.md`) | `~/.config/portfolio-ops/secrets.env` on Kalp's Mac (load with `set -a; source ~/.config/portfolio-ops/secrets.env; set +a`). From UptimeRobot, Integrations and API, Main API key. Never in a repo, Vercel, or a commit message | Kalp (UptimeRobot dashboard, Integrations and API, regenerate; then update `secrets.env`) | agents cannot add, edit, or read monitors; the three existing monitors and the status page keep running unattended | in use since 2026-09-18 (T0.3) |
| `POSTHOG_PERSONAL_API_KEY` (+ `POSTHOG_HOST` = `https://us.posthog.com`) | PostHog REST API from scripts: renaming the project, project settings (replay, heatmaps, masking), dashboards, insights, reading events for verification, reading billing (T0.5) | `~/.config/portfolio-ops/secrets.env` on Kalp's Mac (load with `set -a; source ~/.config/portfolio-ops/secrets.env; set +a`). From PostHog, Settings, Personal API keys. **It is an all-access `phx_` key; rotate to a project-scoped key after Phase 1** (runbook "Rotate the PostHog key"). Never printed, never in a repo. It cannot write billing (403 by design) | Kalp (PostHog dashboard) | agents cannot manage PostHog or verify events by API; every deployed snippet (which uses the separate public `phc_` token) keeps sending | in use since 2026-09-18 (T0.5) |
| `NEON_API_KEY` (+ `NEON_PROJECT_ID`) | creating and inspecting Plato's database via the Neon API v2 (runbook "Create a Neon database") | `~/.config/portfolio-ops/secrets.env`. From Neon, Account settings, API keys | Kalp (Neon console) | agents cannot create or inspect Neon databases; a running Plato is unaffected because it uses its own `DATABASE_URL` | in use since 2026-09-18 (T1.3) |
| Vercel CLI login | every Vercel operation (`npx vercel ...`) | `~/Library/Application Support/com.vercel.cli/auth.json` on Kalp's Mac (already logged in, team `kks-projects-2edcb11a`) | Kalp (`npx vercel login`) | no deploys, env changes, or domain changes from the CLI; Git-integration deploys still happen | works now |
| Vercel MCP auth | Vercel MCP plugin tools | authorised once via the link in `STATUS.md` H0 | Kalp (re-authorise) | fall back to the CLI | PENDING (H0) |
| Supabase MCP auth | Supabase MCP tools (schemas, migrations, API settings, Edge Functions, renaming Project B) | authorised once via the link in `STATUS.md` H0 | Kalp (re-authorise) | fall back to the Supabase dashboard through the browser tools | PENDING (H0) |
| `gh` CLI login | repos, archiving, secrets, Actions | `gh auth status` on Kalp's Mac, logged in as `KalpKan` | Kalp (`gh auth login`) | no repo operations from the CLI | works now |

## Variables expected in later phases (names reserved, rows filled when the task lands)

| Variable | Which app | Task |
|---|---|---|
| ~~`NEXT_PUBLIC_APP_SCHEMA`~~ | the new-project template | T1.2 landed 2026-09-18: row above ("every app started from `KalpKan/portfolio-template`") |
| ~~Firebase web config, Pl@ntNet API key, OpenAI API key~~ | PlantWater | T2.1 landed 2026-09-18: rows above; the Firebase *web* config is public and hard-coded in `frontend/src/firebase.js` |


## Operator credentials location (added 2026-09-18, H0 complete)

All operator keys live in `~/.config/portfolio-ops/secrets.env` (mode 600, never in any repo). Load them in a shell with `set -a; source ~/.config/portfolio-ops/secrets.env; set +a`. Names: `POSTHOG_PERSONAL_API_KEY` (all-access; rotate to project-scoped after Phase 1), `POSTHOG_HOST`, `CLOUDFLARE_API_TOKEN` (Edit zone DNS), `UPTIMEROBOT_API_KEY`, `NEON_API_KEY`, `NEON_PROJECT_ID`, `SUPABASE_ACCESS_TOKEN` (personal access token; the Supabase CLI and Management API honour it as an env var). Vercel: CLI login only. GitHub: `gh` login only.
