---
name: portfolio-ops
description: Use when operating, repairing, or extending Kalp's project-hosting platform (the hub at KalpKan/portfolio plus every project subdomain on Vercel, Supabase, Neon, Cloudflare, UptimeRobot, PostHog). Triggers include "portfolio is down", "add a project to the portfolio", "Supabase paused", "deploy failed", "subdomain not resolving", "analytics missing", "health check failing", "where does this env var live", "rotate a secret", "attach a domain", or any task in the Phase 0-4 plan that must update the ops record afterwards.
---

# portfolio-ops

## Overview

This is the repair manual and institutional memory for Kalp's portfolio platform: one hub site (Next.js on Vercel Hobby) that links to every project he has built, each project living on its own subdomain, all on free tiers, with the `.com` domain as the only paid line item.

Core principle: **this skill is written during the build, not after.** Every task that changes the platform appends to one of the five files below before it counts as done. If you are an agent who has never seen this project, read this file top to bottom, then open the file that matches your problem. The system design that all of this executes is `docs/hosting-plan.md` in the hub repo; do not re-litigate it, execute it, and if reality contradicts it, update the doc with evidence.

## When to use

- Something is broken: a site is down, a badge is grey, a deploy failed, a subdomain does not resolve, Supabase says "paused", analytics stopped arriving.
- Something is being added: a new project, a new subdomain, a new Supabase schema, a new secret.
- A task in `docs/superpowers/plans/` just finished and its runbook, settings, or incident must be recorded.
- A reviewer or verifier needs the exact command that proves a part is healthy.

Do not use this skill for the per-project product code itself (promptflip's features, Plato's parser); those live in each project's own repo and docs. This skill covers hosting, wiring, secrets, DNS, monitoring, and analytics.

## System map

One row per host. "PENDING" means the plan calls for it but it is not live yet; update the row when it goes live. The domain is **`kalpkan.com`** (H1 done 2026-09-18, Cloudflare Registrar, zone id `288a6a2d07f7868c85faa8634f86b885`). Hosts whose Vercel project exists have been moved onto it; the rest still serve from a `*.vercel.app` URL until their project is created and the "Attach a domain" runbook is run.

| Subdomain (planned) | Current URL | Host | Repo | Database | Where env vars live | Status |
|---|---|---|---|---|---|---|
| `@` apex + `www` (hub) | **https://kalpkan.com** (live since 2026-09-18; `https://www.kalpkan.com` 308-redirects to it). `https://portfolio-alpha-eight-rjbs2nj1q0.vercel.app` still works as the fallback alias; `portfolio-kks-projects-2edcb11a.vercel.app` is the team-scoped alias behind Vercel deployment protection, so never use that one | Vercel Hobby, team "Kk's projects" (`kks-projects-2edcb11a`), project `portfolio` | `KalpKan/portfolio` (public), local `~/projects/portfolio` | none | Vercel project settings, Environment Variables (currently none; T0.5 adds the two public PostHog names listed in `settings-map.md`) | live on `kalpkan.com` since 2026-09-18 (T0.4 execution; A `76.76.21.21`, www CNAME `a9e60d5e9d41cb23.vercel-dns-017.com`, both DNS-only) |
| `promptflip.` | **https://promptflip.kalpkan.com** (live since 2026-09-18; `https://promptflip-35qv.vercel.app` still works as the fallback alias) | Vercel Hobby, project `promptflip-35qv` (`prj_t2eXoIxI2HN3snfyFpGAkTqHa4VD`; the duplicate `promptflip` project was deleted 2026-09-18 when H5 resolved "keep 35qv"; `~/projects/promptflip/.vercel/project.json` links to `promptflip-35qv`) | `KalpKan/promptflip` (private), local `~/projects/promptflip` | Supabase **Project A** `nhddxonizdxwbvwcxklu` (isolated; never share) | Vercel, project `promptflip-35qv`, Environment Variables. Six names are listed in `settings-map.md`; `NEXT_PUBLIC_APP_URL` = `https://promptflip.kalpkan.com` since 2026-09-18 | live on `promptflip.kalpkan.com` |
| `hoops.` | **https://hoops.kalpkan.com** (live with real data since 2026-09-18; CNAME `71da701c9c3d8bdf.vercel-dns-017.com`, DNS-only; public fallback alias `https://v0-basketball-analytics-dashboard-seven.vercel.app`). Data route `/api/dashboard` (`source: "live"`); the DB keep-alive is Project B's `health` function, not this host | Vercel Hobby, project `v0-basketball-analytics-dashboard` (Root Directory `apps/web`, pnpm workspace) | `KalpKan/Basketball-Stat-Tracker` (public), local `~/projects/basketball` | Supabase **Project B** `platform` (`yzppfufqaekgaxcrsqxp`), schema **`hoops`** (tables `sessions`, `shot_events`; views `session_summaries`, `overall_analytics`, `progress_over_time`, `shot_map_points`, `session_streaks`; function `health_select_one`). Edge Functions `hoops-ingest-shot`, `health` | Vercel project Environment Variables (six names in `settings-map.md`); Edge Function secrets `INGEST_API_KEY`, `POSTHOG_KEY` in Supabase Project B | live (T1.1, 2026-09-18) |
| `plato.` | **https://plato.kalpkan.com** (live since 2026-09-18; CNAME `89cbb06df93ddb6b.vercel-dns-017.com`, DNS-only; fallback alias `https://plato-one-peach.vercel.app`). Health `https://plato.kalpkan.com/api/health` → `{ok, db, service}` | Vercel Hobby, project `plato` (`prj_IrolBtc6nnhtUVxDhBkGZI24bbAA`), one Python 3.12 function (`src.app:app`), auto-deploys from `main` | `KalpKan/Plato` (public), local `~/projects/plato` | Neon Free, project `nameless-waterfall-55271929`, database `plato`, role `plato_owner` (pooled URL in `DATABASE_URL`) | Vercel project `plato`, Environment Variables: `SECRET_KEY`, `DATABASE_URL`, `POSTHOG_API_KEY`, `POSTHOG_HOST` (`settings-map.md`) | live (T1.3, 2026-09-18). No UptimeRobot monitor yet (add via runbook) |
| `plantit.` | **https://plantit.kalpkan.com** (live since 2026-09-18; CNAME `370dd6cbaae773c9.vercel-dns-017.com`, DNS-only, Cloudflare record `8e680afcb9cdf93bef7933944f5e9b4f`; public fallback alias `https://plantit-kappa.vercel.app` (`plantit.vercel.app` is somebody else's project, never use it)). Health: `/api/health` → `{ok, service:"plantit", firestore, photos, identification, care, spend}` | Vercel Hobby, project `plantit` (`prj_d2ATAx5QfXegcaCY2ZB8KGttDDuX`), CRA static build + one Express function `api/index.js`, auto-deploys from `main` | `KalpKan/PlantWater` (public), local `~/projects/plantit` | Firebase **Spark** `plant-it-5e2fc` (Firestore `users/{uid}/plants`, `spend/` counters; RTDB mirror; Google Auth) — never Blaze. Photos: Supabase Project B Storage bucket `plantit-photos` (public-read, 5 MB, image/*) | Vercel project `plantit` Environment Variables (names in `settings-map.md`); Firebase service account copy in `~/.config/portfolio-ops/plantit-firebase-sa.json`. `PLANTNET_API_KEY` / `OPENAI_API_KEY` not yet supplied (demo mode) | live (T2.1, 2026-09-18) |
| `pushups.` | PENDING | Vercel Hobby, static (MediaPipe JS in browser) | new repo (Phase 3) | none | none | PENDING (Phase 3) |
| `emotes.` | **https://emotes.kalpkan.com** (live since 2026-09-18; CNAME `27cc1c6040a60ce2.vercel-dns-017.com`, DNS-only, Cloudflare record `299df004202d8df449da623799220231`; fallback alias `https://emotes-eight.vercel.app`, never `emotes.vercel.app`, which is a stranger's project). Health: `/health.json` → `{"ok":true,"service":"emotes"}` | Vercel Hobby, project `emotes` (`prj_mgoUdU4NQ9X8sfP7Jhcy4xBBM6pr`, repo root, framework Vite, auto-deploys from `main`), static: MediaPipe Face/Hand/Pose landmarkers, WASM + `.task` models self-hosted under `public/mediapipe/` (39 MB, immutable cache; no CDN, no network after load) | `KalpKan/emote-detector-web` (public), local `~/projects/emotes`; Python original preserved in private `KalpKan/clash-emote-bot-python` | none | Vercel project Environment Variables: `VITE_PUBLIC_POSTHOG_KEY`, `VITE_PUBLIC_POSTHOG_HOST` (`settings-map.md`) | live (T3.2, 2026-09-18); 3 heuristic gestures (flex, yawn, thumbs-up), MobileNetV2 classifier deliberately not shipped; Lighthouse perf 0.91 |
| `microtubules.` | **https://microtubules.kalpkan.com** (live since 2026-09-18; CNAME `bb0edf923bf938a4.vercel-dns-017.com`, DNS-only, Cloudflare record `cde1f34a0408fd944c2f40e9da74108c`; fallback alias `https://microtubules.vercel.app`). Health: `/health.json` → `{"ok":true,"service":"microtubules"}` | Vercel Hobby, project `microtubules` (`prj_8syYmtv9vvJTdSHsfNyCo23GPaen`, Root Directory `web`, framework Vite, auto-deploys from `main`), static: OpenCV.js served from `web/public/opencv.js` (no CDN, offline after load) | `KalpKan/Microtubule-Quantification` (public), local `~/projects/microtubules` | none | Vercel project Environment Variables: `VITE_PUBLIC_POSTHOG_KEY`, `VITE_PUBLIC_POSTHOG_HOST` (`settings-map.md`) | live (T1.4, 2026-09-18); matches the Python pipeline to 4 decimals on 3 sample cells |
| `status.` | `https://stats.uptimerobot.com/a6n3Wx3PBp` (PSP id `1263036`; a custom `status.<domain>` is a paid UptimeRobot feature, so this URL is the permanent one) | UptimeRobot public status page, three HTTP monitors at 5 min (`docs/monitors.md`) | n/a | n/a | UptimeRobot dashboard; `UPTIMEROBOT_API_KEY` in `~/.config/portfolio-ops/secrets.env`, never in a repo | live since 2026-09-18 (T0.3) |
| DNS zone | `kalpkan.com`, zone id `288a6a2d07f7868c85faa8634f86b885`, nameservers `carmelo.ns.cloudflare.com` / `sky.ns.cloudflare.com` | Cloudflare Registrar + DNS (Free plan), records DNS-only (grey cloud) | `docs/DNS_PENDING.md` lists every record with its Cloudflare record id | n/a | Cloudflare dashboard; API token is `CLOUDFLARE_API_TOKEN` in `~/.config/portfolio-ops/secrets.env`, never in a repo | live (H1 done 2026-09-18); apex, www, hoops, promptflip records exist, other subdomains wait on their Vercel projects |
| Supabase Project B `platform` | `https://yzppfufqaekgaxcrsqxp.supabase.co` (health: `/functions/v1/health` → `{"ok":true,"db":"ok"}`) | Supabase Free, ref `yzppfufqaekgaxcrsqxp`, region us-east-1, renamed from ShootIt 2026-09-18. **Schemas: `hoops` (basketball)**; the legacy `public.sessions`/`public.shot_events` copies are still there pending a human checkpoint to drop them. Project A `nhddxonizdxwbvwcxklu` (PromptFlip) is the only other active project, ever | per-app repos | itself | `SUPABASE_ACCESS_TOKEN` in `~/.config/portfolio-ops/secrets.env`; per-app keys in each Vercel project | live since 2026-09-18 (T1.1); keep-alive monitor `804030499` |
| Analytics | https://us.posthog.com/project/616829/dashboard/2112106 (dashboard "Kalp portfolio": visitors by site, by country, top demos) | PostHog Cloud US, free plan, project "Kalp portfolio" id `616829`; no card, hard-capped at the free allocation (a custom `$0` limit needs a card, so the free cap is the guardrail) | `posthog-js` in every app via `/ingest/*` reverse proxy, cookieless, autocapture + replay (inputs masked) + 2-4 custom events; contract in `docs/analytics.md` | n/a | Vercel per app: `NEXT_PUBLIC_POSTHOG_KEY` (public `phc_`) + `NEXT_PUBLIC_POSTHOG_HOST=/ingest`; operator `POSTHOG_PERSONAL_API_KEY` in `~/.config/portfolio-ops/secrets.env` (all-access, rotate after Phase 1) | live since 2026-09-18 (T0.5): hub, hoops, plato sending |
| Showcase pages (`/projects/<slug>` on the hub) | **live since 2026-09-18 (T4.1)**: https://kalpkan.com/projects/unpark, `/projects/rc-car`, `/projects/outline`, `/projects/flashcards`. `classmyschedule (REMOVED 2026-09-18: not Kalp's work)` stays `coming` (placeholder page, H9: authorship unconfirmed). Content is `content/projects/<slug>.ts`, template `components/showcase/`, images `public/images/projects/<slug>/` (WebP ≤ 300 KB, tested), videos never committed (runbook "Add a case study") | hub, rendered from `projects.json` entries with `type: "showcase"` + a content file (test keeps them in lock-step) | `KalpKan/portfolio` | none | none (PostHog: `case_study_repo_clicked {slug}`) | live (4 of 5); media from Kalp pending (STATUS.md H3); RC-car history purge PR open |
| (none: template, not a host) | none. Source: **https://github.com/KalpKan/portfolio-template** (public GitHub *template* repo, `is_template: true`; local `~/projects/portfolio-template`) | not deployed; every new hosted project starts from it (`gh repo create KalpKan/<app> --template KalpKan/portfolio-template`). Next.js 16 App Router + TS + Tailwind 4, vitest, zod, Supabase client scoped to `NEXT_PUBLIC_APP_SCHEMA`, migration `0001_create_schema.sql` (`__APP__` placeholder, grants + default privileges + `health_select_one()`), `/api/health` (`db: ok|skipped|error`), PostHog as the hub (`/ingest`, cookieless, `lib/events.ts` TODO), footer "part of kalpkan.com", CI | `KalpKan/portfolio-template` | none (each app gets its own schema in Project B at creation time) | names only, in its `.env.example` (`settings-map.md` row "every app started from the template") | live as a template since 2026-09-18 (T1.2); proven by a 10 min 05 s throwaway spin-up (runbook "Create a new project from the template") |

Fuller reasoning behind every row is in `architecture.md`.

## First 5 checks (triage, in this order)

Run these before changing anything. Each takes under a minute and tells you which layer is broken. Exact commands are in `verification.md`.

1. **UptimeRobot status page.** Open `https://stats.uptimerobot.com/a6n3Wx3PBp` (public, no login; the monitor inventory with ids is `docs/monitors.md`). If every monitor is red at once, suspect DNS or Vercel; if one is red, go to that app; if `promptflip health (DB)` is red but the promptflip page loads, suspect Supabase Project A (runbook "Restore a paused Supabase project").
2. **The failing app's `/api/health`.** `curl -sf https://<app-url>/api/health`. promptflip returns `db: ok` when its Supabase project is up; the hub returns `{"ok": true, "service": "hub"}`. A 200 here with `db` not ok means the database, not the app. A connection failure means DNS or Vercel.
3. **Vercel deployment logs.** `npx vercel ls <project> --scope kks-projects-2edcb11a` then `npx vercel inspect <deployment-url> --logs`. A failed build shows here; a "Ready" deployment with a failing health route points at env vars or the DB.
4. **Supabase project status.** Open the Supabase dashboard (or the Supabase MCP `list_projects`). Free projects pause after 7 days without activity; a paused project is a one-click restore and then `incidents.md` gets an entry because the keep-alive ping should have prevented it.
5. **Cloudflare DNS record.** `dig +short <sub>.kalpkan.com` should return the `<hash>.vercel-dns-017.com` CNAME target recorded in `docs/DNS_PENDING.md` §5 (subdomain) or `76.76.21.21` (apex `kalpkan.com`). Records must be DNS-only (grey cloud); an orange-cloud record breaks TLS between Cloudflare and Vercel (`curl -sI https://<host> | grep -i ^server:` must say `Vercel`, not `cloudflare`). For an app not yet on the domain the check is `curl -sI https://<app>.vercel.app | head -1` (expect `HTTP/2 200`); a `DEPLOYMENT_NOT_FOUND` body means the Vercel project or deployment is gone, not DNS.

If all five are green and the symptom persists, it is application code, not hosting; go to that project's repo.

## Files in this skill

| File | Read it when |
|---|---|
| `architecture.md` | You need to know why something is the way it is before changing it (layers, the two-Supabase-project rule, DNS map, analytics wiring, keep-alive). |
| `runbooks.md` | You need to do a procedure: deploy, add a project, add a schema, rotate a secret, attach a domain, restore a paused project, add an UptimeRobot monitor, re-point OAuth, regenerate types, purge git history. |
| `incidents.md` | Something went wrong (append an entry) or you want to know whether this has happened before (read). Append-only. |
| `verification.md` | You need the exact command that proves a part is healthy. |
| `settings-map.md` | You need to know which env var lives in which dashboard and what breaks without it. Names only, never values. |

## Update rule

A task is not done until the worker has added the relevant runbook, settings row, or incident entry here, and the reviewer has checked it. Then install the updated copy so `~/.claude/skills/portfolio-ops/` matches the repo: `bash ~/projects/portfolio/scripts/install-ops-skill.sh` (the script lives in the hub repo, mirrors `skills/portfolio-ops/` with `rsync --delete`, and can be run from any directory). The repo copy is the source of truth; never edit the installed copy directly. Every verifier FAIL, reviewer-found bug, and production hiccup becomes an `incidents.md` entry. Names of env vars only, never values.

## Hard constraints to carry into every fix

- Spend stays at $0 plus the domain. Never upgrade Vercel, Supabase, Firebase, Hugging Face, PostHog, or anything else to a paid tier; never attach a card. If a fix seems to need money, stop and raise a human checkpoint in `STATUS.md`.
- Exactly two Supabase projects, ever. Project A is promptflip and is isolated; Project B (`platform`) holds one schema per other app. A DB-only app goes to Neon.
- Never commit a secret. Real values live only in the host's env settings.
- Never delete or force-push anything Kalp made without a human checkpoint, except the items the plan explicitly marks for removal.
