---
name: portfolio-ops
description: Use when operating, repairing, or extending Kalp's project-hosting platform (the hub at KalpKan/portfolio plus every project subdomain on Vercel, Supabase, Neon, Cloudflare, UptimeRobot, PostHog). Triggers include "portfolio is down", "add a project to the portfolio", "Supabase paused", "deploy failed", "subdomain not resolving", "analytics missing", "health check failing", "where does this env var live", "rotate a secret", "attach a domain", or any task in the Phase 0-4 plan that must update the ops record afterwards.
---

# portfolio-ops

## Overview

This is the repair manual and institutional memory for Kalp's portfolio platform: one hub site (Next.js on Vercel Hobby) that links to every project he has built, each project living on its own subdomain, all on free tiers, with the `.com` domain as the only paid line item.

Core principle: **this skill is written during the build, not after.** Every task that changes the platform appends to one of the five files below before it counts as done. If you are an agent who has never seen this project, read this file top to bottom, then open the file that matches your problem. The system design that all of this executes is `docs/hosting-plan.md` in the hub repo; do not re-litigate it, execute it, and if reality contradicts it, update the doc with evidence.

## When to use

- Something is broken: a site is down, a badge is grey, a deploy failed, a subdomain does not resolve, Supabase says "paused", analytics stopped arriving.
- Something is being added: a new project, a new subdomain, a new Supabase schema, a new secret.
- A task in `docs/superpowers/plans/` just finished and its runbook, settings, or incident must be recorded.
- A reviewer or verifier needs the exact command that proves a part is healthy.

Do not use this skill for the per-project product code itself (promptflip's features, Plato's parser); those live in each project's own repo and docs. This skill covers hosting, wiring, secrets, DNS, monitoring, and analytics.

## System map

One row per host. "PENDING" means the plan calls for it but it is not live yet; update the row when it goes live. The domain is **`kalpkan.com`** (H1 done 2026-09-18, Cloudflare Registrar, zone id `288a6a2d07f7868c85faa8634f86b885`). Every planned host is on the domain: the Phase 1 audit (2026-09-18 22:40Z) and the Phase 2–4 audit (2026-09-19 00:15Z) both confirmed all nine `kalpkan.com` hosts answer `HTTP/2 200` (www: 308) with valid TLS and `server: Vercel`. The `*.vercel.app` URLs in each row are fallback aliases only.

| Subdomain (planned) | Current URL | Host | Repo | Database | Where env vars live | Status |
|---|---|---|---|---|---|---|
| `@` apex + `www` (hub) | **https://kalpkan.com** (live since 2026-09-18; `https://www.kalpkan.com` 308-redirects to it). `https://portfolio-alpha-eight-rjbs2nj1q0.vercel.app` still works as the fallback alias; `portfolio-kks-projects-2edcb11a.vercel.app` is the team-scoped alias behind Vercel deployment protection, so never use that one | Vercel Hobby, team "Kk's projects" (`kks-projects-2edcb11a`), project `portfolio` | `KalpKan/portfolio` (public), local `~/projects/portfolio` | none | Vercel project settings, Environment Variables: `NEXT_PUBLIC_POSTHOG_KEY`, `NEXT_PUBLIC_POSTHOG_HOST` (T0.5; names in `settings-map.md`) | live on `kalpkan.com` since 2026-09-18 (T0.4 execution; A `76.76.21.21`, www CNAME `a9e60d5e9d41cb23.vercel-dns-017.com`, both DNS-only). **Front end is KalpOS since T6 (2026-09-20):** a power screen (T6.4: pure black, a faint ⏻, "press any key to start"; the key/click/tap sounds the original startup chime inside that gesture, mute toggle in the menubar) then a macOS-style boot (white KK mark, a thin progress bar that fills with the health round, ≥ 2.1 s from the key, ≤ 4 s; `lib/boot.ts`, `components/kalpos/BootScreen.tsx`) then a lock screen (type anything, Enter; no chime at unlock) over a desk, **on every plain visit** since the 2026-09-20 fixes (no localStorage skip; `/projects/<slug>` and `?desk` land on the desk directly); the Projects folder / window lists the registry as tiles (`lib/tiles.ts`), a live tile turns cyan when the browser's `/api/status/<slug>` check answers `ok: true`, case studies open inside windows, `/projects/<slug>` deep-links open the desk with that window; ≤ 768 px folds to the 1e phone sheet. **T6.1 (2026-09-20):** the Trash is an SVG basket (empty/full from `lib/scrapped.ts`) and the dock's Terminal is an interactive read-only shell over a filesystem derived from the registry (`lib/vfs.ts`, `lib/shell.ts`; runbook "Add a project", "The Terminal reads the same registry"), with a scripted `claude` session (`lib/fake-claude.ts`, no network). **T6.2 (2026-09-20):** the KalpOS word in the menubar (■ KalpOS on the phone) is a menu: About, Lock Screen (⌃⌘Q since T6.3; ⌘L is the browser's), Restart… (⌃⌘R, confirm sheet) which replays the whole boot without a page load (`components/kalpos/KalpOSMenu.tsx`, `RestartSheet.tsx`; terminal `reboot`/`lock`; PostHog `menu_action {item}`; verification row "KalpOS menu → Restart replays boot→lock"). **T6.7 (2026-09-20):** the Music app: `lib/site.ts` `PLAYLIST` (three songs; edit that list to change them) feeds the NOW PLAYING widget (`components/kalpos/NowPlaying.tsx`), the Music window (`windows/MusicWindow.tsx`: generated hashed-gradient covers, never fetched art; transport with a fake 3:20 loop, no audio), the phone sheet and the terminal's `/about/music.md`; one shared store `usePlayer.ts`, pure reducer `lib/player.ts`; PostHog `music_track_selected {index}`; verification row "Music app (T6.7)". **T6.3 (2026-09-20):** the desk icons can be dragged anywhere and snap to the 22 px dots (`lib/icons.ts`, persisted in `localStorage` `kalpos:icons`, Clean Up ⌥⌘1 in the KalpOS menu), an icon dropped into the Trash is crumpled, sunk and swatted back out by a drawn hand (`lib/swat.ts`, `TrashSwat.tsx`, PostHog `trash_swat {icon}`), windows close in place and minimise into their own dock tile with one animation each (the old close reused the open keyframes reversed, which CSS never restarts: incident 2026-09-20; proof `docs/reports/evidence/kalpos-close-check.mjs`), and the lock shortcut is ⌃⌘Q (⌘L is the browser's address bar). Verification rows "KalpOS window close / minimise beats", "KalpOS desk icons move, persist, Clean Up", "KalpOS Trash swat", "KalpOS lock shortcut is ⌃⌘Q". Design source `docs/design/KalpOS-Mockups.dc.html`, system in `DESIGN.md`, spec `docs/superpowers/specs/2026-09-20-kalpos-frontend-design.md`, plan `docs/superpowers/plans/2026-09-20-kalpos.md` |
| `promptflip.` | **https://promptflip.kalpkan.com** (live since 2026-09-18; `https://promptflip-35qv.vercel.app` still works as the fallback alias) | Vercel Hobby, project `promptflip-35qv` (`prj_t2eXoIxI2HN3snfyFpGAkTqHa4VD`; the duplicate `promptflip` project was deleted 2026-09-18 when H5 resolved "keep 35qv"; `~/projects/promptflip/.vercel/project.json` links to `promptflip-35qv`) | `KalpKan/promptflip` (private), local `~/projects/promptflip` | Supabase **Project A** `nhddxonizdxwbvwcxklu` (isolated; never share) | Vercel, project `promptflip-35qv`, Environment Variables. Six names are listed in `settings-map.md`; `NEXT_PUBLIC_APP_URL` = `https://promptflip.kalpkan.com` since 2026-09-18 | live on `promptflip.kalpkan.com` |
| `hoops.` | **https://hoops.kalpkan.com** (live with real data since 2026-09-18; CNAME `71da701c9c3d8bdf.vercel-dns-017.com`, DNS-only; public fallback alias `https://v0-basketball-analytics-dashboard-seven.vercel.app`). Data route `/api/dashboard` (`source: "live"`, every number computed in `apps/web/lib/dashboard-data.ts` from raw rows on a device + UTC-day basis, pre-2000 rows hidden); health `/api/health` → `{ok, db, service:"hoops"}` since FIX r1 (`7eead57`, live 2026-09-19 02:35 UTC; monitor `804030256` checks it); the DB keep-alive is Project B's `health` function | Vercel Hobby, project `v0-basketball-analytics-dashboard` (Root Directory `apps/web`, pnpm workspace) | `KalpKan/Basketball-Stat-Tracker` (public), local `~/projects/basketball` | Supabase **Project B** `platform` (`yzppfufqaekgaxcrsqxp`), schema **`hoops`** (tables `sessions`, `shot_events`; views `session_summaries`, `overall_analytics`, `progress_over_time`, `shot_map_points`, `session_streaks`; function `health_select_one`). Edge Functions `hoops-ingest-shot`, `health` | Vercel project Environment Variables (six names in `settings-map.md`); Edge Function secrets `INGEST_API_KEY`, `POSTHOG_KEY` in Supabase Project B | live (T1.1, 2026-09-18) |
| `plato.` | **https://plato.kalpkan.com** (live since 2026-09-18; CNAME `89cbb06df93ddb6b.vercel-dns-017.com`, DNS-only; fallback alias `https://plato-one-peach.vercel.app`). Health `https://plato.kalpkan.com/api/health` → `{ok, db, service}` | Vercel Hobby, project `plato` (`prj_IrolBtc6nnhtUVxDhBkGZI24bbAA`), one Python 3.12 function (`src.app:app`), auto-deploys from `main` | `KalpKan/Plato` (public), local `~/projects/plato` | Neon Free, project `nameless-waterfall-55271929`, database `plato`, role `plato_owner` (pooled URL in `DATABASE_URL`) | Vercel project `plato`, Environment Variables: `SECRET_KEY`, `DATABASE_URL`, `POSTHOG_API_KEY`, `POSTHOG_HOST` (`settings-map.md`) | live (T1.3, 2026-09-18). UptimeRobot monitor `804031239` `plato health (DB, Neon)` on `/api/health` (added 2026-09-18 by the Phase 1 audit; on the status page). Phase 5: FIX r2 `d162e7b` live (parser `19b20e72f3c9`); **TEST r3 2026-09-19 (`docs/reports/plato.md`): PARTIALLY WORKING 66/100; round-2 D13–D17, D19 fixed live; six random outlines labelled (`a354b20`, 24-file gate fails `clean_titles` 94.96 %); open: D21 blocker (invented final-exam dates on Biol 3415G / MOS 2242A), D22 (two-day lecture picker drops the lecture), D23 (printed lab slot + "day of lab" rule), D24 (prose test dates, Format column in titles), D25 (Fall/Winter header). **FIX r3 `76df2b0` live 2026-09-19 (parser `cf69b6354703`): D21–D25 + D18 residue fixed and verified live; gate 13/13 on 24 files with a strict `no_fabricated`; a second corpus test checks the default download's lecture series; still pending: Google/Apple import screenshots. **TEST r4 2026-09-19 (`docs/reports/plato.md`): PARTIALLY WORKING 72/100; D21–D25 + D18 fixed live; five more random outlines labelled (`0bbf8d7`), 29-file gate fails `no_fabricated` 67/69 (HS 2610G midterm on reading week, Calc 1301B quizzes on "week of Jan 19": D26 blocker), D27 prose tutorial slots, D28 titles; live 42/42 in 1.5–6.2 s, 290 events clean under two RFC parsers** |
| `plantit.` | **https://plantit.kalpkan.com** (live since 2026-09-18; CNAME `370dd6cbaae773c9.vercel-dns-017.com`, DNS-only, Cloudflare record `8e680afcb9cdf93bef7933944f5e9b4f`; public fallback alias `https://plantit-kappa.vercel.app` (`plantit.vercel.app` is somebody else's project, never use it)). Health: `/api/health` → `{ok, service:"plantit", firestore, photos, identification, care, spend}` | Vercel Hobby, project `plantit` (`prj_d2ATAx5QfXegcaCY2ZB8KGttDDuX`), CRA static build + one Express function `api/index.js`, auto-deploys from `main` | `KalpKan/PlantWater` (public), local `~/projects/plantit` | Firebase **Spark** `plant-it-5e2fc` (Firestore `users/{uid}/plants`, `spend/` counters; RTDB mirror; Google Auth) — never Blaze. Photos: Supabase Project B Storage bucket `plantit-photos` (public-read, 5 MB, image/*) | Vercel project `plantit` Environment Variables (names in `settings-map.md`); Firebase service account copy in `~/.config/portfolio-ops/plantit-firebase-sa.json`. `PLANTNET_API_KEY` set 2026-09-18 (Kalp's key; health says `identification: "plantnet"`); `OPENAI_API_KEY` is **never** set and, since T2.2 (`36a6432`, 2026-09-19), not even read: a visitor may type their own key into the app (browser localStorage, per-request `X-OpenAI-Key` header, never stored or logged server-side), so `care: "bundled"` + `careWithVisitorKey: "openai"` is correct. CI: `.github/workflows/ci.yml` (jest + CRA build) | live (T2.1, 2026-09-18; Phase 2–4 audit PASS 2026-09-19). Production = `e67d21f` since 2026-09-18 23:47 UTC (deployment `plantit-61y8q97tt`): reviewer fixes `72b7c3c` (dotenv for local run, device-secret firmware routes, honest hardware-mode watering, connect-device validation, phone nav, no dialog flicker), the persisted waiting-for-sensor flag (`ca980a7`) and a labelled placeholder for pre-2026 Firebase Storage photos that Spark can no longer serve (`e67d21f`). T2.2 + CI + the identification corpus (`tests/fixtures/plants/`, `scripts/run-corpus.js`, spec `docs/reports/plantit-spec.md`) pushed 2026-09-19 (`36a6432`, `a50344c`); Phase 5 round 0 baseline: identification 15/15 genus, the open defects are the non-plant fallback and snake-plant/ZZ care. TEST round 1 (2026-09-19, `docs/reports/plantit.md`): PARTIALLY WORKING, 4/10 stories at the bar; blocker = mug saved as demo Monstera; majors = snake-plant/ZZ care, low-confidence display, blank unknown route, key fragment in Vercel logs, Lighthouse mobile 0.75/0.68, button contrast 1.8:1. **FIX round 1 (`1d1894b` + `fad7516`, 2026-09-19, CI green; live since 03:23 UTC): mug → `422 notAPlant` before any write, snake plant/ZZ dry-out care (synonyms + ZZ entry + careBar per corpus item), low-confidence warning + runner-ups + chip, not-found page, key never logged, `main.js` 246 → 182 KB gz + no sign-in iframe at load + lazy pages, buttons `#062b1c` on green (9.6:1); 61 API + 19 frontend tests incl. the offline corpus replay; local production-shaped stack: corpus 7/7 bars, Lighthouse 0.99/0.92. (the deploy was refused twice by Vercel's rolling window, then accepted at 03:23 UTC; live rows in `verification.md` re-run 2026-09-19 20:47–21:20 UTC)**. **TEST round 2 (2026-09-19 03:20 UTC, `docs/reports/plantit.md`): PARTIALLY WORKING; live = round 1 (4/10, mug still saved, score 58); `fad7516` on a real-dependency local stack = 9/10 (corpus 7/7, 0 console errors, D1–D5, D7, D10–D13 fixed); still open: D14 deploy, D6 `/plants` Lighthouse 0.77–0.80 with real plants, D8 dialog hides Water now at both widths, D9, D15, D16.** **FIX round 2 (2026-09-19): `fad7516` turned out to be live since 03:23 UTC (the CLI attempt was accepted, the Mac slept, see incidents), and `5b77609` deployed by `git push` at 21:15 UTC (deployment `plantit-1qr7pvfy9`, bundle `main.256a2826.js`): D14 closed; D6: 400 px card renditions, no framer (main.js 144 KB gz), preconnects, PostHog after content, readings in `/api/plants` → production Lighthouse `/plants` 0.91–0.95, `/login` 0.96–0.99 with 3 real plants; D8: dialog leads with the reading, Water now above the fold at 1440×900 and 390×844; D9 done (moisture on cards, neutral sensor chip). Live proof: corpus subset 7/7 (mug 422, snake plant/ZZ threshold 8), `/api/nope` clean, 0 `sk-` in `vercel logs`. 65 API + 31 web tests, CI green. Open: D15 (runner-ups not selectable), D16 (mug stays loaded).** **TEST round 3 (2026-09-20 00:10 UTC, `docs/reports/plantit.md`): WORKING, 10/10 spec stories at the bar on the live `5b77609` (full corpus on production 7/7, harness at 1440/390, 0 console errors, Lighthouse `/login` 0.97–0.99 and `/plants` 0.92–0.94, 0 `sk-` in logs, all round-1/2 defects fixed live except D15/D16); grade not-yet, score 84: two new majors outside the corpus, D17 (at 50/50 Pl@ntNet calls a real photo is saved as a bundled species with "Confidence: 89 %") and D18 (phone photos over 4 MB refused instead of downscaled), plus minors D19–D22. Next: FIX round 3 (D17, D18, then D15/D16/D19–D22), then review + verifier.** UptimeRobot `804031032`. Getting `ca980a7` out took retries: Vercel's team-wide 100/day limit refused deploys intermittently on 2026-09-18 (`incidents.md`, runbook "Deploy an Express+CRA app to Vercel" common failures) |
| `pushups.` | **https://pushups.kalpkan.com** (live since 2026-09-18; CNAME `80c9fa355067b1a6.vercel-dns-017.com`, DNS-only, Cloudflare record `aa8d4ae5b08733159fa34067c5aa9b02`). Health: `/health.json` → `{"ok":true,"service":"pushups"}` | Vercel Hobby, project `pushups` (`prj_YE2wfMUkft3oHIaqkRyILcaE8a2R`, repo root, framework Vite, auto-deploys from `main`), static: MediaPipe PoseLandmarker **full** model + WASM self-hosted (`public/models/`, `public/wasm/` copied at build), form judged by geometry rules only since FIX r3 (2026-09-19; the TF.js classifier and `@tensorflow/tfjs` were removed, `/models/form-v3/` 404s), no network after load | `KalpKan/pushup-tracker-web` (public), local `~/projects/pushups`; Python original `KalpKan/AI-Pushup-Form-Tracker` (purge PR #1 open), read-only clone `~/projects/pushups-python` | none | Vercel project Environment Variables: `VITE_PUBLIC_POSTHOG_KEY`, `VITE_PUBLIC_POSTHOG_HOST` (`settings-map.md`) | live (T3.1, 2026-09-18; reviewer APPROVE, verifier PASS, Phase 2–4 audit PASS 2026-09-19). FIX r1 2026-09-19 (`0dbdc48`): time-based body-scaled rep counter, geometry rules with reasons, classifier v2 retrained on the site's own landmarks (96 % held-out, same person; gated to the training orientation), placement hints; corpus gate in `npm test` on Python and browser landmark traces (14/15, one known miss); live fake-camera corpus 14/15, 14/15, 13/15 with attempts identical across runs; runbook "Deploy a browser-ML app (MediaPipe) to Vercel". **TEST r2 (2026-09-19, `docs/reports/pushups.md`): PARTIALLY WORKING 58/100; counting right and stable (14/15 ×3, all numbers identical across runs, demo 4/4 ×3, hints < 1 s) but a NEW BLOCKER: `/models/form/*` was replaced in place under a one-year `immutable` header, so every returning browser (Kalp's Chrome included) loads the 36-input v1 classifier into 24-input code → black stage, "0 good of 0", silent exceptions; FIX r2 must version the model URL and catch errors in the frame loop. Majors: 13/69 rep verdicts wrong, knee pushups never counted, hint debounce resets on flickering detection.** **FIX r2 2026-09-19 (`634fdc4` + `a4870a4`, live via the Git integration 23:39 UTC, deployment `pushups-c20lxmvw1`, CI green): D1 closed (model at the versioned `/models/form-v3/`, `MODEL_URL` bumped per retrain, old path 404s, wrong-width refusal + cache-bypassing retry, frame loop stops with a status message and a `session_failed` event; `scripts/e2e-failure-modes.mjs` passes live); D2: classifier v3 (augmented, 5-seed ensemble) consulted over the bottom + ascent, top judged on the 0.4 s before the descent → 65/67 verdicts on both trace sets (was 62/57), `test_video` no longer a known miss, ratchet 0.95; D3: knee pushups count as "knees down" attempts, plank-to-knees drops do not; D4–D6: hints held with hysteresis, phantom second pose ignored, counting paused (grey skeleton) for no pose / dark / two people / frontal / head or feet well outside. `npm test` 139 passed; live fake-camera corpus 15/15 ×3 with every number identical across runs and equal to the offline gate; demo 4/3 ×3. Residual: the demo's 6.2 s worm rep is a classifier coin flip (graded good in camera mode), `IMG_1513`'s mild pike is graded good, D8 (slow-device warning) open.** **TEST r3 (2026-09-19, `docs/reports/pushups.md`): PARTIALLY WORKING 68/100 on the live `634fdc4`: round-2 D1, D3–D5 fixed, D2/D6/D7 mostly; counting at the bar (15/15 ×3 identical, 29.6 fps, first rep, Playwright Chromium 5/5, demo 4/3 ×3 graded as labelled); NEW blocker D1: the bad-form clips mirrored (visitor facing the other way) grade 4/4, 4/4, 4/3 good because the classifier is gated off for that facing (`form.ts:106`); majors D2 per-frame classifier noise (red flicker on clean reps, verdict depends on frame rate on 4/15 clips), D3 false permanent "Head out of frame" on `test_video`/`test_video_2`; D4–D9 minors.** **FIX r3 (2026-09-19, `docs/reports/pushups.md` r3 D1–D3 + D6–D9): the neural classifier was retired (held out honestly it was a coin on Kalp's own unseen bad reps and "bad" on every clean rep of a second person; its facing gate was a same-person proxy), verdicts are geometry over the top and bottom windows with two new bottom rules ("dropped to the floor", chest-up sag), a third trace set of every clip flipped (`tests/fixtures/traces-browser-mirrored/`) is in the gate, the head hint keys on the collapsed size of a cut-off head, tiles reset when the demo ends, a slow-device line appears under 8 fps; `npm test` 172 passed + 4 `it.fails` known misses (`bad_IMG_4456` in every facing, `bad_IMG_4451` on Python landmarks); corpus verdicts 55/58/57 of 67 per set, identical across facings and frame rates; live fake camera 14/15 both facings (the known miss), demo 4/4 ×3 in both modes.** **TEST r4 (2026-09-20, `docs/reports/pushups.md`): PARTIALLY WORKING 74/100 on the live `b622fa3`: round-3 D1–D3, D6, D8, D9 fixed (mirrored bad clips 4/0, 4/1; demo 0 red frames; no false head hint; slow-device line at 6 fps), D4/D5 and the pause flicker not; counting at the bar (14/15 ×3 identical in both facings, 30 fps min 25, 0 phantom reps, Playwright Chromium 5/5, Lighthouse 0.97/1.00/1.00); NEW blocker D1: 9 of 21 high-confidence bad reps graded good in both facings (`bad_IMG_4456` 4/4, the worm ascents, `IMG_1513`), verdicts 58/68 live; majors D2 (mirrored `bad_IMG_4451` good count 1→4 with the frame-drop phase), D3 (mirrored `IMG_1513` phantom attempt), D4 (demo says 4/4 under a caption saying 2–3); minors D5–D7.** UptimeRobot liveness monitor `804031518` on `/health.json` (added 2026-09-19 by the Phase 2–4 audit; on the status page) |
| `emotes.` | **https://emotes.kalpkan.com** (live since 2026-09-18; CNAME `27cc1c6040a60ce2.vercel-dns-017.com`, DNS-only, Cloudflare record `299df004202d8df449da623799220231`; fallback alias `https://emotes-eight.vercel.app`, never `emotes.vercel.app`, which is a stranger's project). Health: `/health.json` → `{"ok":true,"service":"emotes"}` | Vercel Hobby, project `emotes` (`prj_mgoUdU4NQ9X8sfP7Jhcy4xBBM6pr`, repo root, framework Vite, auto-deploys from `main`), static: MediaPipe Face/Hand/Pose landmarkers, WASM + `.task` models self-hosted under `public/mediapipe/` (39 MB, immutable cache; no CDN, no network after load) | `KalpKan/emote-detector-web` (public), local `~/projects/emotes`; Python original preserved in private `KalpKan/clash-emote-bot-python` | none | Vercel project Environment Variables: `VITE_PUBLIC_POSTHOG_KEY`, `VITE_PUBLIC_POSTHOG_HOST` (`settings-map.md`) | live (T3.2, 2026-09-18; reviewer APPROVE, verifier PASS, Phase 2–4 audit PASS 2026-09-19); 3 heuristic gestures (flex, yawn, thumbs-up), MobileNetV2 classifier deliberately not shipped; Lighthouse perf 0.91. TEST r1 (2026-09-19, `docs/reports/emotes.md`): PARTIALLY WORKING 35/100 (D1/D2 blockers, D3–D6 majors). FIX r1 (`4e25a95`, CI unit + corpus green): rules rewritten as weakest-of-named-cues, time-based engine, hints, portrait stage, mute; corpus 100 % everywhere, 70/70 clips (its deploy waited on the 100/day window and went out with FIX r2). TEST r2 (2026-09-19, `docs/reports/emotes.md`): PARTIALLY WORKING, 50/100 on the fixed build (production still 35): round-1 D2–D11 fixed in `4e25a95`, but through the real pipeline a thumb beside the head fires Goblin Muscle 8/13 (per-frame veto in `fuseScores` + pose jitter, blocker), a gesture < 1.5 s after another never plays (2 s gate swallows the edge, major), the hint flickers (major); held-out mirror/portrait/far landmark sets keep precision at 100 % with yawn recall 63–75 %. **FIX r2 (`ea1e718`, live 2026-09-19 21:50 UTC via the Git integration): smoothing before the conflict rules + flex/thumb ratio + one-fist takeover (D1), pending emote gate with a 0.7 s cross-emote gap (D2), time-held hints (D3), nine VIDEO-mode landmark reels as a committed gate; real pipeline tu04x4 12/12 Thumbs Up, fast 3/3, live official PASS at 1000 and 390, Lighthouse 1.00; minors D5/D6/D8/D9 and H11 still open.** **TEST r3 (2026-09-19, `docs/reports/emotes.md`): PARTIALLY WORKING, 72/100 on the live `10c2bae`: round-2 D1–D4 and D7 fixed (33/34 live fake-camera runs at 1000 and 390, thumb beside the head 12/12 + 22/24, back-to-back 3/3, hard reels 0, 10–14 fps throttled PASS, Playwright Chromium PASS, Lighthouse 1.00); open: a clear flex (`flex-09`) plays Thumbs Up 5/6 with the gate widened to accept it (D1 major), flex-12 late 1.5 s, yawn-17 missed, held-out yawn recall 63–75 %, 0.6 s rests merge, phone fold, one unexplained 390 px run, README conflict-rule copy; H11 still open.** **FIX r3 (`51f74f8` + `d80120d`, live 2026-09-20 ~00:45 UTC via `npx vercel --prod`, bundle `index-Cx23TXpj.js`): D1 closed at the root (the "thumbs-up" is the other hand at chest height; the pose is stale 300–400 ms after a cut, so a thumbs-up now waits while the flex is undecided, 200 ms settle, 500 ms cap, flex average restarted when the pose lands), the round-2 `accept` widening deleted, two 30 fps reels added to the gate after a faster variant flipped `tu04x4` live while the 10 fps gate stayed green; live flex09x3 Goblin Muscle 18/18 at both widths, official / tu04x4 / fast / hard PASS, Lighthouse 1.00; 70 unit + 114 corpus; D9 copy fixed; open minors D2–D8, H11.** UptimeRobot liveness monitor `804031519` on `/health.json` (added 2026-09-19; on the status page) |
| `microtubules.` | **https://microtubules.kalpkan.com** (live since 2026-09-18; CNAME `bb0edf923bf938a4.vercel-dns-017.com`, DNS-only, Cloudflare record `cde1f34a0408fd944c2f40e9da74108c`; fallback alias `https://microtubules.vercel.app`). Health: `/health.json` → `{"ok":true,"service":"microtubules"}` | Vercel Hobby, project `microtubules` (`prj_8syYmtv9vvJTdSHsfNyCo23GPaen`, Root Directory `web`, framework Vite, auto-deploys from `main`), static: OpenCV.js served from `web/public/opencv.js` (no CDN, offline after load) | `KalpKan/Microtubule-Quantification` (public), local `~/projects/microtubules` | none | Vercel project Environment Variables: `VITE_PUBLIC_POSTHOG_KEY`, `VITE_PUBLIC_POSTHOG_HOST` (`settings-map.md`) | live (T1.4, 2026-09-18); matches the Python pipeline to 4 decimals on 3 sample cells and to 2 decimals on all 60 corpus images in Chrome **and WebKit** (FIX r1 2026-09-19, `efe7f76`: colour profiles stripped before decoding, analysis in a Web Worker, wrong-file refusals, warnings, downloads, phone scroll; `npm run test:browser` PASS in both engines locally and live, Lighthouse 0.97/0.97/0.99). **TEST r2 2026-09-19: WORKING, 10/10 stories at the bar** (`docs/reports/microtubules.md`; Lighthouse 1.00 ×3; seven minors D12–D18 open, the biggest being the Safari < 16.4 fallback path; real-iPhone/Safari check is H17). UptimeRobot liveness monitor `804031520` on `/health.json` (added 2026-09-19; on the status page) |
| `status.` | `https://stats.uptimerobot.com/a6n3Wx3PBp` (PSP id `1263036`; a custom `status.<domain>` is a paid UptimeRobot feature, so this URL is the permanent one) | UptimeRobot public status page, nine HTTP monitors at 5 min (hub, promptflip, hoops, Project B, plato, plantit, pushups, emotes, microtubules), all on `kalpkan.com` hosts or Project B (`docs/monitors.md`); the three static-demo monitors were added 2026-09-19 | n/a | n/a | UptimeRobot dashboard; `UPTIMEROBOT_API_KEY` in `~/.config/portfolio-ops/secrets.env`, never in a repo | live since 2026-09-18 (T0.3) |
| DNS zone | `kalpkan.com`, zone id `288a6a2d07f7868c85faa8634f86b885`, nameservers `carmelo.ns.cloudflare.com` / `sky.ns.cloudflare.com` | Cloudflare Registrar + DNS (Free plan), records DNS-only (grey cloud) | `docs/DNS_PENDING.md` lists every record with its Cloudflare record id | n/a | Cloudflare dashboard; API token is `CLOUDFLARE_API_TOKEN` in `~/.config/portfolio-ops/secrets.env`, never in a repo | live (H1 done 2026-09-18); all nine records exist (apex, www, promptflip, hoops, plato, plantit, pushups, emotes, microtubules; ids in `docs/DNS_PENDING.md` §5), every one `proxied: false` |
| Supabase Project B `platform` | `https://yzppfufqaekgaxcrsqxp.supabase.co` (health: `/functions/v1/health` → `{"ok":true,"db":"ok"}`) | Supabase Free, ref `yzppfufqaekgaxcrsqxp`, region us-east-1, renamed from ShootIt 2026-09-18. **Schemas: `hoops` (basketball)**; the legacy `public.sessions`/`public.shot_events` copies are still there pending a human checkpoint to drop them. Project A `nhddxonizdxwbvwcxklu` (PromptFlip) is the only other active project, ever | per-app repos | itself | `SUPABASE_ACCESS_TOKEN` in `~/.config/portfolio-ops/secrets.env`; per-app keys in each Vercel project | live since 2026-09-18 (T1.1); keep-alive monitor `804030499` |
| Analytics | https://us.posthog.com/project/616829/dashboard/2112106 (dashboard "Kalp portfolio": visitors by site, by country, top demos) | PostHog Cloud US, free plan, project "Kalp portfolio" id `616829`; no card, hard-capped at the free allocation (a custom `$0` limit needs a card, so the free cap is the guardrail) | `posthog-js` in every app via `/ingest/*` reverse proxy, cookieless, autocapture + replay (inputs masked) + 2-4 custom events; contract in `docs/analytics.md` | n/a | Vercel per app: `NEXT_PUBLIC_POSTHOG_KEY` (public `phc_`) + `NEXT_PUBLIC_POSTHOG_HOST=/ingest`; operator `POSTHOG_PERSONAL_API_KEY` in `~/.config/portfolio-ops/secrets.env` (all-access, rotate after Phase 1) | live since 2026-09-18 (T0.5); audit 2026-09-18: `$pageview` seen from every `*.kalpkan.com` host, custom events from hub, hoops, plato (server-side `posthog-python`, so no `$host` on those), plantit, pushups, emotes, microtubules |
| Showcase pages (`/projects/<slug>` on the hub) | **live since 2026-09-18 (T4.1)**: https://kalpkan.com/projects/unpark, `/projects/rc-car`, `/projects/porsche-pcb-keychain` (KiCad renders); `/projects/eeg` published as "under construction" (registry `coming`, not linked from the hub until H14); `flashcards` is a placeholder page (draft content); classmyschedule removed 2026-09-18 (not Kalp's work, H9 closed); Outline removed 2026-09-18 per Kalp. Content is `content/projects/<slug>.ts`, template `components/showcase/`, images `public/images/projects/<slug>/` (WebP ≤ 300 KB, tested), videos never committed (runbook "Add a case study", incl. the KiCad render steps); reviewer fixes 2026-09-18 22:50Z (stacked diagram edge labels + width test, DRC wording, draft-gate test, `MediaItem.src` local-only, schematics inverted in dark theme) are live: their commit `a47f0d7` was refused by Vercel's daily limit at 22:53 UTC but the next hub push `3c12205` (which contains it) deployed at 23:01 UTC; the Phase 2–4 audit (2026-09-19) confirmed the stacked `>events</tspan>` label on the live RC-car page. Reviewer APPROVE, verifier PASS. **T6.5 (2026-09-20):** every tagline and case study is in Kalp's own words (`docs/content/project-descriptions.md`; the Porsche keychain is a Porsche cut-out with a keychain hole, never a basketball player), media still owed is one table in `docs/content/media-wanted.md` | hub, rendered from `projects.json` entries with `type: "showcase"` + a content file (test keeps them in lock-step) | `KalpKan/portfolio` | none | none (PostHog: `case_study_repo_clicked {slug}`, on the "Top demos by usage" insight since 2026-09-19) | live (3 + 1 under construction); media from Kalp pending (STATUS.md H3); RC-car history purge PR open (H10) |
| (none: template, not a host) | none. Source: **https://github.com/KalpKan/portfolio-template** (public GitHub *template* repo, `is_template: true`; local `~/projects/portfolio-template`) | not deployed; every new hosted project starts from it (`gh repo create KalpKan/<app> --template KalpKan/portfolio-template`). Next.js 16 App Router + TS + Tailwind 4, vitest, zod, Supabase client scoped to `NEXT_PUBLIC_APP_SCHEMA`, migration `0001_create_schema.sql` (`__APP__` placeholder, grants + default privileges + `health_select_one()`), `/api/health` (`db: ok|skipped|error`), PostHog as the hub (`/ingest`, cookieless, `lib/events.ts` TODO), footer "part of kalpkan.com", CI | `KalpKan/portfolio-template` | none (each app gets its own schema in Project B at creation time) | names only, in its `.env.example` (`settings-map.md` row "every app started from the template") | live as a template since 2026-09-18 (T1.2); proven by a 10 min 05 s throwaway spin-up (runbook "Create a new project from the template") |

Fuller reasoning behind every row is in `architecture.md`.

## First 5 checks (triage, in this order)

Run these before changing anything. Each takes under a minute and tells you which layer is broken. Exact commands are in `verification.md`.

1. **UptimeRobot status page.** Open `https://stats.uptimerobot.com/a6n3Wx3PBp` (public, no login; the monitor inventory with ids is `docs/monitors.md`). If every monitor is red at once, suspect DNS or Vercel; if one is red, go to that app (every live app has one, the static demos on `/health.json`); if `promptflip health (DB)` is red but the promptflip page loads, suspect Supabase Project A (runbook "Restore a paused Supabase project").
2. **The failing app's `/api/health`.** `curl -sf https://<app-url>/api/health`. promptflip returns `db: ok` when its Supabase project is up; the hub returns `{"ok": true, "service": "hub"}`. A 200 here with `db` not ok means the database, not the app. A connection failure means DNS or Vercel.
3. **Vercel deployment logs.** `npx vercel ls <project> --scope kks-projects-2edcb11a` then `npx vercel inspect <deployment-url> --logs`. A failed build shows here; a "Ready" deployment with a failing health route points at env vars or the DB.
4. **Supabase project status.** Open the Supabase dashboard (or the Supabase MCP `list_projects`). Free projects pause after 7 days without activity; a paused project is a one-click restore and then `incidents.md` gets an entry because the keep-alive ping should have prevented it.
5. **Cloudflare DNS record.** `dig +short <sub>.kalpkan.com` should return the `<hash>.vercel-dns-017.com` CNAME target recorded in `docs/DNS_PENDING.md` §5 (subdomain) or `76.76.21.21` (apex `kalpkan.com`). Records must be DNS-only (grey cloud); an orange-cloud record breaks TLS between Cloudflare and Vercel (`curl -sI https://<host> | grep -i ^server:` must say `Vercel`, not `cloudflare`). For an app not yet on the domain the check is `curl -sI https://<app>.vercel.app | head -1` (expect `HTTP/2 200`); a `DEPLOYMENT_NOT_FOUND` body means the Vercel project or deployment is gone, not DNS.

If all five are green and the symptom persists, it is application code, not hosting; go to that project's repo.

## Files in this skill

| File | Read it when |
|---|---|
| `architecture.md` | You need to know why something is the way it is before changing it (layers, the two-Supabase-project rule, DNS map, analytics wiring, keep-alive). |
| `runbooks.md` | You need to do a procedure: deploy, add a project, add a schema, rotate a secret, attach a domain, restore a paused project, add an UptimeRobot monitor, re-point OAuth, regenerate types, purge git history. |
| `incidents.md` | Something went wrong (append an entry) or you want to know whether this has happened before (read). Append-only. |
| `verification.md` | You need the exact command that proves a part is healthy. |
| `settings-map.md` | You need to know which env var lives in which dashboard and what breaks without it. Names only, never values. |

## Update rule

A task is not done until the worker has added the relevant runbook, settings row, or incident entry here, and the reviewer has checked it. Then install the updated copy so `~/.claude/skills/portfolio-ops/` matches the repo: `bash ~/projects/portfolio/scripts/install-ops-skill.sh` (the script lives in the hub repo, mirrors `skills/portfolio-ops/` with `rsync --delete`, and can be run from any directory). The repo copy is the source of truth; never edit the installed copy directly. Every verifier FAIL, reviewer-found bug, and production hiccup becomes an `incidents.md` entry. Names of env vars only, never values.

## Hard constraints to carry into every fix

- Spend stays at $0 plus the domain. Never upgrade Vercel, Supabase, Firebase, Hugging Face, PostHog, or anything else to a paid tier; never attach a card. If a fix seems to need money, stop and raise a human checkpoint in `STATUS.md`.
- Exactly two Supabase projects, ever. Project A is promptflip and is isolated; Project B (`platform`) holds one schema per other app. A DB-only app goes to Neon.
- Never commit a secret. Real values live only in the host's env settings.
- Never delete or force-push anything Kalp made without a human checkpoint, except the items the plan explicitly marks for removal.
