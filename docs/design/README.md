# Design working notes for the hub

One line: the visual system is `DESIGN.md` at the repo root (tokens, type, marks, do/don't), the product context is `PRODUCT.md`, and the per-surface direction contract the design tool reads is `.impeccable/surfaces/app-page-tsx.md` (kept there because the tool loads it from that path). Screenshots from each design pass live in `docs/images/` (`hub-polish/` for the 2026-09-18 batch). Nothing in this folder is needed to run the site.
