{
  "run3_merged": {
    "buildModel": "opus",
    "checkModel": "opus",
    "projects": [
      {
        "slug": "plato",
        "repo": "/Users/kalp/projects/plato",
        "url": "https://plato.kalpkan.com",
        "specSources": "the repo README.md, DEPLOYMENT.md, src/ module docstrings, the 'figma landingpage/' folder, tests/, and docs/hosting-plan.md section 1 row for Plato",
        "focus": "Extraction pipeline consistency: every recurring lecture/lab/tutorial slot and every dated assessment (with weights) lands in the .ics correctly across a labelled corpus of real Western outlines; failures explained to the user. Measure recall/precision per event type before and after. NOTE: docs/reports/<slug>-spec.md and possibly docs/reports/<slug>.md already exist in the portfolio repo from a paused earlier run; REUSE the spec and its test assets (update only if wrong), and treat any existing defect report as the previous round to re-test.",
        "rounds": 4
      },
      {
        "slug": "hoops",
        "repo": "/Users/kalp/projects/basketball",
        "url": "https://hoops.kalpkan.com",
        "specSources": "the repo README.md, docs/, apps/web/, packages/contracts, supabase/",
        "focus": "Dashboard reads real hoops rows, math verified, iOS app clearly marked unavailable. NOTE: docs/reports/<slug>-spec.md and possibly docs/reports/<slug>.md already exist in the portfolio repo from a paused earlier run; REUSE the spec and its test assets (update only if wrong), and treat any existing defect report as the previous round to re-test.",
        "rounds": 2
      },
      {
        "slug": "microtubules",
        "repo": "/Users/kalp/projects/microtubules",
        "url": "https://microtubules.kalpkan.com",
        "specSources": "README.md, GETTING_STARTED.md, USAGE_GUIDE.md, STATISTICS_GUIDE.md, web/README.md, src/",
        "focus": "Browser result within 1 pp of Python on every sample and 3 new images; explains the number; handles bad input; works on a phone. NOTE: docs/reports/<slug>-spec.md and possibly docs/reports/<slug>.md already exist in the portfolio repo from a paused earlier run; REUSE the spec and its test assets (update only if wrong), and treat any existing defect report as the previous round to re-test.",
        "rounds": 2
      },
      {
        "slug": "plantit",
        "repo": "/Users/kalp/projects/plantit",
        "url": "https://plantit.kalpkan.com",
        "specSources": "the repo README.md, backend/src/index.js route list, frontend/src components, arduino/ sketch, docs/hosting-plan.md section 1 row for PlantWater",
        "focus": "Kalp: 'Plant It is not working as intended'. Consumer grade = a stranger can sign in, upload a plant photo, get a correct species and sensible care guidance in < 10 s, add the plant, see moisture (simulated device) and trigger watering, and never hit a dead route or console error; hardware-only features clearly labelled. ALSO implement T2.2: an optional 'Use your own OpenAI key' field in the UI (stored only in the visitor's browser localStorage, sent per request in a header, never persisted or logged server-side, never set as a server env var); when absent, care guidance stays the bundled/canned path. Update README accordingly (remove any suggestion that Kalp sets OPENAI_API_KEY). Also add a GitHub Actions CI workflow running the jest suites. NOTE: docs/reports/<slug>-spec.md and possibly docs/reports/<slug>.md already exist in the portfolio repo from a paused earlier run; REUSE the spec and its test assets (update only if wrong), and treat any existing defect report as the previous round to re-test.",
        "rounds": 3
      },
      {
        "slug": "pushups",
        "repo": "/Users/kalp/projects/pushups",
        "url": "https://pushups.kalpkan.com",
        "specSources": "the new repo README.md and docs/superpowers/specs/2026-09-18-pushups-design.md in the portfolio repo, plus the original Python repo ~/projects/pushups-python and the one-pager PDF '/Users/kalp/Desktop/Out and About/Sidequest/One Pagers/AI-Powered Pushup Form Tracker - Google Docs.pdf'",
        "focus": "Kalp: 'it only worked on uploaded videos; I want it real-time in the browser, working very well and consistently, counting pushups correctly'. Consumer grade = live webcam at >= 15 fps on a laptop, rep count within +/-1 of ground truth on every test clip (build ground truth by counting frames yourself), no double counts, good/bad form feedback that matches the one-pager's definition, works on a phone front camera, clear guidance on camera placement. Also add a GitHub Actions CI workflow (vitest with --pool=forks --maxWorkers=1 and per-test timeouts). Deploy at most once per fix round (Vercel Hobby caps 100 deployments/day per team). NOTE: docs/reports/<slug>-spec.md and possibly docs/reports/<slug>.md already exist in the portfolio repo from a paused earlier run; REUSE the spec and its test assets (update only if wrong), and treat any existing defect report as the previous round to re-test.",
        "rounds": 4
      },
      {
        "slug": "emotes",
        "repo": "/Users/kalp/projects/emotes",
        "url": "https://emotes.kalpkan.com",
        "specSources": "the new repo README.md, docs/superpowers/specs/2026-09-18-emotes-design.md in the portfolio repo, and the original Python project '/Users/kalp/Desktop/Out and About/Sidequest/Clash Royale Emote Bot/' README and docs (read-only)",
        "focus": "Kalp: 'it was not detecting the emotes well at all; the model needs a lot of work'. Consumer grade = each of the three gestures (flex, yawn, thumbs-up) fires within 1 s of being performed, with < 1 false trigger per minute of neutral video, on desktop and phone; build ground-truth clips (record or synthesize with MediaPipe landmark fixtures), tune landmark-angle rules with hysteresis/cooldown, consider a small landmark-based classifier trained on collected fixtures (no image model), and report per-gesture precision/recall. Also add a GitHub Actions CI workflow (vitest with --pool=forks --maxWorkers=1 and per-test timeouts). Deploy at most once per fix round (Vercel Hobby caps 100 deployments/day per team). NOTE: docs/reports/<slug>-spec.md and possibly docs/reports/<slug>.md already exist in the portfolio repo from a paused earlier run; REUSE the spec and its test assets (update only if wrong), and treat any existing defect report as the previous round to re-test.",
        "rounds": 4
      }
    ]
  },
  "run1": {
    "buildModel": "opus",
    "checkModel": "opus",
    "projects": [
      {
        "slug": "plato",
        "repo": "/Users/kalp/projects/plato",
        "url": "https://plato.kalpkan.com",
        "specSources": "the repo README.md, DEPLOYMENT.md, src/ module docstrings, the 'figma landingpage/' folder, tests/, and docs/hosting-plan.md section 1 row for Plato",
        "focus": "Extraction pipeline consistency: every recurring lecture/lab/tutorial slot and every dated assessment (with weights) lands in the .ics correctly across a labelled corpus of real Western outlines; failures explained to the user. Measure recall/precision per event type before and after. NOTE: docs/reports/<slug>-spec.md and possibly docs/reports/<slug>.md already exist in the portfolio repo from a paused earlier run; REUSE the spec and its test assets (update only if wrong), and treat any existing defect report as the previous round to re-test.",
        "rounds": 4
      },
      {
        "slug": "hoops",
        "repo": "/Users/kalp/projects/basketball",
        "url": "https://hoops.kalpkan.com",
        "specSources": "the repo README.md, docs/, apps/web/, packages/contracts, supabase/",
        "focus": "Dashboard reads real hoops rows, math verified, iOS app clearly marked unavailable. NOTE: docs/reports/<slug>-spec.md and possibly docs/reports/<slug>.md already exist in the portfolio repo from a paused earlier run; REUSE the spec and its test assets (update only if wrong), and treat any existing defect report as the previous round to re-test.",
        "rounds": 2
      },
      {
        "slug": "microtubules",
        "repo": "/Users/kalp/projects/microtubules",
        "url": "https://microtubules.kalpkan.com",
        "specSources": "README.md, GETTING_STARTED.md, USAGE_GUIDE.md, STATISTICS_GUIDE.md, web/README.md, src/",
        "focus": "Browser result within 1 pp of Python on every sample and 3 new images; explains the number; handles bad input; works on a phone. NOTE: docs/reports/<slug>-spec.md and possibly docs/reports/<slug>.md already exist in the portfolio repo from a paused earlier run; REUSE the spec and its test assets (update only if wrong), and treat any existing defect report as the previous round to re-test.",
        "rounds": 2
      }
    ]
  },
  "run2": {
    "buildModel": "opus",
    "checkModel": "opus",
    "projects": [
      {
        "slug": "plantit",
        "repo": "/Users/kalp/projects/plantit",
        "url": "https://plantit.kalpkan.com",
        "specSources": "the repo README.md, backend/src/index.js route list, frontend/src components, arduino/ sketch, docs/hosting-plan.md section 1 row for PlantWater",
        "focus": "Kalp: 'Plant It is not working as intended'. Consumer grade = a stranger can sign in, upload a plant photo, get a correct species and sensible care guidance in < 10 s, add the plant, see moisture (simulated device) and trigger watering, and never hit a dead route or console error; hardware-only features clearly labelled. ALSO implement T2.2: an optional 'Use your own OpenAI key' field in the UI (stored only in the visitor's browser localStorage, sent per request in a header, never persisted or logged server-side, never set as a server env var); when absent, care guidance stays the bundled/canned path. Update README accordingly (remove any suggestion that Kalp sets OPENAI_API_KEY). Also add a GitHub Actions CI workflow running the jest suites. NOTE: docs/reports/<slug>-spec.md and possibly docs/reports/<slug>.md already exist in the portfolio repo from a paused earlier run; REUSE the spec and its test assets (update only if wrong), and treat any existing defect report as the previous round to re-test.",
        "rounds": 3
      },
      {
        "slug": "pushups",
        "repo": "/Users/kalp/projects/pushups",
        "url": "https://pushups.kalpkan.com",
        "specSources": "the new repo README.md and docs/superpowers/specs/2026-09-18-pushups-design.md in the portfolio repo, plus the original Python repo ~/projects/pushups-python and the one-pager PDF '/Users/kalp/Desktop/Out and About/Sidequest/One Pagers/AI-Powered Pushup Form Tracker - Google Docs.pdf'",
        "focus": "Kalp: 'it only worked on uploaded videos; I want it real-time in the browser, working very well and consistently, counting pushups correctly'. Consumer grade = live webcam at >= 15 fps on a laptop, rep count within +/-1 of ground truth on every test clip (build ground truth by counting frames yourself), no double counts, good/bad form feedback that matches the one-pager's definition, works on a phone front camera, clear guidance on camera placement. Also add a GitHub Actions CI workflow (vitest with --pool=forks --maxWorkers=1 and per-test timeouts). Deploy at most once per fix round (Vercel Hobby caps 100 deployments/day per team). NOTE: docs/reports/<slug>-spec.md and possibly docs/reports/<slug>.md already exist in the portfolio repo from a paused earlier run; REUSE the spec and its test assets (update only if wrong), and treat any existing defect report as the previous round to re-test.",
        "rounds": 4
      },
      {
        "slug": "emotes",
        "repo": "/Users/kalp/projects/emotes",
        "url": "https://emotes.kalpkan.com",
        "specSources": "the new repo README.md, docs/superpowers/specs/2026-09-18-emotes-design.md in the portfolio repo, and the original Python project '/Users/kalp/Desktop/Out and About/Sidequest/Clash Royale Emote Bot/' README and docs (read-only)",
        "focus": "Kalp: 'it was not detecting the emotes well at all; the model needs a lot of work'. Consumer grade = each of the three gestures (flex, yawn, thumbs-up) fires within 1 s of being performed, with < 1 false trigger per minute of neutral video, on desktop and phone; build ground-truth clips (record or synthesize with MediaPipe landmark fixtures), tune landmark-angle rules with hysteresis/cooldown, consider a small landmark-based classifier trained on collected fixtures (no image model), and report per-gesture precision/recall. Also add a GitHub Actions CI workflow (vitest with --pool=forks --maxWorkers=1 and per-test timeouts). Deploy at most once per fix round (Vercel Hobby caps 100 deployments/day per team). NOTE: docs/reports/<slug>-spec.md and possibly docs/reports/<slug>.md already exist in the portfolio repo from a paused earlier run; REUSE the spec and its test assets (update only if wrong), and treat any existing defect report as the previous round to re-test.",
        "rounds": 4
      }
    ]
  }
}
