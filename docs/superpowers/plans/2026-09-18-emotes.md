# T3.2 Emote Detector in the Browser — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship `https://emotes.kalpkan.com`: a static Vite + TypeScript page running MediaPipe Face/Hand/Pose landmarkers in the browser, porting the three working gesture rules (flex → Goblin Muscle, yawn → Princess Yawn, thumbs-up → Thumbs Up) from the Python app, with sound, a cooldown, webcam on desktop and phone, a demo mode, and no network after load.

**Spec:** `docs/superpowers/specs/2026-09-18-emotes-design.md`. Source (read-only): `~/Desktop/Out and About/Sidequest/Clash Royale Emote Bot/` → private `KalpKan/clash-emote-bot-python`.

**Tech stack:** Vite 8 + TypeScript 6, `@mediapipe/tasks-vision` 1.0.1 (WASM + `.task` models self-hosted under `public/mediapipe/`), posthog-js, vitest 5, Vercel Hobby project `emotes`, Cloudflare DNS-only CNAME.

## Global constraints

- Spend $0; no upgrades; no cards; non-commercial. Never commit secrets (`.env.example` names only).
- Do not ship the MobileNetV2 classifier; say so in the README.
- Supercell attribution + "not affiliated" notice (footer + README) linking the Fan Content Policy.
- PostHog contract (`docs/analytics.md`): `/ingest` rewrite with `:path(.*)`, cookieless, events `session_started`, `emote_fired {emote}`, `demo_video_played`.
- vitest with `--pool=forks --maxWorkers=1` and a per-test timeout; kill own node processes before reporting.
- Commits end with `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`.

---

### Task 1: Preserve the Python original
- [x] `git init` the Desktop folder in place, `.gitignore` for `data/raw/`, venvs, caches; commit as-is (`71e3e9f`, 147 files).
- [x] Push to new private repo `KalpKan/clash-emote-bot-python`.

### Task 2: Scaffold `~/projects/emotes` and assets
- [x] `package.json` (Vite, TS, vitest, posthog-js, `@mediapipe/tasks-vision`), `npm install`.
- [x] Copy WASM runtime (SIMD + nosimd) to `public/mediapipe/wasm/`, download the three `.task` models to `public/mediapipe/models/`.
- [x] Convert the three used WAVs to MP3 (mono 32 kHz 48 kbps) and copy the three PNGs into `public/emotes/`; `public/health.json`.
- [x] `tsconfig.json`, `vite.config.ts` (vitest include + timeout), `vercel.json` (ingest rewrites + immutable cache headers for `/mediapipe/*`), `.gitignore`, `.env.example`.

### Task 3: Gesture rules in TypeScript (TDD)
- [x] `src/gestures/geometry.ts`: `clamp`, `normalise`, `dist`, `angle` (ports of the Python helpers) + tests.
- [x] `src/gestures/flex.ts`: `scoreFlex(pose)` = max of `scoreArmFlex` for both arms + fixtures (raised bent arm near head → ≥ 0.5; arm down → 0).
- [x] `src/gestures/thumbsUp.ts`: `scoreThumbStrict(hands)` (port of `_score_thumb_direction`) and `scoreThumbLoose(hands)` (port of `_score_thumbs_up`) + fixtures (thumbs-up → high; open palm → 0; thumbs-down → 0).
- [x] `src/gestures/face.ts`: `faceMetrics(face478)` and `scoreYawn(metrics)` + fixtures (wide mouth + narrowed eyes → > 0; open mouth with open eyes → 0).
- [x] `src/gestures/engine.ts`: `GestureEngine.update({pose, hands, face})` → fused scores, conflict rules, FSM (dwell 3 / cooldown 3), `fired` list; tests for dwell, cool-down, conflicts, one winner.
- [x] `src/emotes.ts`: gesture → emote table (id, name, image, sound) + `EmotePlayer` with 2 s spam prevention + tests for the cooldown timing.

### Task 4: Page
- [x] `index.html`: header, video + canvas stage, "Start camera" / "Play demo" buttons, status line, emote overlay, gesture score meters, "How it works", footer with Supercell notice; phone-first CSS (`src/style.css`).
- [x] `src/landmarkers.ts`: lazy loader for FaceLandmarker / HandLandmarker / PoseLandmarker from `/mediapipe/…` with progress callback.
- [x] `src/main.ts`: camera start (`facingMode: "user"`), demo start (bundled clip, loops), `requestAnimationFrame` loop → landmarkers → engine → overlay + sound; analytics events.
- [x] `src/analytics.ts` (microtubules pattern, three events).
- [x] Demo mode: no video file; `src/demo.ts` replays the test fixtures (tweened) through the real engine with a stick-figure overlay (`tests/demo.test.ts` is the headless twin); `demo_video_played` event.

### Task 5: Verify locally
- [x] `npm test` (forks, 1 worker), `npm run typecheck`, `npm run build`; bundle sizes noted.
- [x] Open the built site in Chrome (`vite preview`), run demo mode, confirm emote fires + sound, Network tab only same-origin + `/ingest`.

### Task 6: Repo + deploy
- [x] README (what it is, the 3 gestures, how it works, MobileNetV2 not shipped and why, Supercell notice, non-developer section "How to run this / How to deploy this / Where the settings live").
- [x] Public repo `KalpKan/emote-detector-web`, first commit, push.
- [x] `npx vercel link --project emotes`, framework Vite, env `VITE_PUBLIC_POSTHOG_KEY` / `VITE_PUBLIC_POSTHOG_HOST` (prod + preview), `vercel --prod`, `git connect`.
- [x] Attach `emotes.kalpkan.com` (runbook: `domains add`, `domains verify --json` → CNAME, Cloudflare DNS-only record, poll health).
- [x] Lighthouse on the live host (≥ 0.85): 0.91; `/ingest/e/` → 400; PostHog events arriving.

### Task 7: Ops record
- [ ] `skills/portfolio-ops/`: SKILL.md system-map row, runbooks.md (emotes specifics), settings-map.md rows, verification.md block, incidents.md entries for anything that failed; `scripts/install-ops-skill.sh`.
- [ ] `STATUS.md` task row + session log; `projects.json` emotes entry (url, healthUrl, repo, status live) after the URL answers 200; `docs/DNS_PENDING.md` §5 row.
- [ ] Commit only own files in the portfolio repo (`git pull --rebase --autostash` first), push; kill own node processes.
