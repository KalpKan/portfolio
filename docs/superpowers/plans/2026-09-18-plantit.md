# T2.1 — PlantWater / "Plant It" repaired and live at plantit.kalpkan.com

_Written 2026-09-18 by the T2.1 worker. Bite-sized, checkbox per step; ticked as executed. Source: `docs/hosting-plan.md` §1, §6 (database plan item 7), §7 item 7; goal-mode appendix Phase 2._

**Goal:** `KalpKan/PlantWater` becomes one Vercel Hobby project `plantit` (CRA static frontend + Express as the `/api` serverless function), Firebase stays on Spark (Firestore, RTDB, Google Auth on project `plant-it-5e2fc`), plant photos go to Supabase Storage bucket `plantit-photos` in Project B, a visitor can use the whole app with no hardware (simulated sensor + "Water now"), and every paid API call is capped per day with a canned fallback. $0.

**Decisions (made, not asked):**
- Repo layout stays `frontend/` + `backend/`; a root `package.json` owns the backend deps and the Vercel build; `api/index.js` exports the Express app. One repo, one Vercel project.
- `sharp` first (Vercel's Linux builder installs `@img/sharp-linux-x64` from the lockfile); switch to `jimp` only if the deploy fails. The README records which one shipped.
- Firebase Admin credentials come from the service-account file Kalp already has locally (copied to `~/.config/portfolio-ops/`, never into the repo) and live in Vercel env as `FIREBASE_PROJECT_ID`, `FIREBASE_CLIENT_EMAIL`, `FIREBASE_PRIVATE_KEY`.
- Pl@ntNet / OpenAI keys are not on this machine: the app ships a demo path (bundled lookup of 5 common plants, deterministic per photo) and STATUS.md gets a "Needs Kalp" item naming the two env vars.
- The old per-user lifetime cap of 2 identifications is replaced by the global per-day spend guard (50/day each for Pl@ntNet and OpenAI, Firestore counter), so visitors are never locked out and Kalp is never billed beyond the free tiers.

## Steps

### A. Repo repair
- [x] A1 Clone to `~/projects/plantit`, read README + code.
- [x] A2 Fix `.firebaserc` (only `default: plant-it-5e2fc`), trim `firebase.json` to rules, delete `backend/src/minimal-test.js`, `backend/src/cloud-run-example.js`, `backend/test-local.js`, `backend/start.js`, `backend/Dockerfile`, `backend/.dockerignore`, `apphosting.backend.yaml`, `(public)/`; gitignore `.firebase/` and un-ignore `.env.example`.
- [x] A3 Root `package.json` (backend deps + jest), `api/index.js`, `vercel.json` (build, output `frontend/build`, `/ingest` proxy, `/api/*` → function, SPA fallback).

### B. Backend
- [x] B1 Split `backend/src/index.js` into `app.js` (exports app, lazy Firebase init, env-driven CORS) + `index.js` (local listen).
- [x] B2 `spendGuard.js` with jest tests (allows to limit, denies after, new day resets, fake Firestore transaction).
- [x] B3 `demoPlants.js`: 5 bundled plants (Pl@ntNet-shaped candidates + care) and `pickDemoPlant(buffer)`; care lookup by species/genus.
- [x] B4 `/api/identify`: guard → Pl@ntNet or demo; care: guard → OpenAI or canned/fallback; photo → Supabase Storage (`storage.js`), never Firebase Storage.
- [x] B5 `simulatedDevice.js` with jest tests (moisture decays from lastWatered, clamps, deterministic per plant; watering resets) + routes `GET /api/plants/:id/device`, `POST /api/plants/:id/water` (logs event in Firestore `events` subcollection, updates RTDB).
- [x] B6 `/api/health` → `{ok, service:"plantit", firestore:"ok"|"error"}` (503 on error); remove test endpoints.
- [x] B7 `npm test` passes (`jest --runInBand`).

### C. Frontend
- [x] C1 `config.js`: `REACT_APP_API_URL` default same-origin; all components use `apiUrl()`; dev proxy `localhost:3001`.
- [x] C2 PostHog (`analytics.js`, boots after `load`, `/ingest`, cookieless); events `plant_photo_uploaded`, `plant_identified`, `water_now_clicked`.
- [x] C3 Plant dialog: "Simulated sensor" panel (moisture, threshold, Water now, last events) when no hardware has reported; real-device buttons labelled "hardware required"; "Demo result" chip on demo identifications.
- [x] C4 `npm run build` clean (CI warnings fixed, not silenced).

### D. Platform
- [x] D1 Supabase Storage bucket `plantit-photos` in Project B (public, 5 MB, image/*), service-role key only in Vercel env.
- [x] D2 Vercel project `plantit` linked from the repo, env set non-interactively (prod + preview), `npx vercel --prod --yes`; `/api/health` 200 with `firestore: ok` on the `.vercel.app` URL.
- [x] D3 Firebase Auth authorized domains += `plantit.kalpkan.com` (Identity Toolkit API with the service account, else browser). Done: list is `localhost`, `plant-it-5e2fc.firebaseapp.com`, `plant-it-5e2fc.web.app`, `plantit.site`, `plantit.kalpkan.com`, `plantit-kks-projects-2edcb11a.vercel.app`, `plantit-kappa.vercel.app` (the foreign `plantit.vercel.app`, which is not our project, was removed).
- [x] D4 Attach `plantit.kalpkan.com` per runbook (`domains add`, config API CNAME, Cloudflare DNS-only record, poll HTTPS 200); `docs/DNS_PENDING.md` §5 row. Done 2026-09-18 17:55: CNAME `370dd6cbaae773c9.vercel-dns-017.com`, Cloudflare record `8e680afcb9cdf93bef7933944f5e9b4f`, HTTPS 200 ~35 s after the record.
- [x] D5 UptimeRobot v3 monitor `plantit health (Firestore)` on `/api/health`, added to PSP 1263036; `docs/monitors.md` row. Done: monitor id `804031032`.

### D2 addendum (second worker, 2026-09-18 evening)
- [x] D2b Production bug found by the API smoke test: `/api/identify` and `/api/plants/:id/water` answered 500 `Cannot read properties of undefined (reading 'fromMillis')` because `backend/src/firebase.js` did not expose `Timestamp` (the jest fake did). Fixed with `backend/src/firebase.test.js` (27 tests now). Commit `0f068db` in `KalpKan/PlantWater`.
- [x] D2c Login page: `alert()` on a cancelled popup froze the tab; replaced by an inline message + disabled button while the popup is open (same commit).

### E. Proof, docs, ops
- [ ] E1 claude-in-chrome on the live host: Google sign-in (stop at consent if it needs Kalp), upload a plant photo, see identification + care, Water now on the simulated sensor; screenshots to `~/projects/plantit/docs/images/`.
- [ ] E2 PostHog: the three events arrive with `$host = plantit.kalpkan.com`.
- [ ] E3 README (non-developer: how to run / deploy / where settings live), `.env.example` names only, `arduino/` note that hardware routes are optional.
- [ ] E4 Ops skill: runbook "Deploy an Express+CRA app to Vercel", settings-map rows, verification block, SKILL.md row, incidents for anything that failed; `install-ops-skill.sh`.
- [ ] E5 STATUS.md (task row, session log, Needs-Kalp item for the two API keys), `projects.json` (url, healthUrl, live), commit + push both repos.
