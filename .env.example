# The hub needs no secrets in Phase 0.
# When it does (for example NEXT_PUBLIC_POSTHOG_KEY in T0.5), list the NAME here
# with no value, and put the real value in Vercel: Project "portfolio" -> Settings -> Environment Variables.
# Never commit a real .env file; .gitignore already blocks them.
